# 🔖 セッション引き継ぎ（最終更新: 2026-08-31 20:30）

## 🆕 2026-08-31: 自動公開レーンが2本、黙って止まっていた（キュー枯渇・番人の口が1つしか無かった）

**症状**＝ルーティンは毎日きちんと走り、台帳に「🚩キュー枯渇・補充依頼」を書き続けていた。
**誰も読んでいなかった。**

| レーン | 枯渇の連続日数 | 直した後 |
|---|---|---|
| **proverb（投資格言）** | **5日連続**（8/27〜8/31・PROVERB_LEDGER） | キュー 45→49・着手できる残り **0→4件** |
| **scam（投資詐欺）** | **7日連続**（SCAM_LEDGER の補充依頼） | キュー 10→13・着手できる残り **0→3件** |
| autodraft（心理＆リスク管理） | 未枯渇（残8） | キュー 55→63・残り **8→15件** |

🔑 **根本原因＝§⑤の番人が `drafts/AUTODRAFT_GUIDE.md` **1本しか見ていなかった**。**
§⑤は「静かな停止を捕まえる」ために作ったのに、**新しいレーンを増やしたときに登録する手順が無かった**
（proverb は 2026-08-06〜、scam は 2026-08-13〜）。

**入れたもの（同期済み）:**

1. `check_automation_health.py` に **`QUEUE_LANES`** を新設＝**3レーンを1つずつ報告**する。
   ⚠️ **レーンを増やしたら必ずこの表に足すこと**（コメントにも明記した）
2. ⚠️ **台帳（LEDGER）に載っている key は残量に数えない。**ルーティン自身が
   「LEDGER と guides.html のどちらにも未登録の最初の1件」を選ぶので、台帳に載った時点で選ばれない。
   数に入れると**動かせない在庫が床を支える**（`sell-in-may`＝人間がクローズ／scam の3件＝エスカレ中）。
   除外したものは報告に件数と key を出す（黙って減らさない）
3. `_test_topic_queue.py` に⑧⑨⑩⑪を追加（3レーン登録・バッククォート無しの表・台帳除外・台帳が
   読めなくても止まらない）。**全テスト合格**・`mw.py check` ✅OK（警告26件は既存のナビ不足で無関係）

### ⏭ この件で残っている判断（オーナー）

1. 🔴 **投資格言シリーズは、本当に汲み尽くしている。**8/19（#40〜45）に続く**2度目の枯渇**で、
   今回は洋の言い回し中心に**4本しか立たなかった**（44本で和の相場格言はほぼ網羅）。
   → **次に枯渇したら水増しせず、①44本の総集ハブを作って完結させる ②後継シリーズへ枠を渡す**
   のどちらかを決めること（2026-08-13 に格言枯渇から詐欺シリーズを起こした前例と同じ判断）。
   PROVERB_GUIDE.md の末尾にもこの申し送りを書いた
2. 🚩 **scam の未公開3件はキュー枯渇ではなく個別のエスカレ待ち**＝
   `investment-seminar`（e-Govで金商法の条文確認→即公開可）／`crypto-scam`（警察庁PDFで統計修正）／
   `recovery-scam`（🔴黒・本文削除済み・再執筆）。最短ルートは SCAM_LEDGER 末尾に書いてある。
   **3件解消すれば、それだけで残り3→6件になる**
3. proverb と scam は床5に対していま4件・3件＝**番人は🟡のまま**。これは真の黄色（4日後・3日後に止まる）


### 🆕 2026-08-31（続き）: 格言の後継シリーズ「東証のしくみ」を新設し、ルーティン化した

**格言は本当に汲み尽くしている**（44本・2度の枯渇）ので、水増しではなく**後継へ枠を渡す**判断をした
（2026-08-13 に格言枯渇から詐欺シリーズを起こしたのと同じ形）。

**後継を選んだ根拠（実測）**＝既存362本を**本文までgrep**して空きを測った:

| 題材 | 本文で主役になっている記事 |
|---|---|
| TOB・呼値・立会外分売・議決権行使 | **0本**（言及すらゼロ） |
| ストップ高安・適時開示・市場区分・貸株・逆日歩・単元未満株 | **0本**（脇役の一言のみ・6〜7本に散在） |

一次情報も到達を確認（`jpx.co.jp` 200 / `fsa.go.jp` 200・手元実測）。
⚠️ **クラウド側の到達は別**なので、初回実行で担当が測り直して台帳に書く手順をプロンプトに入れてある。

**入れたもの:**

- `drafts/TSE_GUIDE.md`（新規・手順書＋**題材キュー18件**）。PROVERB_GUIDE を土台に、
  🚨 **このシリーズだけの絶対条件**を足した＝**制度の数値は一次情報を開いて確かめる・本文に確認日を書く・
  届かなければ書かずにエスカレ**（値幅制限の幅や呼値の刻みを思い出しで書くのが最大の事故）
- ルーティン **`tse-daily-auto`** `trig_01HbSi32JHhWEhoUK2erQhJr`（毎日 **11:37 JST**・model=claude-sonnet-5）。
  初回発火＝**2026-09-01 11:37 JST**
- `check_automation_health.py` の `QUEUE_LANES` に4レーン目として登録／`sync_to_github.py` に手順書を追加／
  `_test_topic_queue.py` に検査を追加（**全テスト合格**）
- `CLAUDE.md` に**シリーズ自動公開レーンの一覧節**を新設＝proverb / tse / scam の trigger ID と手順書・台帳の対応を1か所に。
  🚨 **「レーンを増やしたら QUEUE_LANES に登録する」を明記**（今回の見逃しの再発防止）

⚠️ **訂正**＝同日に心理シリーズへ足した `fee-impact` は**既に公開済みの記事だった**（追加理由に書いた
「誘導先が存在しない」も誤り）。`odd-lot-investing`（単元未満株）に差し替え済み。他7件に重複なし。

### ⏭ この件で残っている判断（オーナー）

1. ✅ **格言レーンの畳み方＝キューに仕込み済み**。#46〜#49 のあとに **#50 `series-index`（最終回・総集編）**を
   置いた＝48本をテーマ別に並べる総目次で、**後継の `guide-tse-*` へ送客**して終わる。⚠️ 残るオーナー作業は
   **#50 が公開されたら `proverb-daily-auto` を止めること**（約5日後）。止めないと以後は毎日「🚩キュー枯渇」を書き続ける
2. しばらくは **格言と東証が両方走る**（1日2本）。4日ほどで格言が尽きるので、その時点で①を実行すれば揃う
3. 番人の現状＝autodraft 🟢16件／proverb 🟡4件／scam 🟡3件／**tse 🟢18件**

### 🆕 2026-09-01 夜: 許可リスト追加が効いた／「数字で見る企業」初回公開／日本株に構造的な壁

**オーナーが許可リストに追加**（`jpx.co.jp` / EDINET / TDnet / SEC、のちに `en.wikipedia.org` / `ja.wikipedia.org`）。
**クラウド実測で全部200**＝朝 `EGRESS_BLOCKED` だった `jpx.co.jp` も開通。

⚠️ **私（Claude）の操作ミス**＝到達性だけ測らせるつもりで `RemoteTrigger` の `run` に測定用プロンプトを渡したが、
**`run` は body の events を無視し、保存済みプロンプト（記事を書いて公開するフロー）で走った**。
止めるAPIが無いため、土曜の分が火曜に動いた。以後、**`run` でプロンプトは上書きできないと考えること。**

#### 初回実行の結果（`cse_01DPSoVNWU53P25LW7TKmCJP`）

- ✅ **`guide-company-aapl-apple.html` を公開**（Apple・海外株）。数字は Form 10-Q/10-K＋確認日、
  リスクは "Item 1A. Risk Factors"、材料は**上がる5件／下がる5件で対**。免責3層あり。禁止語なし
  （「目標株価」の2件は**免責文の中**＝「目標株価の提示は行っていません」で違反ではない）
- 🚩 **日本株（アドバンテスト6857）は正しくエスカレ**——決算短信は取れたが**有価証券報告書に到達できず**、
  §2-2「両方に届かなければ書かない」に従って見送り、米国株に切り替えた

#### 🚨 日本株が構造的に書けない（要オーナー判断）

| | クラウド実測 |
|---|---|
| TDnet 日別一覧 → 決算短信PDF | ✅ 200（⚠️ **保管は約5〜6週間だけ**。それ以前は404） |
| EDINET トップ | ✅ 200 |
| EDINET 書類検索API v1 | ❌ **403（先方のWAF）**＝許可リストでは直らない |
| EDINET 書類検索API v2 | ❌ **401＝Subscription-Key が必要** |
| 各社IRサイト | ❌ 経路遮断 |

🔑 **§1④の芯は有報の「事業等のリスク」**で、決算短信にはこの節が無い。だから**このままだと日本株は毎週エスカレする。**

⏭ **オーナー判断（どれか1つ）:**
1. **EDINET API v2 の Subscription-Key を取得**（金融庁サイトで無料登録）→ GitHub Secrets へ。**本筋**
2. `disclosure2.edinet-fsa.go.jp` の**Web画面から有報PDFに辿れるか**を次回実行で検証させる（未検証）
3. 各社IRを許可リストに足す → ⚠️ **毎週別の会社なのでいたちごっこ。薦めない**

#### 記事に見つかった弱点2つ（手順書とプロンプトに反映済み）

- ❌ **歴史年表の2行が「各種メディア報道」というURLの無い出典**だった。
  Wikipedia が遮断されていて **WebSearch にフォールバック**したため。
  → **年表は全行にクリックできるURLを必須**にし、`en/ja.wikipedia.org` を歴史専用で許可（オーナーが追加済み）。
  ⚠️ **Wikipedia は年表だけ。数字とリスクには使わない。**
- ❌ **時価総額を `Investing.com`（二次情報）から引いて本文に金額を書いた**。
  → **株価・時価総額は一次情報に存在しない**ので、**本文に金額を書かない**（選定根拠は「候補中で最大」で足りる）。

#### 📌 ついでに分かったこと

- クラウドのクローンは **shallow(50)**。`git fetch --depth=500 origin main` で伸ばす
  （**`--unshallow` は2分でタイムアウト**）。⚠️ 1日80〜90コミットあるので500でも約8日分
- **`jp-rankings.json` が 2026-08-29 を最後に更新されていない**（8/31月・9/1火が欠落）。
  この レーンの日本株候補の唯一の情報源なので、止まると候補が枯れる。**別途要調査**

### 🚨 2026-09-01: 「東証のしくみ」が初回から止まった＋番人がそれを緑と表示していた

**症状**＝新設した `tse-daily-auto` の初回（9/1 11:37 JST）が**記事0本**でエスカレ。他のレーンは全部動いている
（同日：投資詐欺#11・投資格言#46・基礎知識#48・ニュース速報／ai-tsukaikata も深掘りとレシピを公開）。

**原因＝クラウド実行環境から `www.jpx.co.jp` に到達できない。**担当は正しく測って正しく止まっていた：

- curl `(56) CONNECT tunnel failed, response 403` ／ WebFetch `EGRESS_BLOCKED`
  ／ プロキシも `connect_rejected`（"gateway answered 403 to CONNECT"）
- **遮断の種類＝経路（許可リスト）。**先方のbot判定ではない → **ドメインを足せば直る**
- `www.fsa.go.jp` は 200。**手元PCからは `jpx.co.jp` も 200**（2026-09-01 再実測）＝サイト側は正常

#### 🔑 本当の問題は、これを誰も検知できなかったこと（番人の穴・2件）

**① 題材を1日1件ずつ食い潰す設計だった。**`TSE_GUIDE` 手順2は「台帳と guides.html のどちらにも
**未登録**の最初の1件」を選ぶ。エスカレすると台帳に名前が載るので、**翌日は次の題材へ進む**。
→ **18日で全題材が消え、記事は1本も出ず、最後に出る警告は「キュー枯渇・補充依頼」という誤診**になっていた。

⚠️ **これは ai-tsukaikata で既に学んだ教訓の再発**＝「一時的な失敗を永続的な状態として記録すると、
原因が直っても復旧しない」。**再試行の条件を決めずに停止マークを作らないこと。**

**② `check_automation_health.py` の §⑤ が緑を出していた。**残量は「キュー総数 − 公開済み − 台帳記載」なので、
**エスカレのたびに held が増えて remaining が減る＝順調に消化しているようにしか見えない**。
実際この日の tse は **🟢17/18** と表示される状態だった（記事は0本）。

#### 入れた対策

- **`TSE_GUIDE` 手順2＝台帳に名前があるだけで消費済みにしない。**記載を読み、**停止の理由が
  経路遮断（`CONNECT tunnel failed`／`EGRESS_BLOCKED`）や一時エラーなら同じ題材を選び直す**。
  🔑 **印ではなく理由で判断する**（既存の `timely-disclosure` は `🚩` で記録済みなので、
  印で判定すると取り残される）。選び直さないのは**コンプラ🔴黒・品質❌・先方のbot判定**のときだけ
- **`TSE_GUIDE` 手順9＝エスカレを2種類に書き分ける**（`🔁再試行可` ／ `🚩要人間レビュー`）。
  ⚠️ **迷ったら `🔁再試行可`**（題材を食い潰すより、もう一度試すほうが安い）
- **`check_automation_health.py` に `escalation_state()` を新設**＝
  🔑 **「在庫があるか」ではなく「実際に出ているか」を見る。**
  **公開0件なのにエスカレがある → 🔴**／**公開はあるがエスカレ2件以上 → 🟡**／エスカレ1件は鳴らさない
  （人間待ち・再試行待ちは正常）。`_test_topic_queue.py` に⑬として6ケース追加＝**全テスト合格**

#### ⏭ オーナー操作が要る（これだけは私にはできない）

🚨 **クラウド環境のネットワーク許可リストに、次を追加してください。**
claude.ai/code のメッセージ入力欄すぐ上の**雲アイコン → 環境の歯車 → Network access = Custom**。
⚠️ **「Also include default list of common package managers」に必ずチェック**（外すと他の全ルーティンが壊れる）。

| ドメイン | どのレーンが要るか | いま |
|---|---|---|
| `www.jpx.co.jp` | 東証のしくみ（**いま止まっている**）／数字で見る企業 | ❌ 遮断 |
| `disclosure2.edinet-fsa.go.jp`（EDINET） | 数字で見る企業（日本株の一次情報） | 未測定 |
| `www.release.tdnet.info`（TDnet） | 数字で見る企業（日本株の一次情報） | 未測定 |
| `data.sec.gov` ／ `www.sec.gov` | 数字で見る企業（海外株の一次情報） | 未測定 |

🚨 **`company-weekly-auto`（初回 9/5 土 14:23）も同じ壁に当たる見込み**なので、まとめて足すのが早い。
⚠️ **「未測定」はクラウド側でしか測れない**（手元PCでは4件とも200を実測済み）。
足したあとの実行で担当が curl で測り直して台帳に書く手順は、両方の手順書に入っている。

📌 追加すると**環境キャッシュが再構築される**＝次の1回だけ起動が遅い。

### 🆕 2026-08-31（続き2）: 「数字で見る、話題の企業」を新設＝**構造的に枯渇しないレーン**

オーナー要望＝「**今話題の企業の深掘り記事**。歴史だとか今後の展開だとか、将来性はどうなのか」。
ただし追加指示で**将来性の扱いが明確に定義された**（ここがこのレーンの設計の芯）:

> 「将来性というのは、上がるか下がるかということではなく、**上がる可能性がある材料はどういうことなのか、
> 下がる可能性がある材料はどうなのか**、というのを表示しておけば、あとは投資判断は本人で」

🔑 **これは金商法対策の妥協ではなく、記事の型そのもの。**材料を**左右に対で並べて、そこで終える**
（数を揃える・各材料に出典と確認日と「効くとどうなるか」・**重み付けをしない・結論の文を書かない**）。

**なぜこのレーンが「枯渇の答え」なのか**＝ここまでの4レーンは全部**人が題材キューを書く**構造で、
だから枯渇した（格言2度・詐欺1度）。このレーンは題材を **`jp-rankings.json`（直近14日で gainers/losers/hot に
2回以上）から機械的に選ぶ**＝**キューが無い＝補充する人がいらない**。相場が動く限り題材は尽きない。

**入れたもの:**

- `drafts/COMPANY_GUIDE.md`（新規）。§0に**6つの禁止**（目標株価／売買推奨／割安割高の断定／将来予測／
  証券会社名／急騰急落の当日・翌日）、§1に7構成、§2に選定手順、§2-2に一次情報（EDINET・TDnet・JPX）
- 記事の**芯は④**＝**会社自身が有価証券報告書「事業等のリスク」に挙げているリスク**を要旨で紹介し原文へリンク。
  書き手の推測ではなく**会社の自己申告**なので、助言にならずに「下がる材料」を具体で書ける
- ルーティン **`company-weekly-auto`** `trig_017X6e1WvUm2FBvyegzFkAUh`（**毎週土曜 14:23 JST**・
  model=claude-sonnet-5）。初回発火＝**2026-09-05 14:23 JST**
- ⚠️ **ニュースレーンとの衝突を潰した**（オーナーが懸念した点）。デイリーニュースも個別銘柄（決算・M&A・
  急騰急落）を扱うので、**両方の手順書に相互の重複チェックを入れた**＝直近14日に同じ会社が出ていたら
  その週は見送り、14日より前なら書いてよいが速報へリンクする。役割分担＝**ニュース＝その日に何が起きたか／
  本シリーズ＝この会社は何で稼ぎ、材料は何か**
- `check_automation_health.py` に **`LEDGER_WATCH` + `check_ledger_freshness()`** を新設。
  🔑 **キューが無いレーンは残量で見張れない**ので、**台帳の最終更新が10日を超えたら🟡**にする。
  `since`（2026-09-07）より前は鳴らさない＝初回が走る前から赤いと番人の信用が落ちるため
- 🚨 **既存の個別銘柄記事（`guide-nvidia-2026-05.html` 等）を文体の手本にしない**旨をプロンプトと
  NEWS_DAILY_GUIDE に明記。2026年5〜6月に書かれたもので「むしろ割安水準」「NVDAは外せない」
  「強気継続でほぼ確定」を含み、**いまのゲートなら通らない**

**圧迫の確認**（オーナーが気にした点）＝autodraft 16日・tse 18日ぶんの在庫があり、本レーンは**週1本**。
1日あたり +0.14本なので、他レーンを圧迫しない。

**レーンの現状**＝autodraft 🟢16／proverb 🟢5（#50で完結）／scam 🟡3／tse 🟢18／company ⚪見張り待ち。

**🔄 方針を反転：重複は「避ける」から「続報として繋ぐ」へ／海外株も対象に（オーナー指示・同日）**

いったん既刊とのクールダウン（365日／180日）を入れたが、**オーナー判断で撤回した**。

> 「銘柄はかぶってもいいと思います。前回と今回じゃ状況が違いますし、**むしろあの後どうなったのかというのも
> 気になります**。銘柄がダブる場合は**前回どうだったのかという前置きをしてから、今回はこういう状況だ**という
> 記事を書いてもらえれば、それはそれで面白いと思います」
> 「**海外の企業のことも、やっぱり日本株に大きく影響を与える**ので、注目されている銘柄はぜひ記事にしてほしい」

🔑 **重複はコストではなく素材だった。**同じ会社を追い続けると、単発の解説より価値が上がる（定点観測になる）。

**入れたもの:**

- §1 に **⓪「前回の記事のその後」**（既刊がある会社のみ・必須）＝既刊の公開日とリンク（時点を明示）／
  前回の数字3つ → いまの数字（一次情報＋確認日）／その間に起きたこと3〜5件（各行に出典と日付）
- 🚨 **⓪のコンプラ設計がこの変更の肝**：既刊から引いてよいのは**事実・数字・公開日だけ**。
  評価語（「本命銘柄」「割安水準」）を引用すると、**いまのゲートで禁じた表現をこの記事が再生産する**ことになる。
  さらに**「前回の見立てが当たった／外れた」を書かない**——答え合わせは的中率の主張＝助言的になるため。
  書けるのは「前回はこの数字 → いまはこの数字」という事実の対比だけ
- **既存記事は絶対に上書きしない**（同名があれば末尾に `-<YYYY-MM>`）
- 除外は**本レーン内の90日ローテーションだけ**に縮小（大きな動きがあれば例外可）

**海外株の候補源（実測して決めた）:**

⚠️ **個別海外企業のランキングはリポジトリに存在しない**（`hot-assets` は米国セクターETF11本、
シグナルの universe は18銘柄で個別株ゼロ）。そこで既存の git 追跡ファイル2つで代用した：

1. `earnings-calendar.json` の `us`（個別企業＋決算日）で**発表から2営業日〜14日**の会社
   — 🔑 決算直後は数字が揃っていて「数字で見る」シリーズと最も相性がよい
2. ①が空なら**直近15〜60日**の `guide-news-*.html`／`NEWS_LEDGER.md` で主役だった海外企業（速報へリンク）

**日米は交互に回す**＝台帳の最新1件と違うほうを第1候補にする（初回は日本株）。

**海外の一次情報（手元実測 2026-08-31・すべて200）:**

| 用途 | URL |
|---|---|
| 提出書類の一覧 | `data.sec.gov/submissions/CIK<10桁ゼロ埋め>.json` |
| 書類の本文 | `www.sec.gov/cgi-bin/browse-edgar` |
| 決算リリース | 各社IR（例 `investor.nvidia.com`） |

🔑 **§1④「会社自身が挙げるリスク」の米国版＝10-K の "Item 1A. Risk Factors"**（有報「事業等のリスク」と同じ役割）。
⚠️ SEC は User-Agent に連絡先を名乗ることを求めるので、**サイト名とドメインを名乗る**
（🚨 **個人のメールアドレスは書かない**）。🔑 これは**UA偽装ではない**——bot判定の迂回は禁止のままで、
求められた通り身元を名乗るのは別物。名乗っても弾かれるならエスカレして止まる。

---

**🔥 追加で塞いだ穴：既刊との重複（オーナー指摘）**

オーナーから「NVIDIAとAMDは過去に記事で書いてあるはずなので、内容が被らないように」と指摘を受け、
**実際に穴があった**。重複チェックは①COMPANY_LEDGER（**新規＝空**）②ニュースレーン直近14日、の2つしか無く、
**レーン発足前の既刊を見ていなかった**。

実測＝個別1社を主役にした記事は **8本（7社）**。うち**日本株4社は選定元にそのまま出てくる**：

| 会社 | コード | 既刊 | 衝突の危険度 |
|---|---|---|---|
| キオクシアHD | 285A | **2本**（直近が2026年8月） | 🔴 **2026-08-31 時点で gainers 1位**＝初回で踏む寸前だった |
| トヨタ自動車 | 7203 | 1本（2026年5月） | 🟡 |
| ソフトバンクグループ | 9984 | 1本（2026年5月） | 🟡 |
| オリエンタルランド | 4661 | 1本（2026年6月） | 🟡 |
| NVIDIA / AMD / TSMC | 米国株 | 各1本 | ⬜ **衝突しない**＝`jp-rankings.json` は**日本株のみ**で、米国株のランキングファイルは存在しない |

塞いだもの：

- COMPANY_GUIDE §2-1 に **手順3「既刊との重複確認」**を新設（上の表＋除外期日）。
  **既刊2本以上は365日・1本は最新の既刊から180日**あける
- 🔑 **表だけに頼らない**＝表は必ず古くなるので、**毎回 `ls guide-*.html` と guides.html を社名・コードで grep** させる
- 期日を過ぎて書くときも、構成①②（会社概要・歴史）は**要約に留めて既刊へリンク**し、
  主役を④（会社自身のリスク）⑤（上がる材料／下がる材料）に寄せる。リンクは**時点を明示**する
- プロンプトの「要点再掲」にも同じものを手順2として入れた（キオクシアの危険を名指しで警告）

⚠️ **既刊のタイトル自体が、いまのゲートの反例になっている**：「AI半導体ブームの**本命銘柄**」
「孫正義の**最大の賭け**」「**復活**3シナリオ」。手本にしない旨をプロンプトに明記済み。

### ⚠️ この作業で私（Claude）が起こした事故 3件（同じ轍を踏まないため）

1. **プロンプト更新の1回目で2つ壊した**＝①「賭」を誤字にした ②**「残った候補から時価総額の
   大きい順に1社」という選定の詰めを落とした**（除外条件だけ書いて、選び方を書かなかった）。
   修正リクエストが1度 **401（OAuthトークン期限切れ）**で弾かれたが、再送して**両方修正済み**
   （`updated_at` 2026-08-31T12:48Z）
2. 🚨 **このファイルを一度0バイトにした。**原因＝Python で `open(p,"w",encoding="utf-8").write(s)` は
   **開いた時点で中身を消す**ので、`.write()` がエンコード例外で落ちると**空のファイルが残る**。
   GitHub 側（`b73d859`）から curl で復元した。
   🔑 **対策＝先にエンコードしてから開く**（`data = s.encode("utf-8")` を実行してから `open(p,"wb")`）。
   これなら例外が出てもファイルに触れていないので無傷
3. **引き金は絵文字の書き方**＝Python のソース文字列に絵文字を**サロゲート対のエスケープ**
   （U+D83D のような単独では不正なコードポイントを2つ並べる形）で書いたため、UTF-8 に変換できなかった。
   ⚠️ **絵文字はそのままの文字で書くか、コードポイント1つ（U+1F534 形式）で書くこと。**
   さらに Bash のヒアドキュメントは**バックスラッシュのエスケープを1段食う**ので、
   二重に書いたつもりが単独のサロゲートになる

### ⏭ この件で残っている判断（オーナー）

1. **7本の既存個別銘柄記事**（NVIDIA・AMD・トヨタ等）に、いまのゲートなら通らない表現が残っている。
   直すか・古い記事として残すかは未決。**新レーンには影響しない**（手本にしない旨を明記済み）

## 🆕 2026-08-29 夜: 決算の仕分け＝「中身」より「ギャップ」。オーナー仮説は実データで逆だった

記録＝**`research/_jp_earnings_selection_findings.md`（新設）**。⚠️探索（事前登録なし）。
オーナー仮説「好決算が出たのに寄りでまだ上げていない銘柄を狙う」を、5年分の実データで検証した。

- 母集団＝`_pead_events.csv` の**引け後開示のみ**×`_jq_daily_lake`×`_jq_fins_cache`＝
  **36,032件 / 558営業日 / 3,225銘柄**。成績＝`fwd10`（**翌営業日の寄りで買って10営業日**）。
  好決算＝**営業利益の前年同期比 +20%↑**（開示と同時に判明＝先読み無し。`surprise_ret`は結果なので不使用）
- **① 発表前から動いている**: 発表前20営業日のドリフトは 好決算 **+3.16%** / 中間 +1.80% / 悪決算 +0.86%
  ＝「決算前ニュースで中身を当てる」の価値は**既に価格に入っている分だけ削られる**
- **② 仕分けの結果（平均fwd10）はギャップ方向に単調増加**:
  好決算×`gap≤−2%` **−0.02%**（勝率45.6%）→ `0〜+2%` +1.02% → `+5%超` **+1.64%**。
  検定＝「まだ上げていない(gap<+2%)」−「もう上げた(gap≥+5%)」= **−1.09%・95%CI[−1.57〜−0.60]・p<0.001**。
  マイナス寄りに絞ると **−1.37%・p<0.001**＝**狙いたい側が最も悪い列**
- **③ 決算の中身に価値が無い**: 行計は 好決算+0.88% < **悪決算+0.97%**。同じギャップ帯で
  「好決算−悪決算」を検定すると有意なのは2帯だけで**どちらも好決算のほうが悪い**（①−0.78% p<0.001／④−0.97% p=0.023）
- **④ 機序**: 好決算×`gap≤−2%` は**発表前ドリフトが+4.34%と全セル最大**＝
  「まだ織り込まれていない」のではなく**発表前に上げきっていて材料出尽くし**。だから寄りで買うと負ける
- ⚠️ **同日対照を引いていない**＝絶対値には市場の上昇が入る。**行間・列間の相対比較が結論の本体**
- ⚠️ 好決算＝**OP前年同期比**であって**コンセンサス比サプライズではない**（有料）。この限界は残る
- 🔑 **窓が違えば符号が違う**: 同じ「gap大」でも、**寄りで買って10日持つ**なら +1.64%、
  **寄りで売って15分**なら下方向（Q46）。混同しないこと

---

## ✅ 2026-08-29 夜: 事前登録簿の改竄検出（Q45）→ **無効化して Q48 として再登録・解決済み**

`mw evolve` が `❌ 事前登録違反: Q45 の本文が登録後に変更されている` を出す。**調査結果＝私の Q46 追記が原因ではない**:

- Q46 挿入前の状態を復元して再計算しても同じ hash（`0fefbd1a…`）＝登録簿の `c58f8e8a…` と不一致
- **登録済みQのうち一致8 / 不一致1（Q45のみ）**＝ハッシュ関数や行末の問題ではなく、Q45 の本文が実際に変わっている
- ⚠️ **原状回復できない**: `research/hypothesis_queue.md` は **SYNC対象外＝リモートに無い（404）**、
  ローカルに `.bak` も無い、gitでもない。**何がいつ変わったかを再現する手段がリポジトリ内に存在しない**
- 💡 **唯一の可能性＝OneDrive の版履歴**。作業フォルダはOneDrive配下なので、Web UI から
  8/11（登録日）〜8/28 の版を開けば差分が見えるはず。**これはオーナーの手が要る**
- 📌 **登録簿を書き換えて黙らせるのは禁じ手**（改竄検出の意味が消える）。ルールどおりなら
  「Q45 を改竄検出により無効化し、同じ設計文を新Q番号で再登録」が正しい直し方＝**オーナー判断**
- ℹ️ 実害の範囲＝`mw evolve` が exit 非0 になるだけ。Actions/番人には連鎖しない（`mw evolve` は手動実行）

### 対処（2026-08-29・オーナー判断「無効にして再登録」）

- **Q45 を無効化**し、`research/hypothesis_queue_archive.md` の【2026-08-29 無効化】節へ退避（＝無効化時点の本文をそのまま保存。改竄前は復元不能）
- **同一本文を Q48 として再登録**（`### Q48`・`📝 事前登録: Q48 を登録簿に記録（2026-08-29）`）。新設した `- 再登録の経緯:` 欄に経緯・限界・根拠を全部書いた（この欄も凍結対象）
- 🔒 **登録簿の Q45 の sha256 は書き換えていない**（`c58f8e8a…` のまま）。番人が文書化した手順どおり `"archived": "2026-08-29"` と `"invalidated"` の理由文を**追記しただけ**
- ✅ `python _doctrine_check.py` = `✅ DOCTRINE check OK（anchor 44 件突合 / 登録簿照合済 / warning 1）`（残った warning 1 は無関係＝`_jp_turtle_tracker.json` が DOCTRINE.md より新しい）

**再登録が正当だと判断した根拠＝Q45 は一度も検定していない**（3点とも実物で確認）:

1. 状態は登録以来 **⏳未着手**
2. **本検定スクリプトが存在しない**（`research/` にあるのは採取 `_lvh_event_harvest.py`・台帳 `_lvh_build_ledger.py`・解決 `_lvh_codemap.py`・検出力 `_lvh_power.py` のみ）
3. 検出力 `_lvh_power.py` の print 文を全数確認＝**N・日数・銘柄数・σ・MDE と対照側の p0/ρ しか出さない**。コード内に「平均値は意図的に出さない」「イベント標本の結果は見ていない」と明記

⚠️ **開示**: (a) **イベント標本の σ（23.79% 等）は観測済み**＝ばらつきは見た。方向も効果量も見ていないが**完全なブラインドではない** (b) 原本が復元不能なので **「Q48 が Q45 と同一設計である」ことは証明できない**＝2026-08-29 時点の本文をもって**新規に凍結し直したもの**として扱う (c) OneDrive の版履歴を見れば差分が分かる可能性は**まだ残っている**（無効化は取り消せないが、何が変わったかの記録は取れる）


---

## 🆕 2026-08-29 夜: Q46 登録（寄り天売り）＝A2 の実行可能性チェックを通した

オーナー指示「A2の実行可能性チェックを進めて」。**関門は通った**が、通したことで効果量が弱まった。

### 実行可能性（実測）

| 関門 | 結果 |
|---|---|
| 制度信用で売れるか（貸借銘柄） | ✅ **行ベース88% / 銘柄ベース93%**（J-Quants `equities/master` の `MrgnNm`。`_jp_margin_class.json` に保存） |
| 逆日歩・規制の要注意（日々公表銘柄） | ✅ **1/100件**＝ほぼ無い |
| 流動性 | ✅ 代金中央値134億円・1日3.1件 |
| 空売り価格規制（アップティック） | ⚠️ **未反映**。51単元超は直近価格以下で売れない＝「下がる寄り天を売る」と最も相性が悪い |
| 貸株在庫（一般信用） | ⚠️ **プログラムから確認不能**（証券会社ごと） |

🔑 **売れない銘柄を外すと効果が弱まる**: 非対称の差 −19.0%（p=0.021）→ **貸借限定で −16.5%（CI[−31.7〜+0.8]・p=0.061）**。
外した14行は急落率50.0%・平均−2.23% ＝ **エッジの一部は「売れない銘柄」に集中していた**（Q46に限界として明記）。

### 登録したもの

- **`research/hypothesis_queue.md` に Q46 を追記→登録簿に記録済み**（`mw evolve` が sha256 を固定）
- 主要評価指標＝**非対称の差**（平均r15mは**検出力が出ない**＝N=300でもMDE 0.54% > 効果量0.45%。
  非対称の差はN=300でMDE 10.9pp < 16.5%＝判定できる。**この事実を登録時に自己申告**）
- 判定＝**前向きN=300 到達時に1回だけ**（≒96営業日＝2027-01中旬）。昇格しても候補の旗立てのみ
- トラッカー **`_jp_gapup_short_tracker.py` 新設**＋`_research_trackers_daily.py` に登録。
  実行確認済み（前向きN=0＝登録日より後の行がまだ無い＝正しい挙動）

---

## 🆕 2026-08-29 夜: 寄り付き研究＝先着ラベルを実装／逆の視点で「急騰の法則」は無いと確認

研究記録＝**`research/_jp_open_intraday_findings.md`（新設・ローカル専用）**。⚠️全て探索（事前登録なし）。
先行研究 `_jp_open_gap_findings.md` の4本は**すべて「ギャップ＝前日引け→寄り」**が対象で、
**寄り付いた後の窓（5〜15分）は未着手**だった。そこを初めて検証した。

### ① `jp_open_logger.py` に先着ラベルを実装（オーナー承認）

`mfe15`/`mae15` は5分足3本の max/min ＝**順序が消えていた**ので「+2%と−2%のどちらが先か」が
分からず、方向＝期待値の検定ができなかった。ここを塞いだ。

- 新列 **`ft2`**（up/down/**same**/none。`same`＝同一バー内で上下とも触れた＝**順序不明を不明と言う**）
  ＋ **`b1h,b1l,b2h,b2l,b3h,b3l`**（バーごとの高安を寄り値からの%で生保存＝**後から任意の閾値で復元可**）
- `ensure_header()` を新設＝列を増やしたとき既存CSVを新ヘッダへ一度だけ書き換える（冪等・`.bak` を残す）。
  **実行済み: 1,872行そのまま・既存値の完全一致を照合・`jp-open-log.csv.bak` 作成済み**
- テスト **`_test_jp_open_logger.py` 新設＝18/18 PASS**
- **実データで通し検証**（LOGを一時ファイルへ差し替え・`--date 2026-08-28`）＝40行・21列・
  ft2分布 none29/up9/down2。例: 2160 は b1h=2.388 で1本目に+2%到達→`up`（照合済み）
- ⚠️ `jp_open_logger.py` も `jp-open-log.csv` も **SYNC禁止（🔴黒・ローカル専用）**＝
  **この改修はリポジトリにバックアップが無い**。本番反映は月曜 09:30 のWindowsタスクから

### ② 検証した仮説と結果（オーナー仮説2つ）

| 仮説 | 結果 |
|---|---|
| **注目度＝前日ランキング掲載** | ✅ **当たり**。掲載1,719件の急騰率9.1% vs 非掲載(◎○のみ)153件0.7%＝**13倍**（+8.4pp・CI[+5.8〜+11.2]・p<0.001・FDR✅）。値上がり側も値下がり側も動く＝効いているのは方向でなく注目度。⚠️**急落率も同じだけ上がり**（11.7% vs 0.0%）、方向指標 `pos` の差は −0.048・p=0.317＝**方向のエッジ無し** |
| **材料＝ニュース** | ⏳ **未決**。前営業日に決算あり(N=35)の急騰率20.0%（+10.4pp・CI[−0.7〜+30.3]・p=0.069・FDR不通過）。急落率17.1%＝対称。大量保有(N=39)は+1.0pp・p=0.938＝効果なし。**決算を判定できる行が557/1,872**（カレンダー蓄積が7/29開始）＝Nが足りないだけ |

🔎 **手がかり1つ**: 材料あり銘柄は **r5m −0.59% → 5→15分 +0.47%**（材料なしは −0.14%→+0.12%）＝
「寄り直後に投げて戻す」形。**N=45で未検定**。決算の蓄積が進めば検定できる（判定目安 2026-09-01）。

### ③ 逆の視点（急騰群だけを集める）＝「急騰の法則」は見つからなかった

急騰157件の共通点を数えると「代金≥80億が58.6%」等が出るが、**急落群では67.2%**＝共通点ではない。
2特徴の組合せも上位5件すべて検定で不成立（最良でも p=0.445）。

**唯一の強い非対称は逆向き**＝`gap≥+4%`（ギャップアップで寄った）**N=114 で急騰13.2% / 急落33.3%**・
平均r15m −0.73%・非対称 −19.0%（CI[−33.3〜−2.8]・p=0.021・**ただしBH-FDRは不通過**）。
先行研究①および**積み残しA2「裏返し＝寄り天売り」**と同方向。1日3.3件・代金中央値134億円。
🚧 **売り側なので実行可能性の確認が先**（信用売り可否・逆日歩・空売り規制・貸株在庫）＝A2が止まっている関門と同じ。

---

## 🆕 2026-08-29 夕: 日本株ランキングの鮮度ガード（番人①に追加＋新設§⑫）

オーナー確認「値上がりランキングや信用残はJ-Quantsから？」→ **違う**ことを実測で確定した。
`build_jp_rankings.py`＝**Yahoo価格のみ**／`build_jp_margin.py`＝**JPX公式の日次Excel**、どちらも**キー不要**。
J-Quantsを使うのは①騰落レシオ（8/22新設・**Actions上で動く唯一のJ-Quants利用**＝だからSecretが初めて必要になった）
②`_`始まりのローカル専用5本（スクリーナー／日足レイク／決算カレンダー／大量保有）だけ＝
どのワークフローからも呼ばれていないことを確認済み。

### 直した2点（オーナー承認）

1. **番人①に `jp-rankings.yml` を追加**（閾値 **30h**・warn）。hot-assets 最上段を作る唯一のジョブなのに
   監視対象外だった。閾値は実測で置いた＝直近30runの日跨ぎ間隔は 24〜25h、**8/27は1日まるごと
   スキップされて 33.5h**あいた＝その形を捕まえる幅
2. **番人 §⑫「日本株ランキングの鮮度」を新設**。①は「走ったか」しか見ない死角＝**データ側**で見る。
   `jp-rankings.json` の `asof` と**上流(Yahoo)自身の確定最終営業日**を突き合わせる
   （祝日カレンダーを持たずに済む／場中は当日の未確定バーを1本落とす＝番人は09:30 JST＝場中に走るので
   これが無いと毎朝誤検知する）
3. **`build_jp_rankings.py` に2つのガード**:
   - **日付混在ガード**＝銘柄ごとの最終バー日付の多数派を asof とし、少数派の銘柄は落とす
     （別々の営業日の騰落率を1つの表に混ぜない）
   - **巻き戻しガード**＝取れた営業日が前回より古ければ**書かずに終了**（前回分を温存＝次の回に委ねる）
- テスト **`_test_jp_rankings_freshness.py` 新設＝16/16 PASS**（A7は実際に起きた 8/29 04:34 JST の形を固定）
- **本番で 🟡→🟢 の両方を実測**（＝この検査が本当に区別できることの証拠）: 修正前 `🟡 asof=2026-08-27 / 上流=2026-08-28` → `jp-rankings.yml` を手動起動して **asof 2026-08-28・universe 399 に復旧** → 再走で `✅ 🟢 asof=2026-08-28（上流の確定最終営業日と一致）`。①にも `日本株ランキング: 成功 0.0h前` が出た
- ℹ️ `jp-margin.json` は `asof=2026-08-27` のままだが、JPX「日々公表銘柄」が**翌営業日公表**＝仕様どおりの遅れ。同じ物差しで鳴らすと誤検知になるので ⑫ の対象から外してある

### 🔍 実測した事象（原因は上流の時刻・うちのコードではない）

- 8/26夕→8/28未明で **jp-rankings の cron が1日スキップ**され、以後 実行時刻が **17時台→03:30〜05:10 JST** へ移動
- その早朝の回では **Yahoo がまだ 8/28(金) の日足を返しておらず** `asof=2026-08-27` で完走
  （同じ時刻に日本から叩くと 8/28 が返る＝**リージョン/エッジ差の可能性**）。
  ジョブは success・commitは「No changes」＝**成功したままライブが1営業日古い**という静かな嘘
- ⚠️ §⑫ の限界＝**ランナーから見える上流**と突き合わせる。ランナー側も 8/27 までしか見えない回は🟢になる
  （そのときは生成もできない＝行動可能な差だけを鳴らす、という意味では正しい挙動）

---

## 🆕 2026-08-29: 🔴未決2件の実測確認 ＋ 番人 §⑩ の「永久に無音」穴を塞いだ

オーナー指示「🔴未決の2件から続けて」。**2件とも実測の結論は「オーナーの手が要る状態のまま」**
（下の §③ の表は不変）。その確認の過程で、番人が7日間🟢で嘘をついていた穴が見つかったので塞いだ。

### ✅ 実測した2件の現状（推測ではなく API/ライブで確認）

| 🔴 | 実測結果（2026-08-29 15:50 JST） |
|---|---|
| **JQUANTS_API_KEY 未登録** | **まだ未登録**。`GET /actions/secrets` の一覧に無い（登録済み8件＝ALERT_RECIPIENT / EDINET_API_KEY / GEMINI_API_KEY / GMAIL_APP_PASSWORD / GMAIL_USER / MY_TRADES_CSV_URL / NEWSAPI_KEY / YOUTUBE_API_KEY）。リモート `touraku-history.json` は 404・`market-health-history.json` の `health.touraku_ratio` は `last_ok:null / points:0`・ライブ market-health.html は「N/A 取得不可」。**壊れてはいない**（8/22 の申告どおり）が、**7日間ずっと暗いまま** |
| **弁護士相談の予約** | ⛔ **同日夕に「見送り」で決着**（オーナー判断）。期日ゲートは取り消し済み＝ai-tsukaikata commit `a42642e`。**グレーは恒久的に白のみ設計**。詳細は上の 8/29 夕の節 |

### 🐛 見つけて直した穴 — 番人 §⑩ が「一度も取れていない系列」を永久に見逃す

**証拠**: 8/28 の automation-health は `✅ 🟢 監視 5 系列すべて閾値内（観測開始前 1 本）` と報告していた。
その「観測開始前 1 本」＝ touraku_ratio は**8/22 に追加してから一度も点が入っていない**。
`eval_series_health` は `last_ok=None` を「新設初日を赤くしない」ために無条件で見逃しており、
**その猶予に上限が無かった**＝設定漏れ（今回は APIキー未登録）は永久に🟢のまま隠れる。
＝ 8/5 の教訓「**壊れたのにアラートが鳴らない経路を疑え**」の同型。

- `build_health_history.py`: `SERIES_META` に **`since`（監視開始日）** と `pending_hint` を追加。
  **`PENDING_GRACE_DAYS = 3`**（自己修復は最初の成功runで1年ぶん埋める＋1日2回runなので3日で6回＝
  正常なら翌日に点が入る）。`eval_series_health` は**4要素タプル**を返すよう拡張
  （`stale, pending, watched, overdue`）。`pending` は従来どおり全件＝表示は壊さない
- `check_automation_health.py` §⑩: `overdue` があれば 🟡 warn（緑にしない）。
  系列に `pending_hint` があれば原因候補も本文に出す
- テスト `_test_health_history.py` に **L1〜L5 を追加＝60/60 PASS**
  （L1は**本番の実データ形をそのまま固定**・L4 は新設当日を鳴らさない性質・L5 は since 書き忘れ検知）
- **本番で実測済み**: sync 後に `automation-health.yml` を手動起動 → run `33239656779` で
  `🚨 🟡 一度も取れていない系列 1/5: 騰落レシオ(25日)（新設から7日・猶予3日…）` を Issue #4 に投稿
- ⚠️ **副作用（意図的）**: JQUANTS_API_KEY を登録するまで**毎朝この🟡が Issue #4 にコメントされる**。
  止め方は「登録する」か「系列ごと落とす」の2択（黙らせるだけの3択目は作らない）
- 🔑 `sync` は `check_automation_health.py` で🚫stale になった（同日午前に別経路で ⑪ が入ったため）。
  **反射で --force せず**、リモート raw を取り直して diff → 削除・変更行が**自分が書き換えた2行だけ**で
  あることを機械照合してから `--force`（定石どおり）

### 📌 同日に確認した別件（対応不要）

- **main の force-push（8/29 01:11 UTC・299コミット）は同日午前に復旧済み**
  （`ea2d79ca restore:` ＋ `fc290b10 feat(番人): ⑪`）。`39b939e6...main` は **behind 0**＝欠落なし。
  番人 §⑪ が30時間ウィンドウで再掲するだけ（時間経過で消える）
- **Issue #5（health-check 8/29 00:07 JST「index.html の日付が古い」）は自己解消**。
  ライブ index の最終更新は **2026年8月29日 13:21 JST**＝正常。閉じてよいかはオーナー判断
- **automation-health は 8/29 の定時（09:30 JST）に走っていない**（最後の schedule 実行は 8/28 11:35 UTC）
  ＝ Actions の cron スキップ。今回は手動起動で代替した


## 🆕 2026-08-22 午後: 積み残しの設計書差分＋ブランドカラー Phase 2 を実行

オーナー指示「保留中の作業をすべて進めて」に対し、対象を3点に絞って実施（他は
オーナー本人の判断／作業が要る項目のため対象外にした＝下記「オーナーの手が要るもの」節は不変）。

### ① market-health-history 設計書の差分＝騰落レシオを5系列目として追加

`docs/superpowers/specs/2026-08-19-market-health-history-design.md` §3 が「別物問題」を理由に
チャート対象外にしていた騰落レシオを、8/22午前の欠陥A対応（J-Quants方式）でブロッカーが解消したのを受けて
5系列目として実装した（設計書 §11 に詳細）。

- `build_health_history.py` に `fetch_touraku_ratio_series()` を追加。**ネットワークは叩かない**＝
  `build_touraku_history.py` が集めた `touraku-history.json` をローカルで読み、日付ごとに25日窓の
  `ratio25` を計算するだけ（式は build_touraku_history から import＝複製しない）
- `ZONES["touraku_ratio"]` を新設し、`generate_market_news.py` の `analyze_touraku()` が持っていた
  ハードコード閾値（底値圏<60／売られすぎ60-80／通常80-120／買われすぎ120-140／過熱圏140-）を
  数値そのままで移設。`analyze_touraku()` は `classify_zone("touraku_ratio", ...)` を呼ぶだけに置換
  （リファクタ前後で戻り値が完全一致することをローカル実測で確認）
- 🐛 **実装中に見つけたバグ**: `update-market-news.yml` は元 `build_health_history → build_touraku_history`
  の順だった。`fetch_touraku_ratio_series()` はローカルJSONを読むだけなので、この順だと当日ぶんの
  更新前を読んで**touraku_ratio系列だけ毎日1日遅れる**。`build_touraku_history → build_health_history`
  に入れ替えて修正
- ⚠️ **stale_days=10 は暫定値**。touraku-history.json は2026-04-16開始でまだ年末年始を跨いだ実測が無い
  （実測最大間隔は6日＝GW想定）。他系列と同じ「実測+1日」まで詰めるのは2027年初に見直す
- テスト `_test_health_history.py` を D9〜D12・K1〜K5 で拡張＝**全55/55 PASS**（新設5件含む）。
  ローカルで `build_health_history.py` を実行し、touraku_ratio 62点・最新116.8%（2026-08-21時点）・
  `classify_zone` が「通常」🟢に分類することを実測確認
- push 済み（コミット `1de67ad`）・`update-market-news.yml` を手動 trigger 済み。
  ⚠️ **リモートの `touraku-history.json` はまだ存在しない**＝`JQUANTS_API_KEY` シークレット未登録のため
  （§Aで既知）。**このチャートが実際にデータを持つのは、オーナーがシークレットを登録した後の初回runから**
  （自己修復ロジックで自動的に約4ヶ月ぶん埋まる。手動作業は不要）
- 🚨 **verify中に見つけた別の実害バグ（今回の変更が原因ではない・当日朝の欠陥A対応コミットが原因）**:
  `update-market-news.yml` の「Commit and push」ステップが `git add ... touraku-history.json` を含んでいたが、
  `JQUANTS_API_KEY` 未登録の間はこのファイル自体が存在しない（`build_touraku_history.py` が例外を握り潰して
  保存せず終了するため）。**存在しないパスを `git add` に混ぜると他のファイルも道連れで exit 128 になり、
  コミット全体が失敗する**＝欠陥A対応コミットが乗った 8/22 朝以降、update-market-news.yml は**生成には
  成功するが毎回 push だけ失敗し、6コアページが一切更新されていなかった**（実測: このセッションで trigger
  した最初の2回が同じ理由で failure）。`if [ -f touraku-history.json ]; then git add touraku-history.json; fi`
  に修正（コミット `a87d87e`）→ 3回目の trigger で成功（run 32556895773・全ステップ success）。
  **ライブでの実測確認済み**: Contents API で `market-health-history.json` に `touraku_ratio` キーが
  追加されていること／`market-health.html` の4チャート（VIX・CNN・Crypto・バフェット）が正常表示・
  既存の騰落レシオ・ゲージカードが `N/A 取得不可` を正しく表示（`analyze_touraku(None)` のリグレッション無し）
  を確認

### ② ブランドカラー Phase 2 適用

`apply_brand_color.py --phase 2 --include-cloud --apply` を実行。dry-run 時点で対象370ファイル中
**変更が要ったのは18ファイル・54箇所だけ**（大半のSYNC登録済み記事＋生成スクリプト8本は既にPhase2相当の
スタイルだった＝「承認済み・実測306本895箇所」という旧見積りは実測と乖離していた。実際に差分があったのは
主に直近のクラウド自動生成記事）。sync dry-run では18件中17件が**内容一致でスキップ**（＝ライブは既に
その配色）、実送信は `guide-margin-trading.html` の1件のみ。62/62 の既存回帰テスト（`_test_apply_brand_color.py`）
は全PASS。同じコミット `1de67ad` で push 済み

### ③ 副次的に見つけた/直したもの

- `_pull_mirror.py --apply` でローカルミラーが11本遅行していたのを解消（クラウド公開済み・ローカル未取得）。
  取り込んだ4本（`guide-counterparty-risk.html` 等）が `mw check` で SYNC_FILES 未登録エラーになったため、
  `sync_to_github.py` の SYNC_FILES に追加登録（`_pull_mirror.py` 自身が印字する定型の次の手順どおり）
- ⚠️ **未対応（今回のスコープ外・既存の別問題）**: `mw check` の警告33件（guides.htmlカード未掲載4本／
  ナビCSS max-width欠落11本／back-to-topボタン欠落3本）はすべて今回の変更と無関係の既存問題。触っていない
- ⚠️ **無関係の既存テストフレーク1件**: `_test_publish_link_gate.py`「実例6件すべてを検出」が
  `guide-proverb-soba-no-kane-tako-no-ito.html` を検出できず1/7失敗。この記事は現在ローカルに実在するため
  （ミラー取り込み等で存在するようになった）、テストの固定フィクスチャ（過去の壊れリンク実例）が
  現状と合わなくなっただけ＝コードのバグではない。今回は直していない

---

## 🚩 次のセッションは、ここから（状態はすべて 2026-08-19 22:35 JST の実測）

### ① 最初にやること — ✅ 格言キューは補充済み（8/19 夜・オーナー判断）

**#40〜45 の6本を `drafts/PROVERB_GUIDE.md` に追加してリモート反映済み**（commit `89b8af8`・実測でリモート45行）。
明日 8/20 10:12 の routine は **#40「株価は美人投票」** を取るはず。**次の枯渇は 8/26 頃**。

補充した6本＝#40 美人投票（ケインズ・出典明示）／#41 価格は支払うもの、価値は手に入れるもの（グレアム帰属は諸説あり明示）／
#42 阿呆になりて買いのタネ蒔け（本間宗久・#22の対句）／#43 知ったらしまい／#44 強気も弱気も儲かるが豚は屠られる（作者不詳と明示）／#45 利は元にあり。
**既存39件＋リモート公開 guide 記事347本と突合して重複ゼロを実測**。各行に「既存記事と被らせない線引き」を芯として書き込んである
（例: #41 は `guide-per-pbr.html` が指標の計算なので、こちらは指標を使う前の考え方に限定）。

🔑 **キューの ✅公開済 マークは当てにならない**＝proverb/scam とも routine はマークを書かない。
**数えるときは公開ファイルの実在で数える**（`guide-proverb-<slug>.html` が live 200 か）。

📌 **詐欺レーンは残5本（#6〜10）＝8/24 頃に枯渇**。オーナー判断は「8/24 前に再判断」＝**次に来るのはこれ**。

### ② automation-health ⑧ — ✅ 修正して本番データで緑を実測

`book-watch-weekly` が永久に滞留と誤検知される件（成果物は `guide-new-books.html` で名前が一致しない）を修正。
**`normalize_target()` は触っていない**＝表記ゆれ（同じものの別表記）とは別物なので、**対応表 `TARGET_ARTIFACT`**（`check_automation_health.py`）を新設し、
**表示名はルーティン名のまま・実体照合だけ別名に飛ばす**形にした。リモート反映済み（commit `547d193`）。

- **入れる前に過去全件で誤検知を測った**（8/18 の教訓どおり）: リモートの実 `REVIEW.md`（見出し48件・🚩12件）×リポジトリ直下486ファイルで
  7/27〜8/19 の24日ぶんを回し、**変わったのは book-watch の判定だけ・他11件の🚩は全日で不変**。
- 本番データで `check_escalation_backlog` を実行＝**滞留0件・未対応🚩 0件**（修正前は「book-watch-weekly（4日放置）」）。
- 回帰テスト `_test_escalation_backlog.py` に **F1〜F6 を追加＝26/26 PASS**（別名が効くこと／実体が無ければちゃんと残ること／
  表示名を別名に置き換えないこと／対応表に無い対象は従来どおり／キーが増えたらテストも更新する、を固定）。

### ③ オーナーの手が要るもの

| 件 | 状態 |
|---|---|
| ⛔ **弁護士相談＝見送りで決着（2026-08-29 オーナー判断）** | 予約しない。設計の2分岐のうち「相談しない」側を確定＝**グレー全域は恒久的に白のみ設計**。ai-tsukaikata `tools/check_freshness.py` に `LAWYER_CONSULT_DECLINED` を新設して催促を停止（`LAWYER_CONSULT_DONE` は False のまま＝事実を偽らない）。資料 `_lawyer_consult_2026.md` は破棄せず論点として保管 |
| **Issue #4 を閉じてよいか** | 中身（#065/#067）は解決済み。8/19 夜時点で **⑧の指摘も解消**＝閉じられる状態 |
| **詐欺レーンの補充判断** | 残5本・8/24 頃に枯渇。今回は「触らない（8/24 前に再判断）」を選択済み |
| 未完タスク | ブランドカラー **Phase 2**（承認済み・実測306本895箇所・`--phase 2` 実装済み）／**AdSense 再申請** |

### 🚩 8/19 夜に見つけた別件 — `sync_to_github.py --dry-run` が**黙って本番 push していた**

`--dry-run` を解釈するのは `batch_upload()` だけで、**`--batch` を付けずに `--dry-run` だけ渡すと逐次パスに落ちて実際に push する**。
今回それで `check_automation_health.py` が「確認のつもり」で本番反映された（結果は意図どおりだったので実害なし・
巻き戻しも無し＝cloud が新しい2ファイルはちゃんと🚫で止まっていた）。
**修正済み**＝`--dry-run` 単体なら `--batch` を自動付与する（`sync_to_github.py` の `main()`）。
⚠️ **この修正はローカルにしか無い**（`sync_to_github.py` は SYNC禁忌＝リモートはクラウド用スタブ）。ローカル専用ファイルは
**リポジトリにバックアップが無い**ので、PC が飛ぶと消える。

### 🆕 市場健康度の「毎日の変化」チャート — 実装完了（オーナー依頼・8/19 夜）

`market-health.html` に4系列の日次推移チャートを追加した。**過去1年をバックフィル済み＝公開初日から線が出る**。

- 仕様書 `docs/superpowers/specs/2026-08-19-market-health-history-design.md`（**§10＝実測で変えた設計判断**）
- 実装計画 `docs/superpowers/plans/2026-08-19-market-health-history.md`
- 新規 `build_health_history.py`（SYNC入り）／`market-health-history.json`（**SYNC禁忌**）／テスト `_test_health_history.py`（**44/44 PASS**）
- 収集は `update-market-news.yml` に相乗り（生成の**前**・`continue-on-error`）。見張り番は automation-health **§⑩**

**チャートにした4系列**＝米VIX ／ CNN恐怖&強欲 ／ Crypto恐怖&強欲 ／ 米バフェット指数。
**入れなかったもの**＝日経VI（無料の直接ソースが存在しない。`^NKVI` 等すべて404）／騰落レシオ（下記の欠陥を先に直す）／
CAPE・日経予想PER（元データが月次・週次＝日次チャートに向かない）。

🔑 **実装中に4つ、推測が実データと食い違った**（全部 §10 に証拠つきで記録済み）:

1. **FRED は Actions からも届かない**（設計時は「Actions なら届く」と想定）。ワークフローは success のまま
   gdp=None で完走していた＝**握り潰しは静かに嘘をつく**。→ 世界銀行 API（キー不要・年次）に差し替え
2. **`^W5000` の単位を校正した**＝2026-04-20 の指数 71,240.01 ÷ GDP 30,769.7 = 231.5% に対し、
   同日にサイトが手動で載せていた参照値は 232%。差 0.5pp で**1ポイント≒10億ドルが正しい**と確認（テスト J3 で固定）
3. **Crypto恐怖&強欲に CNN の区分を当てていた**（既存の欠陥）。両ソースの全公開値で境界を実測すると別物で、
   値46をサイトは「中立」、発表元は "Fear" と呼んでいた。→ 各ソース自身の区分へ分離
4. **停止検知の閾値「一律3日」は誤り**。353日ぶんを日毎に回した誤検知＝一律2日:174回／**一律3日:24回**／
   一律4日:2回／**系列別:0回**。実測の最大間隔（vix4・cnn4・crypto1・buffett5日）から系列別に置いた

### ✅ 2026-08-22: 既存の欠陥3件（A・B・C）すべて対応完了

| # | 内容 | 状態 |
|---|---|---|
| B | vix.html の警戒ライン・恐怖ラインの点線 | **修正済み・ライブ確認済み**。`build_vix_html` の `<head>` に `chartjs-plugin-annotation` の `<script>` を追加（他ページと同じ1行）。本番vix.htmlで `Chart.getChart().options.plugins.annotation.annotations` に2件登録されていることをJS実行で確認 |
| C | ダークモードが5ページ（vix/charts/hot-assets/calendar/market-health）で機能しない | **修正済み・ライブ確認済み**。根本原因は `site-search.js` 単体ではなく、**`safe_write()` が全ページに自動注入する `NEWSLETTER_FORM`／`BACK_TO_TOP` の `<style>` ブロック**（body.dark対応ずみの自己完結ウィジェット）が、ページ本体より先にDOMへ乗る位置にあり、判定JSの「`body.dark` セレクタが1つでもあれば明示ルールあり」という緩い判定に誤ってヒットしていた（index/guidesは自前の本体ダークCSSを別途持つので症状が出なかっただけ）。3箇所の注入元（site-search.js の動的`<style>`／NEWSLETTER_FORM／BACK_TO_TOP）に `data-mw-scoped="widget"` を付け、判定JS（7箇所すべて）がそれをスキップするよう修正。本番vix.htmlで `toggleTheme()` 実行→背景が実際に `#0d1117` になることを確認 |
| A | 騰落レシオが別の指標だった件 | **修正済み・J-Quants方式で本番実装**（オーナー判断＝J-Quants方式を選択）。詳細は次項 |

#### A の詳細（8/22・J-Quants方式で実装完了）

- **一次ソース（nikkei225jp.com）は直せない**（ページ構造変更＋robots.txtがJSONエンドポイントをDisallow）。旧フォールバック（TOPIX ETFの日次騰落"日数"を銘柄数の代わりに使う近似）は指標として別物だった（詳細は上の調査ログ）ため削除。
- **新規実装 `build_touraku_history.py`**（J-Quants `equities/bars/daily`＋`equities/master` で東証プライム全銘柄の終値を取得→前日比で値上がり/値下がり銘柄数を集計）が `touraku-history.json`（**SYNC禁忌**＝Actions生成・`check_site_consistency.py`のSYNC_FORBIDDENに登録済み）へ日次で追記。`generate_market_news.py` の `get_touraku_ratio()` はこのJSONから直近25営業日ぶんの `ratio25()` を計算する（一次ソースの生死に関わらず安定動作）。
- **収集は `update-market-news.yml` に相乗り**（`build_health_history.py` と同じ位置＝生成の**前**・`continue-on-error`）。API呼び出しは1回の生成あたり約10コール（プライム銘柄マスタ1件＋直近10暦日ぶんの日足）＝軽量
- **ローカルで実データバックフィル済み**（`python build_touraku_history.py --backfill`＝86営業日ぶん取得・`touraku-history.json` に seed 済み）。**現在の25日騰落レシオ＝116.8%**（2026-08-21時点、真の公式値とほぼ完全一致）。`get_touraku_ratio()` を実際に呼んで116.8を返すこと・`analyze_touraku()` が「通常/🟢」に正しく分類することを実測確認済み
- テスト `_test_touraku_history.py` — **19/19 PASS**（merge_points冪等性・compute_up_down前日値照合・ratio25の窓境界と0除算・calendar_days_back土日除外・load_historyの堅牢性）
- **🔴 オーナーの手が要る最後の1点**: **GitHub リポジトリに `JQUANTS_API_KEY` シークレットを登録してください**（Settings > Secrets and variables > Actions > New repository secret。値は `market-news-config.json` の `jquants_api_key` と同じもの）。`touraku-history.json` は SYNC禁忌（ローカルからpush不可）だが、`build_touraku_history.py` は `market-health-history.json` と同じ「蓄積点が少なければ自動でバックフィル相当のレンジを取りに行く」自己修復ロジック（`SELF_HEAL_MIN_POINTS`）を持たせてある＝**シークレット登録後の最初のrunで自動的に約4ヶ月ぶん埋まり、手でファイルを転送する必要は無い**。**登録するまでは**`equities/master` 呼び出しが失敗し続け（continue-on-errorで握り潰される）、騰落レシオは「取得不可」表示のまま＝壊れはしない
- 参考: どちらの方式で進めるかは一度オーナーに確認し、「J-Quants方式（正確・推奨）」を選択いただいた上で実装した

### 📎 2026-08-17〜19 にやったこと（要点）

| 件 | 結果 |
|---|---|
| 🔴 **メールが15日間止まっていた** | 原因＝**Gmailアプリパスワードの失効**（8/1に2サイト目の鍵を作った前後で消えた）。オーナーが再発行して復旧・実測で送信成功。⚠️**MW用の鍵は一覧から消えていた／ai-tsukaikata用は生存**＝同じGoogleアカウントなので、今後アカウントのパスワードを変えると**両サイトのメールが同時に死ぬ** |
| 恒久対策 | `check_automation_health.py` に **⑨＝毎朝 login だけ試す**（送信しない）を新設。本番で3日連続 🟢。①に `weekly-zone-email.yml` も追加 |
| 🔑 **なぜ15日気づけなかったか** | メール送信5レーンのうち**赤くなるのは `weekly-zone-email` だけ**（他4本は `except Exception` で握り潰して緑）。その唯一の警報が監視対象外だった。⚠️**握り潰し自体は正しい設計**（送信失敗でシグナル記録を巻き添えにしない）ので変えていない |
| ⑦ NHK経済フィード | **NHK側のRSSが全カテゴリ停止**（Last-Modified 8/8・cat0/4/5/6 とも同時・移設先なし）と実測して削除。監視12本すべて閾値内 |
| ⑤ topicキュー | 3本→**9本**（#40〜45を追加。公開済み334本と突合して重複ゼロを確認） |
| ⑧ book-watch | ゲート赤の原因は**既存エントリの旧ブランドカラー5箇所**（今回の追加が原因ではない）。`BOOKWATCH_GUIDE.md` に `apply_brand_color.py` 実行を追加→**routine が自分で直して 8/18 に公開**＝SOP修正が効くことを実測 |
| 🚩 **#072 を修正して公開** | Opusコンプラの指摘は2件だったが、**独立に再計算するとIS列4行すべてが誤り**（金属36.0%→**22.8%**／指数〜44%→**56.0%**／BTC43.2%→**40.5%**／原油55.9%→**60.0%**）。結論「指数グループも安定して改善」は**事実と逆**（-5.0ppの低下）だったため書き直し。claims 16→20本・`asof` 内蔵（元は `null`＝SOP違反）。20/20緑で 8/18付け公開・ライブ200 |
| 🔒 **固定オラクルを拡張** | `signal_lab_verify.py` に **`period_label_check`**＝IS/FWD を名乗る列の値がその期間の claim 由来でなければ RED。#067→#072 と**2回同型**だったため昇格。過去66本で実測し**赤は#072のみ・65本は緑のまま**。テスト `_test_period_label.py` 17件 |

### 🔑 この3日で学んだこと（次に効くもの）

1. **新しい番人は、入れる前に過去全件で誤検知を測る。** 今回それをやったから初版のバグに気づけた
   （`fired_from` と `fired_before` を両方持つ claim＝FWDの部分窓を IS と誤分類し、**正しい記事 #071 を「取り違え3件」と誤判定**していた）。
   測らずに入れていたら「正しい記事が毎朝赤くなり、しかも人間が番人を信じてしまう」いちばん質の悪い壊れ方をしていた。
2. ⚠️ **ローカルの `signals-log.json` は放っておくと古くなる**（今回**6日古い**8/12版で、最初の再計算がずれた）。
   SYNC禁忌＝push はされないが**取り込みもされない**。研究系の再計算をする前に必ずリモートから取り直すこと。
3. **監査AIの指摘は「見つかった分」であって「全部」ではない。** #072 は 4件中2件しか挙がっていなかった。
4. **ローカルの `guides.html` / `generate_market_news.py` は毎回古い前提で。** 今回2回とも古く、
   そのまま公開していればクラウドが出した記事を巻き戻していた。**公開前に必ず reconcile**。

### 📌 積み残し（着手判断が要る）

- **「claim不在」の数字**＝記事の表に出ているのにどの claim にも対応しない値。期間宣言のある記事5本で**26件**（#069:11・#071:7・#072:7・#070:1・**#067:0**）。
  #067 だけ 0 件＝きちんと直された回。⚠️ ゲート化は**今日は見送った**（同じ日に2本目の番人を入れると検証が薄くなるため）。締めるなら過去全件の実測から。

---

> ## ✅ 2026-08-13: 投資詐欺シリーズ発足・第1号公開（格言キュー枯渇の後継レーン）
> - **格言シリーズ枯渇を確定**：キュー33本全消化（8/6エスカレ・routine空転8日目）。**宿題残り＝#23「セル・イン・メイ」が既存 guide-sell-in-may.html との重複で人間判断待ち**（統廃合/スラッグ差別化/相互リンクのいずれか）＋キュー補充するかの判断
> - **新シリーズ「投資詐欺から身を守る」**：単一ソース＝`drafts/SCAM_GUIDE.md`（SOP＋手口キュー10本・SYNC入り）。法務鉄則＝実名を書かない/判定させず確認手順/相談窓口必須/統計は一次のみ
> - **第1号公開**：`guide-scam-sns-celebrity-ad.html`（SNS型・著名人なりすまし広告）。ゲート＝決定論GREEN→コンプラOpus→独立Opus→最終Opus白。警察庁2026上半期公表値（797.9億円・5,893件≒1件1,354万円）
> - 🔑 教訓＝**検索AI要約の数字を鵜呑みにしない**（1,362.5万円は公表2値の割り算と不一致→監査で発覚）・出典なしの他類型比較を書かない（「オレオレ=数十万円」は一桁誤りだった）
> - **副次修正**：ライブ総論 `guide-investment-scams.html` の金融庁相談ダイヤルが誤番号だった（0570-016812→正 0570-050588・IP 03-6206-6066）＝修正済み。**被害者が誤番号に架電する実害級**
> - **✅ 同日追加（オーナー指示「すべて実行」で3件完遂）**：①格言キューに#34〜39補充（次の枯渇≒8/20頃）②#23セル・イン・メイ＝クローズ（既存記事へ統合・執筆しない・キュー行に⛔明記＋台帳に記録）③**routine `scam-daily-auto` 新設**（`trig_01Vt1tdpbcPqPkRZvNYPc5HT`・毎日11:12 JST・初回8/14＝キュー#2ロマンス詐欺。proverbと同env/model・SCAM_GUIDE §3法務鉄則をプロンプトに内蔵）
> - ⏰ 明日の確認2点：10:12の proverb（#34損切りは早く利は伸ばせ を選ぶはず）と 11:12の scam 初回（#2）。どちらもエスカレなら台帳を見る


> ## ✅ 2026-08-12: #065/#067 エスカレ復旧＝完了・公開済み
> - **#065**（3日滞留）: 免責3箇所＋軟化6件＋監査3巡の追加修正（計15件・うち #059行の -0.650→-0.417 誤帰属を出典照合で修正）→ verify 6/6緑（--asof凍結）→ Opus4巡で白 → `--allow-backdate` で 8/9 付け公開（復旧手順書§2 A案）
> - **#067**（2日連続エスカレ＝ループ）: 原因はオラクルに期間次元が無く「IS」列へ全期間の数字を書いても緑で通る構造穴。**`signal_lab_verify.py` に `fired_before`/`fired_from` を正式拡張**（制約を増やす方向・match末尾）→ claims 22件をIS/FWD分離で再構築（asof内蔵）→ 本文IS列を真のIS値へ全面書き直し（真のISはトレンド勾配なし・IS 39.1%=分岐割れを自己開示）→ 22/22緑 → コンプラOpus→独立Opus白 → 8/12付け公開
> - 再発防止: `SIGNAL_LAB_SOP.md` に「claimsにasof必須」「IS/FWD比較記事は期間をclaimsで宣言必須」を追記。CLAUDE.md の filterキー一覧に regime4/fired_before/fired_from を反映。復旧手順書§3に「オラクルの表現力不足が原因のループはキー拡張で塞ぐ」を追記
> - ⚠️ 残るオーナー確認1件: routine プロンプト本体（リポジトリ外）が SOP の新ルールを拾うかは**明朝 08-13 の生成物で確認**（claims に asof と fired_before/fired_from が入っていれば浸透）


> ## ⚖️ 法務の持ち越し（全文は SESSION_ARCHIVE）
> ① **記事末尾の「対応戦略」テンプレが黒の製造機**（11/13がここ）＝「読者に何をさせるかで締める」書き方をしない。
> ② 弁護士アジェンダの最重要＝**「不特定多数向け・随時閲覧可能な媒体」除外の射程**（全文は auto-memory）。③ **グレー8件は弁護士相談まで保留**。
> ④ ⚠️**二次情報は数字が壊れる**＝個別銘柄の決算は**決算短信のパース値＋適時開示で照合**してから書く。
>
> ## ✅ **ブランドカラー Phase 1 ＝ 完了**（8/5・ライブ確認済み。306ファイル・巻き戻しゼロ）
> ⚠️ **track-record / political-feed / youtube-summary / 週次 / 月次は「次回の生成時」に反映**（各ワークフローの周期依存）。
>
> ### 決まっていること（再検討不要）
> 藍 `#1E3A6E`（面）／藍(明) `#2C4F8F`（h2・リンク）／山吹 `#E8A317`（**塗り専用**＝白地の文字は2.17:1で不可）／
> 山吹(淡) `#F5EDD8`。**意味色 🟢`#1a7f37` 🔴`#cf222e` 🟡`#9a6700` は不変**。設計書＝`docs/superpowers/specs/2026-08-04-brand-color-design.md`、
> 計画＝`docs/superpowers/plans/2026-08-04-brand-color.md`。ツール＝`apply_brand_color.py`（**テスト62件**）。
> **ロールバック**: `python apply_brand_color.py --phase 1 --include-cloud --revert --apply`（往復不一致ゼロを実測済）。
> バックアップ＝`_backup_brand_2026-08-04/`（292本・`_`始まりでSYNC外）。
>
> ### ⚠️ 実測で分かった落とし穴（Phase 2 で効くものだけ残す。Phase 1 固有の全文＝SESSION_ARCHIVE【2026-08-08／08-11 退避】）
> - **色→色の一括置換は禁止。** `#0969da` の366箇所は役割がバラバラ。RULES は**素の文字列ペアで名指し**（正規表現にすると revert が不正確になる）
> - **「名指しの形が実体と違う」を3回踏んだ**（f-string／`style`属性／`var(--accent)`。3つともコード側で解決済み）＝**疑うときは「どこに書かれた色か」を先に数える**
> - **h1 の font-size は3種**（1.95/1.85/1.8rem）。1種だけ書くと大多数を取りこぼす
> - **ダークで藍は 1.70:1 ＝ ほぼ不可視。** ダークの見出しは `#e6edf3`（16.02:1）を注入。マーカー `/*mw-brand*/` 付きで revert が正確
> - 🔑 **🚫stale で `--force` を反射で打つな。正順＝取り込み → 自分の変換を乗せ直す → 「差分は自分の変換だけ」を機械照合 → `--force`。** 8/5・8/11 の2回とも当たっている
> - 🔑 **適用漏れはライブの computed 値で確かめるのが最短**（ローカルの grep では「役割が違う同じ色」と区別できない）
>
> ### 未完の作業
> | # | 内容 |
> |---|---|
> | 1 | **Task 7: Phase 2**（本文リンクを藍(明)＋**下線**・オーナー承認済み）。⚠️**旧見積「136本403箇所」は f-string 対応前の値で、実測は306本895箇所**（生成ページも対象に入ったため）。`--phase 2` は実装済み |
>
> ✅ **8/5 完了分は SESSION_ARCHIVE【2026-08-11 退避】へ。未完は上の Task 7 Phase 2 のみ。**
> 🔴 8/5 の最大の教訓＝**「壊れたのにアラートが鳴らない」経路を疑え**（Pages が12時間全失敗してもライブは旧ビルドを配信し
> `health-check` は緑のまま＝監視が"日付が今日か"しか見ていなかった）。再発防止は下の強制ルール表が単一の真実。
>
> ✅ **(a)為替介入＝8/3決着・(b)404レポート＝優先度低**（全文は SESSION_ARCHIVE【2026-08-05/08-06 退避】）。持ち越し＝「**外挿の適用限界を🔴で自己申告する型**」は速報記事へ横展開の価値あり。
>
> **(c) 日本株の蓄積＝結論は秋・ゲート到達は10月中旬・人がやる作業ゼロ**（詳細は auto-memory `project_jp_open_edge`・圧縮前全文は SESSION_ARCHIVE【2026-08-06 退避】。本検定は事前登録どおり自動見送り中）。
>
> **持ち越す教訓（コード・memoryに移せなかった分だけ）**
> - **書き込み直後の検証に raw を使わない**＝CDNキャッシュで嘘をつく。**Contents API (`?ref=main`)** で見る。
>   ライブ(marketwatch-jp.com)も `pages-build-deployment` の完了待ちがあるので、main の実体と分けて判断する
> - **新ゲートを足したら既存全件で誤検知率を実測**する（素朴な実在チェックは 217/217件 RED になった）
> - **ゲートは全レーンが通る関門に置く**。片側だけでは別レーンが素通りする（免責検査の穴＝8/1 の実例）

## 🛡️ コードが強制しているルール一覧（"覚える"でなく"コードで強制"）

> 設計思想（CLAUDE.md／auto-memory `feedback_rules_as_code`）＝**人が手で守るルールを、コードが自動で守る形へ**。
> 新ルールは「文書に書いて記憶で守る」より「**チェックを1個足す**」。私が記憶で守るルール数をゼロに近づける。
> 文書が長くなったら、必須ルールはコードへ移して文書から消し、古い履歴はアーカイブする（このスリム化もその一環）。

| 強制しているルール | 受け皿コード（単一の真実） | 効果 |
|---|---|---|
| SYNC禁忌ファイルを誤って push しない | `check_site_consistency.py` の `SYNC_FORBIDDEN`（`mw check`） | 巻き戻し事故を push 前に **error 停止** |
| **ドキュメントの文字でライブが止まるのを防止** 🆕8/5 | `.nojekyll`（Jekyll を丸ごと迂回）＋ `check_site_consistency.py` の `check_liquid_in_markdown` | Jekyll は**同期対象の .md 24件を Liquid として解釈**する。手順書に波括弧2連を書いただけで**Pages ビルドが12時間全失敗**（`duration=0` の即失敗・本文は "Page build failed." のみ）。**ライブは古いビルドを配信し続け health-check も緑**＝人力では気づけない。検査は `.nojekyll` 有無で error/warning を切替（無ければ実際に落ちるので error）。誤検知ゼロを実測 |
| **新記事が旧テンプレ/旧色で公開されるのを防止** 🆕8/5 | `check_guide_draft.py` 検査9（判定は `apply_brand_color` の RULES/TOKENS を流用＝**色の知識を複製しない**） | ①Phase1の役割に旧色が残る ②ブランド色が1つも無い（＝既存と構造が違うテンプレ）の2条件で公開ブロック。`guide-signal-lab-060` はクラウドが**CSS変数式の別テンプレ**で出したため既存ルールが1つも当たらず、60本中1本だけ未適用だった。**誤検知率を全295本で実測＝0本**（真陽性1のみ） |
| **ロゴのラスタ画像が旧色で取り残されるのを防止** 🆕8/5 | `make_favicons.py`（`favicon.svg` を**単一の真実**として Pillow で描き直す。cairosvg 等の依存は足さない） | `apply_logo.py` はHTML内のインラインSVGしか直せず、**favicon-32/192・apple-touch-icon・ico は旧色のまま残る**（SVG対応ブラウザだけ新色という食い違い）。旧SVGで描き起こして現物と照合＝画素の96〜99.6%が一致することを実測してから配色を変えた。⚠️ロゴを変えたら `apply_logo.py --apply` と**両方**回す |
| **コミット連打で Pages のビルド上限を叩かない** 🆕8/5 | `sync_to_github.py --batch`（Git Data API で blob/tree→**1コミット**。テキストは tree に content 直埋め・バイナリのみ blob 化） | Contents API は**1ファイル＝1コミット**なので 257件の sync がビルド連打になる（実測 JST04時台48件/05時台11件/06時台25件・目安は10回/時）。一括なら**内容一致251件は tree 取得1回で通信ゼロ判定**＝往復も激減。staleness ガードと `--force` の意味は逐次版と同一 |
| 公開前に main を取り込む（reconcile） | `publish_article.py` 内蔵 reconcile | ローカル公開での巻き戻しを防止 |
| **ローカルが古い状態での sync 巻き戻し防止** 🆕 | `sync_to_github.py` の staleness ガード（remote_sha baseline 比較） | 前回 sync 後に GitHub 側が更新されたファイルの push を **🚫中止**（意図的なら `--force`） |
| **公開記事が guides.html カードから消えていないか** 🆕 | `check_automation_health.py` §③（`automation-health.yml` 毎朝09:30 JST） | 巻き戻し（local-drift）を **翌朝 Issue で即検知** |
| **同一 workflow の同時実行レース防止** 🆕 | `update-market-news.yml` の `concurrency`（cancel-in-progress: true） | push(on:push)＋手動trigger の二重起動を新しい方に一本化＝失敗run・誤アラートを根絶 |
| kinsho-v1 免責 / 10ボタンナビ / リンク切れ / SYNC_FILES登録 | `check_site_consistency.py`（`mw check`／土曜 `site-qa-lint`） | 不変条件を push 前に検査・exit 1 |
| 研究日誌の数値捏造防止 | `signal_lab_verify.py`（固定オラクル・編集禁止） | claims.json を signals-log から独立再計算して突合 |
| 更新履歴の整列・最新5件 | `generate_market_news.py` の `_history_items` | 手で削らない（日付降順・自動整列） |
| **発注前ルールの遵守を数値監視** 🆕 | `_trade_discipline_check.py`（`mw discipline`・週次 /loop） | 指標持ち越し/指数重ね張り/SL未設定/損切りずらし/JP✕を **EVダメージ順に可視化** |
| **相関ポジ合算リスク2%・実スリッページ** 🆕7/5 | 同上 R6/SLP（フォーム任意3欄=口座残高/リスク額/予定価格の入力分から自動判定） | 同テーマ×同方向の同時保有を横断検出し合算%で2%判定（金銀・FXも対象＝指数限定R3の一般化）。前向き検証の群割当は登録時5ルール固定＝R6は参考枠 |
| **公開日のUTC日付ミス防止** 🆕7/6 | `signal_lab_verify.py` の `date_check()`（再監査は `SIGNAL_LAB_SKIP_DATE_CHECK=1`） | datePublished/公開表記≠JST今日なら**赤=公開ブロック**（#031が7/5付けで公開された事故の再発防止） |
| **似スラッグの重複記事防止** 🆕7/6 | `check_guide_draft.py` 検査7 スラッグ重複検査（トークン集合の同一/包含） | ⑫⑬型の「語順違い/部分一致スラッグの同一主題」自動公開を**RED=人間エスカレ**（82記事総当たりで誤検知ゼロ） |
| **研究台帳の数値転記ミス・改竄・肥大化防止** 🆕7/7 | `_doctrine_check.py`（`mw evolve`・固定オラクル扱い） | DOCTRINEのアンカー**全件**（件数は書かない＝古びる）を出典JSON/mdと突合＋事前登録簿SHA256＋サイズ予算＋§1構造検査で**error停止**。予算は7/31に30/36KBへ改定＝上の(d)節が根拠。⚠️prose は**部分一致**なので、退避時は必ず前後で `_doctrine_check.py` を回す（偽陽性通過の実例あり） |
| **自動生成レーンの投資助言化を防止** 🆕8/1 | **`compliance_gate.py`**＝禁止語の単一の真実（SYNC対象）。**3レーン全部**が import＝`auto_weekly_review`／`generate_monthly_report`／`auto_weekly_strategy`（テスト=**`_test_weekly_review_gate.py` 18件＋`_test_compliance_gate.py` 31件**） | 禁止語に触れた生成文は不採用→次モデル→全滅なら決定論テンプレへ。**LLMの自己申告に頼らずコードで止める**（プロンプト修正だけでは再発する）。⚠️**8/1夕方の追加調査で、同じ穴が月次・週次戦略にも在ると判明**＝weekly-review を直した時点では塞がっていなかった。良性マスク（`BENIGN_PHRASES`＝「注目すべき」等）で週次戦略の誤検知を 1/10→0/10 に実測 |
| **免責(kinsho-v1)の全レーン検査** 🆕8/1 | `check_site_consistency.py` の免責検査を `AUTO_PREFIXES` の `continue` **より前**へ移動 | 除外が SYNC登録/リンク切れの誤検知回避のために置かれ、**巻き添えで免責検査まで飛んでいた**＝免責ゼロ2本を1本も検知できず。免責は生成レーンを問わない共通不変条件。誤検知は実測ゼロ（278本中、未設置は真に2本） |
| **銘柄チャートを記事に埋め込む** 🆕8/1 | `_gen_stock_panel.py`（テスト=**`_test_gen_stock_panel.py` 30件**） | 座標は全て計算（目分量のSVGは必ず破綻）。**10倍超の値動きは自動で対数軸**／軸に負の株価を出さない／x軸右端は必ず最終足／**最終足の出来高が未確定なら描かず注記**（決算日の出来高が中央値の2.9%＝Yahooの未確定値で「急騰は薄商い」と逆の示唆になる事故を防止）／トレンドライン・目標株価・売買示唆は描かない |
| **「登録したが走らない設計」の防止** 🆕7/31 | `_precheck_feasibility.py`（`control`＝同日対照が必要本数取れるか／`dim`＝使う次元が対象ログに在るか。テスト=**`_test_precheck_feasibility.py` 13件**）＋`mw evolve` の次候補に欠落欄を併記 | 必須欄チェックは**欄が埋まっているか**しか見ておらず、**埋めた設計が実行可能かは見ていない**。Q10（19銘柄に `min_ctl=30`＝理論上限18）と Q24（BTログの `news_count` 保有0.0%）を1本ずつ失って追加。⚠️**独立warningは足さず、提案するその場で欠落を出す**＝鳴りっぱなしを1本も増やさない設計 |
| **非公開研究（research/）の公開リポ流出防止** 🆕7/31 | `check_site_consistency.py` 検査1 `check_sync_forbidden`＝**ディレクトリ単位**の規則（テスト=**`_test_sync_research_guard.py` 22件**） | 列挙式は足し忘れが穴になる＝実測で research/ の非アンダースコア .md **13件中 登録済みは2件だけ**（`hypothesis_queue_archive.md`＝非公開仮説の全文147KB すら未登録）。境界は実態と一致（SYNC_FILES に research/ 配下は皆無）＝**誤検知率は全件でテストが毎回実測** |
| **非公開研究ファイルの公開リポ流出防止** 🆕7/7 | `check_site_consistency.py`＝SYNC_FORBIDDEN追加＋**`_`プレフィックス=ローカル専用規約** | DOCTRINE/queue/`_jp_*`等が SYNC_FILES に混入したら**error停止**（REDテスト7ケース済） |
| **公開記事への下書き残骸混入防止** 🆕7/7 | `signal_lab_verify.py` date_check の残骸検査 | 「下書き中」が本文に残っていたら**赤=公開ブロック**（#032実例の再発防止） |
| **休場中の発火を勝率に含めない** 🆕7/11 | エンジン=`generate_technical_alerts.py`週末閉場ガード（土07:00〜月06:00 JST・BTC除外・発火スキップ）＋集計=`is_weekend_closed_fire`（track-record/週次/月次の3本に同一定義複製） | 塩漬けデータ発火（実測214件・勝率33% vs 全体41.6%＝週明けギャップでSL直撃の測定アーティファクト）を源流と集計の両方で遮断。生ログは不変・ページに除外注記あり |
| **ローカル公開の日付事故防止** 🆕7/22 | `publish_article.py` の `check_date_gate`（免除は `--allow-backdate`・テスト=`_test_publish_date_gate.py` 5件） | 公開日≠JST今日なら **🚫 exit 1 で公開停止**（7/15事故の恒久対策・signal-lab date_check と同型） |
| **自動公開レーンの「静かな停止」検知** 🆕7/26 | `check_automation_health.py` §⑤（`automation-health.yml` 毎朝09:30 JST・テスト=`_test_topic_queue.py` 12件） | autodraft の未公開 topic が5件未満で **Issue**。①②は「走ったか」しか見ないのでキュー枯渇による仕様どおりの停止を捕まえられなかった（7/20〜24 に5日連続スキップを誰も検知できなかった実例）|
| **事前登録した仮説の「登録漏れ」検知** 🆕7/27 | `check_automation_health.py` §⑥（同 09:30 JST・`check_tracker_registration`） | コード側の宣言（`SEED`＋register定数）と実体（`signal-lab-tracker.json`）を突合し、**idもfilterも不在**なら **Issue**。7/27 に Q35の3件が「SEEDに足しただけ＝一度も登録されず」なのに台帳が「観測開始」と書いていた事故の恒久対策。**filter重複による正常スキップ（`metal_all_1d`等4件）は誤検知しない**ことを実データで確認済み |
| **エスカレした研究日誌が「直せなくなる」のを防止** 🆕8/11 | `signal_lab_verify.py` の **基準時刻凍結**（claims.json の `"asof"` ／ CLI `--asof`。打ち切りは `outcome_resolved_at`＝決済確定時刻。テスト=**`_test_asof_freeze.py` 16件**）。**使い方の正は `SIGLAB_ESCALATION_RECOVERY.md`** | 🚩が付いた回は**後日ライブログが進むと永久にREDで直せなかった**（#065 0/6・#067 は当日夕方に 11/11→2/11）。凍結で **#065 6/6・#067 11/11 に完全再現**を実測。⚠️捏造不可は不変＝数字は実ログとの完全一致が必要で asof は断面を選べるだけ。しかも **asof の日付は記事の公開日と一致必須**。⚠️`fired_at` で切ると誤り |
| **エスカレの滞留検知** 🆕8/11 | `check_automation_health.py` §⑧（`ESCALATION_STALE_DAYS=1`・テスト=**`_test_escalation_backlog.py` 13件**） | 「走ったが人間待ちで止まっている」形は①②③⑤の全部の死角だった＝オーナーが**目視**で気づくまで #065 が2日滞留。解決判定は公開実体（`guide-<target>.html` の有無）。🚩当日は鳴らさない（ゲートが働いた正常系） |
| **やりかけの仮説を増やしすぎない（WIP上限）** 🆕8/11 | `_doctrine_check.py` の `QUEUE_WARN_N/QUEUE_ERR_N`＝**12本で黄信号・16本で error**（`check_queue_wip`・テスト10件。**根拠と閾値の単一ソースは同ファイルの定数コメント**） | 旧・キューのバイト予算(36KB)を置換。要点＝36KBは「1.3KB/本」前提だったが、証拠要件を入れた **7/26以降**のブロックは平均7.8KB／以前は1.6KB＝**厚く書くほど罰せられる**逆向きの予算だった（較正が1日ずれていた）。error は**1本閉じるまで新規登録しない**WIP上限。⚠️`declutter_audit.py` の表からキューを外した（二次側が一次側と食い違う対処を出すため） |
| **事前登録の「空欄のまま登録済み」防止** 🆕7/26 | `_doctrine_check.py` の `REQUIRED_Q_FIELDS`＋`_q_field_gaps`（回帰テスト=**`_test_doctrine_registry.py` 23件**・実キュー31件でE2E確認） | 新Qは 登録日/ルール素案/検証設計/**対照**/主要評価指標/合格基準/**検出力** が埋まるまで **error＝登録簿に載せない**。SHA256は登録"後"の改竄しか見ておらず、テンプレのまま登録される穴があった。既存Qには遡及しない |
| **取り直せないスナップショットの欠測検知** 🆕7/28 | `_doctrine_check.py --agenda`（`mw evolve`）の心拍鮮度＋`_jp_earnings_cal_logger.py` の追記/冪等 | 決算カレンダーは**翌営業日1日分・履歴なし＝走らなかった日は永久欠測**。3日沈黙で ⚠️。**automation-health は GitHub 側でローカル専用ロガーを見られない**ため番人をここに置いた。BOM有無/沈黙/正常の3分岐を実測（BOMで例外→握り潰し→**番人が黙る**壊れ方を実際に踏んで修正済み） |
| **実在しない記事へのリンク公開を防止** 🆕7/30 | `publish_article.py` の `check_link_gate`（判定は `check_guide_draft.internal_link_check` に一本化＝基準の単一ソース。テスト=**`_test_guide_link_check.py` 20件＋`_test_publish_link_gate.py` 7件**） | 参照先が実ファイルとして存在しなければ **🚫 exit 1 で公開停止**（免除は `--allow-missing-links`）。Search Console の404の恒久対策。**要点は「全レーンが通る関門に置く」**＝`check_guide_draft` 側だけでは news/proverb レーンが素通りする。併せて `CLOUD_GENERATED` でSYNC禁忌ページを除外しないと**ナビ経由で全記事RED**（実測217/217→5件） |
| **ローカルミラーの遅行を解消** 🆕7/31 | `_pull_mirror.py`（ローカル専用・冪等・dry-run既定） | クラウドが公開/更新した記事を取り込む。**内容ハッシュ(git blob sha)で比較**するので「ローカルに在るが古い」も検出。取り込み内容＝リモートと同一なので **sync は「⏭️内容変更なし」でスキップ＝無駄なコミットが出ない**。`guide-new-books.html` は SYNC_FORBIDDEN のため除外。⚠️これを怠ると `mw check`・404監査・FPテストが**揃って誤検知**する（7/31 に3回） |
| sitemap 全記事網羅 | `generate_market_news.py` の `build_sitemap_xml`＋`is_noindex_slug`（除外の単一ソース） | 全 guide を自動収集・手動編集不要。未掲載＝noindex 対象の意図的除外（7/31 実測で55本中54本が該当＝**不具合ではない**）。⚠️新記事公開時は sync 後に **workflow を手動 trigger**（下記の push 順序） |
| **「Run failed」の誤判定を防ぐ** 🆕8/8 | `judge_runs`（`check_automation_health.py`・テスト16件） | 8/6の失敗11件は**10件がcancelled・失敗step0**＋1件が GitHub の `Service Unavailable`＝コード起因ゼロ。旧番人は直近1件の`!=success`判定＝**誤Issueの時限爆弾**。新＝cancelled除外・閾値内にsuccess 0なら異常。⚠️`signal-workflows`群は`signals-log.json`共有＝**分割禁止**（詳細=auto-memory `reference_actions_failure_triage`） |
| **tickerフィードの停止検知** 🆕8/6 | `build_news_ticker.py` の `feed_health`＋§⑦（閾値=`FEEDS`の`stale_days`・テスト27件） | workflow緑のまま特定フィードだけ死ぬ形（8/1 Bloomberg型）を捕捉。同日ソース10→18本（公的機関+トピック横断・バッジは実発行元）。トピック検索は監視外＝誤検知ゼロ方針 |

🆕＝2026-06-20 追加（B＝カバレッジ番人 ／ C＝sync staleness ガード）。新ルールはこの表に1行＋チェック1個で増やす。

---

## ⚠️ 絶対遵守（事故防止）

- **SYNC禁忌**（ローカルから絶対 push しない＝routine/cron/generate が GitHub 側で生成）。**正は CLAUDE.md の SYNC禁忌リスト**。代表例：
  6コアHTML（index/calendar/charts/vix/market-health/hot-assets）／`signals-log.json`／`technical-alerts-history*.json`／`track-record.html`／political系／youtube系／`fundamental-context.json`／`weekly-levels.json`／`weekly-zone-plan.md`／`sitemap.xml`／`weekly-strategy-context.json`／`indicator-result.json`／`signal-lab-tracker.json`／`signals-log-backtest.json`／`article-ideas.md`／`daily-preview.md`／`political-digest.md`／`compliance-scan.md`／`site-qa-report.md`／`panic-scan.md`／`drafts/draft-*`・`drafts/news/*`・`drafts/sns/*`
  → `mw check`（`check_site_consistency.py`）が SYNC_FILES への誤混入を、sync の staleガードが「古いローカルでの上書き」を、それぞれ自動で止める。
- **SYNC対象（OK）**：`*.py`（※`sync_to_github.py` はローカル専用＝GitHub側は**クラウド用スタブ**。⚠️2026-07-28訂正＝**サイズで判定しない**。クラウドが記事を公開するたび `publish_article.py` が追記するので**増え続ける**（616B→3,763B/84記事）。逆に**クラウド公開記事の台帳として使える**。**`mw.py` はSYNC対象**＝7/7訂正。`_`プレフィックスのファイルは全てローカル専用＝SYNC禁止をコードで強制済）／`.github/workflows/*.yml`／個別 `guide-*.html`／`guides.html`／`robots.txt`／`my-trades.json`／`memory/*.md`／各 docs。
- 記事追加は **`python mw.py publish ...` → sync → workflow → ライブ確認**。公開前に compliance-reviewer(Opus)監査・教育トーン・特定銘柄の買い推奨は書かない・kinsho-v1 免責・10ボタンナビ。手動時も `mw check` で push 前点検。
- ネット不調時は無限リトライせず、ブラウザで手動 trigger を依頼（最大3〜5回）。
- **価格キャッシュは消さない**＝Yahoo の遡及上限（1h=730日/15m=60日）を超える分は**再取得不能**（`_all1h` は329日ぶん）。

---

## 📌 アクティブな宿題

## ✅ 大量保有報告書＝**採取完了・Q45 事前登録済み**（2026-08-11 14:58）

> オーナー依頼の2問（①新規提出時の株価 ②「地雷株」の判別）。**台帳採取→検出力実測→Q45登録まで完了**。
> 経緯の全文＝`research/_q45_design.md`／約束（分母・合格基準・不採用案・限界の開示）＝キューの `### Q45` ブロック。
>
> **取り直せないデータは確保済み**（最優先だった）＝`research/_lvh_raw/`（**1,329日・失敗0・府令060 73,653件**／新規10,518件・生レコードは1列も捨てず保存）＋
> `_lvh_secmap_merged.json`（**4,308社**の `EDINETコード→当時の証券コード` 履歴名簿）。
> ⚠️ **窓は毎日先頭が削れる**（2021-08-10以前は既にゼロ）＝**分析は保存済み台帳に対して行い、EDINETを叩き直さない**（同じ標本は二度と作れない）。
>
> **素案は実測で4点誤っていた**（全文＝`_q45_design.md` §2）。最大のものは 🔴 **解決層で生存バイアスが再注入されるところだった**
> ＝EDINET名簿は証券コードを「現在上場」の3,823社にしか持たず、**上場廃止＋改称した会社＝質問Bの標的が辿れない**
> （実例＝現在名「メルディア」＝旧・三栄建築設計32280）。第2パスで **取り逃し0%** に。他3点＝新規は一般/特例の2種でほぼ半々／
> 提出は**場中が63%**（「16-17時台が多い」は誤り）／標本開始2021-09-01。
>
> **検出力（＝登録できた根拠。全表は `_q45_design.md` §4）**: 一般報告 h=20 で train MDE 1.42%/holdout 1.53%＝往復コストと同じ桁。
> ⚠️ **h=60 は合否に使わない**（holdout MDE 4.22%＋ブロック数5＝二重に不成立）。B＝既存ゲート（+13〜48pp）が MDE（5.3/7.9pp）を上回る＝**移植可能性は判定できる／新指標の発明は不可**。
>
> **次の一手**＝Q45 の本検定。⚠️ **`_lvh_power.py` は効果量を意図的に印字しない**設計なので、本検定は別スクリプトで書く。
>
> ✅ **キュー予算は「バイト数」→「アクティブQの本数」へ変更（2026-08-11・オーナー承認）**＝下の強制ルール表に1行追加。現在8本／黄信号12本／上限16本＝緑。

✅ **エスカレ2件（#065/#067）は 2026-08-12 に解決・公開済み**＝上の「✅ 2026-08-12」節が要点。手順の正は `SIGLAB_ESCALATION_RECOVERY.md`（SYNC入り）。
⚠️ ここから引き継ぐのは1点だけ＝**routine が `claims.json` に `asof` を書くようプロンプトへ追記するのはオーナー作業**（routine のプロンプトは repo 外）。

## 🧹 2026-08-11 キュー整理（詳細は auto-memory `project_evolution_loop`）

> 🔴 **止まって見えたのは表示が古かっただけ**＝Q7/Q16 のトラッカーは手動実行設計で1か月誰も回さず、`mw evolve` は
> Q16 を「N 0/100」と表示。実際に回すと **N=27/100・初判定9月上旬**で研究は進んでいた（危うく削除候補にするところだった）。
> 恒久対策＝タスク **`MarketWatch_Research_Trackers`（毎日07:40）**。**前向き観察6本は全て健在**。退避は Q10／Q27 の2件。

| # | 次の一手 | 状態 |
|---|---|---|
| **A** | ~~寄り付き系3択~~ → **7/28 ③守りゲートに決定→着手前チェックでQ番号は発行せず「カバレッジ監査」へ切替**（却下理由＝検出力が"過剰"／📅列は Q14 H1 で登録済＝重複）。未検証は効果量でなく **📅 の precision/recall**。**判定 2026-09-01 目安** | ✅判断済・観測中。人の手作業ゼロ |
| A2 | 残る2択は未着手のまま＝①**裏返し（L1翌日の寄り天売り）**は⚠️**実行可能性の確認が先**（信用売り可否・逆日歩・空売り規制・貸株在庫） ②**「①マイナス寄り vs ③5〜10%」の差の検定** | オーナー判断待ち |
| 0〜0c2 | ~~Q44の事前登録・トラッカーpush・既見窓の重なり実測（7/27分の完了4行）~~ → **2026-07-31 に SESSION_ARCHIVE.md【2026-07-31 退避】へ移動**。要点＝Q44は前向きN≥91で**1回だけ**判定（≒2026-10上旬・覗き見しない）／登録の番人は `check_automation_health.py` §⑥／既見窓の重なりは BTC×1h 1件のみで仮説に不利でない方向 | ✅退避済み |
| 0d | `metal_all_1d`／`other_fx_revL`／`other_fx_long` に**同型の tf 混在**が残る（idは日足を名乗るのに filter に tf 無し）。BTCと同じ棚卸しが要るか判断 | 未着手 |
| 2 | **フォーム3欄（口座残高/リスク額/予定価格）＝1件目の取引で入力**。無いと2%ルール/相関合算/実スリッページが測れず**チェックポイントが計算できない** | **オーナー作業**（取引再開が近い） |
| 3 | **BT3本の再生成タイミング統一**＝`signals-log-backtest-fx.json` だけ 6/16 で止まり**検出器が3種少ない**（`support_bounce`/`double_bottom`/`double_top` 欠落）。他2本は7/19再生成 | Q34/Q35の既知の不備（結論は不変） |
| 4 | Q29 → **2026-09頃に1回だけ再実行**（有効日282日到達時・設計変更なし）。自社株買いの再チェックも同時期 | 事前コミット済み・毎月試すのは禁止 |
| 5 | 規律の前向き検証が **N=0/30 の休眠アーム** | 取引再開で自動起動 |
| 6 | 信頼度スコアの整理（ティア補正の根拠 elite+0.758 が再現しない・メール本文の「HIGH＝優先」文言） | **急がない**＝スコア自体が無反応で実害なし（auto-memory `project_confidence_score_defect`） |
| 7 | **🔴黒の下書き本文を置く「非公開の受け皿」が未実装**（7/26 コンプラ監査の積み残し）。リポジトリは public＝`drafts/` は認証なしで読め、**削除してもgit履歴に残る＝取り消し不可**。⚠️**GitHub Issue も使えない**（public repoのIssueは誰でも読める） | 未着手。実害は現時点ゼロ（🚩9件は全て手続き・技術理由で法務系の黒は0件）＝**次に黒が出たときに初めて効く**穴 |
| 8 | **`weekly-trade-review` routine が `MY_TRADING_RULES.md` の6月時点の旧版を読む**（6/19 にSYNC除外＝ローカル専用にしたため）。7/26 に追加した「デイトレ開始の事前登録」がレビューから見えない | 未着手。**取引再開＝#2 と同時に効いてくる**ので、その前に受け渡し方法を決める |
| 9 | **月次レポート公開3本の本文が未修正**（`guide-monthly-report-2026-05/06/07`）。8/1夕方に発生源はゲートで塞いだが**在庫は残した**＝オーナー判断で「コード側だけ」に限定。露出は noindex・sitemap非掲載・被リンクゼロ＝直URLのみ（**週次レビュー黒と同じプロファイル**）。実物＝07号「4Hシグナルに絞って…積極的なエントリーを試みることを推奨します」／06号「SI=F …ロットを大幅に減らす」 | **オーナー判断待ち**。急がない（次の生成は9/1でゲート済み） |
| 10 | ~~Q24 前向き登録~~ → **8/11 完了**＝主張1本(`news_zero_edge`)・push済(646489d)。⚠️**N=80の初回判定は検出力不足**（σ=1.165R→MDE 0.365R vs 期待効果0.20R）＝非有意を「効果なし」と読まない・実質判断は N≥240(37日)。次の signal-lab-daily(06:10)で tracker に載る＝載らなければ automation-health §⑥ が翌朝Issue化 | ✅完了・要監視1回 |

**セッション冒頭は `python mw.py evolve`**（DOCTRINE突合＋仮説キュー＋トラッカー鮮度＋レジーム状態）。

---


- **⓪ 7/27 午前〜夕方の検証11本（Q33〜Q43）は全て決着済み＝本文は SESSION_ARCHIVE.md（2026-07-27 退避節）へ。**
  結晶化先＝**DOCTRINE §0-11（条件付けは構造には効き相場つきには効かない）／§0-12（出口・建玉・水準は増幅器であって発生器ではない）**、
  各Qの全文＝`research/hypothesis_queue_archive.md` の 2026-07-27 退避節。要点は冒頭★とその訂正3点に集約済み。

<!-- 2026-08-06 declutter: 7/31夜3件・(a)為替/(b)404・(c)全文・輻輳注意・旧入口ナラティブ・旧declutterコメント3本(7/27×2,7/28)を SESSION_ARCHIVE.md【2026-08-06 退避】へ。(f)価格キャッシュ→絶対遵守へ1行・(e)Q24判断待ち→宿題#10へ昇格。 -->

---

## 2026-08-29（続き4）Q47 登録→holdout判定→✅昇格：好決算×ギャップアップの「危険を外すゲート」

**オーナー依頼**＝「登録してください。その時に、高決算ギャップアップで上がる銘柄と、逆に下がる銘柄の特徴も調べてください。失敗銘柄を避けるのは一番大事」

### 結論3行

1. **「上がる特徴」と「下がる特徴」はほぼ同じもの**だった。高ボラ・大ギャップ・52週安値から遠い＝**大勝率も失敗率も同時に上げる**＝方向でなく**振れ幅**を測っている。だから**勝ち選びはできず、負け外ししかできない**
2. 凍結ゲート **`vola20 < 2.0%` かつ `hi52 > −20%`** が holdout で成立＝**失敗率 26.7%→17.8%（差 +8.9pp・95%CI[+3.1,+15.1]・p=0.0013）**、含み損 25.5%→13.4%（+12.1pp・q=0.000）。**大勝率は有意に削っていない**（+1.77pp・p=0.403）
3. **決算の中身は要らない。** 通期計画修正・配当予想修正・進捗率・営業利益YoYの大きさ＝**失敗率にも大勝率にも効かない**。ゲートは**好決算条件を外しても効く**（+11.34pp・q=0.000）

### 手順（＝規律の実演）

- 特徴量 `research/_q47_features.py` → `_q47_features.csv`（**57,878件**・引け後開示のみ・価格特徴は D−1 まで）
- **train/holdout 分割**（train `<2024-07-01` / holdout `≥2024-07-01`）。探索3本（`_q47_explore.py`/`_q47_gate.py`/`_q47_freeze.py`）は**SPLIT前しか読まないハードガード**
- 検出力 `_q47_power.py` は**件数しか印字しない**（効果量を見ずに合格基準を書くため／Q45 `_lvh_power.py` と同じ規律）。holdout N=1,512・MDE 6.1〜8.3pp vs train効果 11.8pp ⇒ 検出可能
- Q47 を登録（`📝 事前登録: Q47 を登録簿に記録`）→ **その後に** `_q47_holdout.py` を**1回だけ**実行

### ⚠️ 一番の教訓＝③が防波堤になった

train では **`hi52` 単独ゲート**が最良に見えた（平均+2.25%・幾何+1.86%・**大勝を落とさない唯一の非対称**）。
これを「良かったほうを後から選ぶ」で主軸にしていたら、**holdout で不成立（+4.43pp・p=0.109）の rule を昇格させていた**。
登録時に**副次③へ落として分解だけ見る**設計にしたことが、そのまま誤採用を防いだ。

### 開示（合否に使わない）

- **年別に不安定**: 2024 内19.8%/外**16.7%**（逆転）／2025 16.1%/25.1%／2026 20.5%/**42.3%**
- グロス・寄り値ちょうどの約定を仮定・好決算はコンセンサス比サプライズではない・業種未使用
- **昇格＝候補の旗立てのみ**。発注・配信・記事化に自動で触れない
- 全文＝`research/_q47_gate_findings.md`（🔒ローカル専用・SYNC外）

### 次の一手（未登録・着手条件つき）

- 副次④＝ゲートは**ギャップアップ全般**に効く可能性（gap≥+5% 全体で 27.5%→16.1%）。**着手条件＝新Q番号で登録し、別の holdout（未使用の 2021-07〜2022-04 or 前向き）で判定**。⚠️ **Q47 の holdout は使い切った**＝同じデータで再判定しない
- 副次②（幾何平均）は検出力不足で不成立のまま。**着手条件＝母集団を倍にする設計**

### 🔴 未決（変わらず）

- ~~Q45 の事前登録違反~~ → **2026-08-29 解決済み**＝無効化して **Q48** として再登録（上の ✅ 節）。⚠️ 台帳 `research/_lvh_raw/`・`_lvh_ledger.json.gz` は 2026-08-11 に1回だけ採取したもので**再取得不能**＝そのまま Q48 で流用する
- `JQUANTS_API_KEY` の Secrets 登録（値＝`market-news-config.json` の `jquants_api_key`）
