# 🔖 セッション引き継ぎ（最終更新: 2026-07-26 22:20）

> **次回の入口＝`python mw.py evolve`**
>
> 🆕 **研究の優先順位がオーナー指示で変わった（7/26・詳細は auto-memory `feedback_research_priority`）**＝**成果の見込み順**に選ぶ。
> ①**勝率・期待値の向上が期待できるもの** ②**高確率で罠になりそうなものを見分けられるもの**。**コンプラNGは公開せず研究内容をメールで送る**（⚠️未実装）。
> **思い込みにとらわれず果敢に**。→ 着手判断は「当たったら勝率/期待値がどう動くか・外れたらどの罠を避けられるか」を先に1行で言えるかで決める。
>
> **次の候補（この順で検討）**
> 1. ~~週足ピボットは効いているか~~ → **7/27 に Q33 として実行＝🚫検定不能でクローズ**（下の⓪-📐節）。**再挑戦は Q34＝タッチを条件にしない設計**が必要で、
>    着手前に検出力チェックからやり直す。データ `_fx1h_prices.json`（9ペア×145週の1時間足）は取得済みで再利用可
> 2. **③資産配分系**は**優先度を下げた**（オーナー「デイトレ中心なので資産配分は先の話。今から考えるのは可」）。⚠️着手時の注意＝
>    `samedate_diff_p`（同日クロスセクション）は**構造的に使えない**（観測が「ポートフォリオ×月」＝同月の対照が1本。`min_ctl=30` を満たせず無言のNaN／
>    下げると"同日対照"の看板だけ残った別物）。流用可は `block_boot_p`/`bh_fdr` のみ。**第一歩は検証でなく対照の選定と検出力見積もり**。
>    ブロック長Lはホライズンでなく**平均保有期間**基準。
>
> ⚠️ **オーナーが近く取引を再開（中心＝デイトレ）**。下の⏱節を先に読むこと。**1件目の取引でフォーム3欄が入らないとチェックポイントが計算できない**。
>
> 📌 **`mw evolve` の warning 2件は既知・追わなくてよい**：①DOCTRINE.md 25.0KB（黄信号24/error28＝残3.0KB。中身は削れないと7/26に検分済み・**予算は動かさずゲージ化した**）
> ②「出典が DOCTRINE.md より新しい: MY_TRADING_RULES.md, _discipline_report.md」＝7/26にデイトレ用チェックポイントを足し `mw discipline` を回したため。
> **これは運用ルールであって検証済み知見ではないので DOCTRINE には結晶化しない**（次にDOCTRINEを触れば消える鮮度warning）。

<!-- 2026-07-24 declutter: 7/21完了節(⓪-7/21/⓪-人気度/⓪-EVアップ/⓪-ナビCSS/⓪-次の作業候補)をSESSION_ARCHIVE.mdへ退避。 -->

<!-- 7/20 完了節（UX利便性改善/指標ステート組合せ/失敗転換検証/シリコンサイクル記事/電池ブロック根治）は 2026-07-21 に SESSION_ARCHIVE.md へ退避。7/19以前の退避分も同ファイル。要点は auto-memory (project_signal_edge_research / project_jp_screener / project_proverb_series) と DOCTRINE に反映済み。-->

## 🛡️ コードが強制しているルール一覧（"覚える"でなく"コードで強制"）

> 設計思想（CLAUDE.md／auto-memory `feedback_rules_as_code`）＝**人が手で守るルールを、コードが自動で守る形へ**。
> 新ルールは「文書に書いて記憶で守る」より「**チェックを1個足す**」。私が記憶で守るルール数をゼロに近づける。
> 文書が長くなったら、必須ルールはコードへ移して文書から消し、古い履歴はアーカイブする（このスリム化もその一環）。

| 強制しているルール | 受け皿コード（単一の真実） | 効果 |
|---|---|---|
| SYNC禁忌ファイルを誤って push しない | `check_site_consistency.py` の `SYNC_FORBIDDEN`（`mw check`） | 巻き戻し事故を push 前に **error 停止** |
| 公開前に main を取り込む（reconcile） | `publish_article.py` 内蔵 reconcile | ローカル公開での巻き戻しを防止 |
| **ローカルが古い状態での sync 巻き戻し防止** 🆕 | `sync_to_github.py` の staleness ガード（remote_sha baseline 比較） | 前回 sync 後に GitHub 側が更新されたファイルの push を **🚫中止**（意図的なら `--force`） |
| **公開記事が guides.html カードから消えていないか** 🆕 | `check_automation_health.py` §③（`automation-health.yml` 毎朝09:30 JST） | 巻き戻し（local-drift）を **翌朝 Issue で即検知** |
| **同一 workflow の同時実行レース防止** 🆕 | `update-market-news.yml` の `concurrency`（cancel-in-progress: true） | push(on:push)＋手動trigger の二重起動を新しい方に一本化＝失敗run・誤アラートを根絶 |
| kinsho-v1 免責 / 10ボタンナビ / リンク切れ / SYNC_FILES登録 | `check_site_consistency.py`（`mw check`／土曜 `site-qa-lint`） | 不変条件を push 前に検査・exit 1 |
| 研究日誌の数値捏造防止 | `signal_lab_verify.py`（固定オラクル・編集禁止） | claims.json を signals-log から独立再計算して突合 |
| 更新履歴の整列・最新5件 | `generate_market_news.py` の `_history_items` | 手で削らない（日付降順・自動整列） |
| **発注前ルールの遵守を数値監視** 🆕 | `_trade_discipline_check.py`（`mw discipline`・週次 /loop） | 指標持ち越し/指数重ね張り/SL未設定/損切りずらし/JP✕を **EVダメージ順に可視化** |
| **相関ポジ合算リスク2%・実スリッページ** 🆕7/5 | 同上 R6/SLP（フォーム任意3欄=口座残高/リスク額/予定価格の入力分から自動判定） | 同テーマ×同方向の同時保有を横断検出し合算%で2%判定（金銀・FXも対象＝指数限定R3の一般化）。前向き検証の群割当は登録時5ルール固定＝R6は参考枠 |
| **公開日のUTC日付ミス防止** 🆕7/6 | `signal_lab_verify.py` の `date_check()`（再監査は `SIGNAL_LAB_SKIP_DATE_CHECK=1`） | datePublished/公開表記≠JST今日なら**赤=公開ブロック**（#031が7/5付けで公開された事故の再発防止） |
| **似スラッグの重複記事防止** 🆕7/6 | `check_guide_draft.py` 検査7 スラッグ重複検査（トークン集合の同一/包含） | ⑫⑬型の「語順違い/部分一致スラッグの同一主題」自動公開を**RED=人間エスカレ**（82記事総当たりで誤検知ゼロ） |
| **研究台帳の数値転記ミス・改竄・肥大化防止** 🆕7/7 | `_doctrine_check.py`（`mw evolve`・固定オラクル扱い） | DOCTRINEのアンカー21件を出典JSON/mdと突合＋事前登録簿SHA256＋サイズ予算＋§1構造検査で**error停止** |
| **非公開研究ファイルの公開リポ流出防止** 🆕7/7 | `check_site_consistency.py`＝SYNC_FORBIDDEN追加＋**`_`プレフィックス=ローカル専用規約** | DOCTRINE/queue/`_jp_*`等が SYNC_FILES に混入したら**error停止**（REDテスト7ケース済） |
| **公開記事への下書き残骸混入防止** 🆕7/7 | `signal_lab_verify.py` date_check の残骸検査 | 「下書き中」が本文に残っていたら**赤=公開ブロック**（#032実例の再発防止） |
| **休場中の発火を勝率に含めない** 🆕7/11 | エンジン=`generate_technical_alerts.py`週末閉場ガード（土07:00〜月06:00 JST・BTC除外・発火スキップ）＋集計=`is_weekend_closed_fire`（track-record/週次/月次の3本に同一定義複製） | 塩漬けデータ発火（実測214件・勝率33% vs 全体41.6%＝週明けギャップでSL直撃の測定アーティファクト）を源流と集計の両方で遮断。生ログは不変・ページに除外注記あり |
| **ローカル公開の日付事故防止** 🆕7/22 | `publish_article.py` の `check_date_gate`（免除は `--allow-backdate`・テスト=`_test_publish_date_gate.py` 5件） | 公開日≠JST今日なら **🚫 exit 1 で公開停止**（7/15事故の恒久対策・signal-lab date_check と同型） |
| **自動公開レーンの「静かな停止」検知** 🆕7/26 | `check_automation_health.py` §⑤（`automation-health.yml` 毎朝09:30 JST・テスト=`_test_topic_queue.py` 12件） | autodraft の未公開 topic が5件未満で **Issue**。①②は「走ったか」しか見ないのでキュー枯渇による仕様どおりの停止を捕まえられなかった（7/20〜24 に5日連続スキップを誰も検知できなかった実例）|
| **事前登録の「空欄のまま登録済み」防止** 🆕7/26 | `_doctrine_check.py` の `REQUIRED_Q_FIELDS`＋`_q_field_gaps`（回帰テスト=**`_test_doctrine_registry.py` 13件**・実キュー31件でE2E確認） | 新Qは 登録日/ルール素案/検証設計/**対照**/主要評価指標/合格基準/**検出力** が埋まるまで **error＝登録簿に載せない**。SHA256は登録"後"の改竄しか見ておらず、テンプレのまま登録される穴があった。既存Qには遡及しない |
| sitemap 全記事網羅 | `generate_market_news.py` の `build_sitemap_xml` | 全 guide を自動収集・手動編集不要 |

🆕＝2026-06-20 追加（B＝カバレッジ番人 ／ C＝sync staleness ガード）。新ルールはこの表に1行＋チェック1個で増やす。

---

## ⚠️ 絶対遵守（事故防止）

- **SYNC禁忌**（ローカルから絶対 push しない＝routine/cron/generate が GitHub 側で生成）。**正は CLAUDE.md の SYNC禁忌リスト**。代表例：
  6コアHTML（index/calendar/charts/vix/market-health/hot-assets）／`signals-log.json`／`technical-alerts-history*.json`／`track-record.html`／political系／youtube系／`fundamental-context.json`／`weekly-levels.json`／`weekly-zone-plan.md`／`sitemap.xml`／`weekly-strategy-context.json`／`indicator-result.json`／`signal-lab-tracker.json`／`signals-log-backtest.json`／`article-ideas.md`／`daily-preview.md`／`political-digest.md`／`compliance-scan.md`／`site-qa-report.md`／`panic-scan.md`／`drafts/draft-*`・`drafts/news/*`・`drafts/sns/*`
  → `mw check`（`check_site_consistency.py`）が SYNC_FILES への誤混入を、sync の staleガードが「古いローカルでの上書き」を、それぞれ自動で止める。
- **SYNC対象（OK）**：`*.py`（※`sync_to_github.py` はローカル専用＝GitHub側は616Bスタブ。**`mw.py` はSYNC対象**＝7/7訂正。`_`プレフィックスのファイルは全てローカル専用＝SYNC禁止をコードで強制済）／`.github/workflows/*.yml`／個別 `guide-*.html`／`guides.html`／`robots.txt`／`my-trades.json`／`memory/*.md`／各 docs。
- 記事追加は **`python mw.py publish ...` → sync → workflow → ライブ確認**。公開前に compliance-reviewer(Opus)監査・教育トーン・特定銘柄の買い推奨は書かない・kinsho-v1 免責・10ボタンナビ。手動時も `mw check` で push 前点検。
- ネット不調時は無限リトライせず、ブラウザで手動 trigger を依頼（最大3〜5回）。

---

## 📌 アクティブな宿題

### 🔜 次セッションの入口（2026-07-26 14:56 更新）

> **在flight（未完了で手が止まっているもの）はゼロ。** 7/26 に #2〜#5 を着地させ、残るのはオーナー作業2件のみ。
> 以下は「次にやると良いこと」であって、途中で放置されているものではない。

| # | 次の一手 | 状態 |
|---|---|---|
| 1 | Q29 → **2026-09頃に1回だけ再実行**（有効日282日到達時・設計は変更しない） | 7/26 実行＝🚫検定不能。事前コミット済み |
| 2 | **フォーム3欄（口座残高/リスク額/予定価格）＝1件目の取引で入力**。無いと2%ルール/相関合算/実スリッページが測れず**チェックポイントが計算できない** | **オーナー作業・優先度↑**（取引再開が近いため） |
| 3 | 規律の前向き検証が **N=0/30 の休眠アーム**（取引が6/4以降停止中） | 取引再開で自動的に動き出す |
| — | 7/26 完了＝watch棚卸し10件／charts.html「50年」（誤検知）／topicキュー15件補充＋番人／REVIEW.md #049 補正 | ✅ |

**セッション冒頭は `python mw.py evolve`**（DOCTRINE突合＋仮説キュー＋トラッカー鮮度＋レジーム状態）。

---


- **⓪-🧱建玉・出口・損切り規約の連続検証 Q38/Q39/Q40/Q41（7/27・オーナー提案4本を全部回した）＋Q35の3件をトラッカー登録**
  - **Q38 分割エントリー（打診1/3＋旧SL位置で2/3）＝❌棄却**: 期待値は**変わらない**（グロス −0.0120 vs ベースライン −0.0130）。内訳が全て＝勝ち42.3%で利益が1/3に減り(−0.889)、下落継続50.8%で損失が減り(+0.338)、逆転6.9%で大勝ち(+2.962)＝**3効果がほぼ完全に相殺**。逆転率12.0%は事前算術の損益分岐22%を下回るが、見落としていた「下落継続でも−1.000R→−0.662R」が補った。🔑**ただしリスク圧縮は本物**＝標準偏差1.153→**0.760(−34%)**／最大DD 59.3R→**44.9R(−24%)**／最悪1トレード −1.000→**−0.667**／勝率42.3→49.4%。⚠️**Sharpeは悪化**(−0.0113→−0.0158)＝**期待値が負のとき分散を下げても負け方が安定するだけ**。→ **価値の実現形は「正のエッジに適用して同じDD許容で約1.5倍サイズ」の1つだけ**。
  - **Q39 R:R≥2選別＋分割＋「追加できたらEで利確」＝🚫検定不能 → Q40で決着**: オーナー仮説「目標を近づければ到達率が上がる」は**実測で確認**（Q38の12.0%→**23.7%**）。だが追加分岐は**0.833R賭けて0.667R取る**＝損益分岐55.5%。**1時間足では同一バー順序の上下界(32.2〜63.5%)が55.5%を跨ぎ判定不能**→🚫。
  - **Q40 15分足で決着＝❌棄却**: 15分足（`_all15m_prices.json`・2026-05-01〜07-27＝母集団100%被覆）で上下界が **36.3%〜52.7%＝丸ごと損益分岐の下**→**判定可能になり答えはNo**。どの順序仮定でも届かない。
  - **Q41 損切り規約（触れたら/終値/ヒゲ許容）＝❌棄却・現行touchが最良**: オーナー指摘「0.1pipでも切るのは硬すぎ、下ヒゲは戻りのサイン」を検証。全1,954件で **close −0.0299／W0.3 −0.0319／W0.5 −0.0299／W0.7 −0.0293＝全部現行より悪い**。**⚠️裾が壊れる**＝最悪 **−1.00R→−8.52R**、`<−1.5R` 0.0%→**5.0〜5.8%**。機序＝**逆指値は損失を構造的に1Rで打ち切るが、終値待ちはその保証を捨てる**。**ヒゲ許容はWをどう動かしてもほぼ無変化＝ヒゲの長さは回復をほとんど予測しない**。
  - 🔑 **Q41の最大の価値＝Q40の副産物（終値基準が+0.20R良い）を覆したこと**。あれはRR≥2の648件・別の建玉構造での**6アーム内部比較＝探索**で、登録時に「合否に使えない・全シグナルで別Q検証が必要」と明記した手順どおり検証したら**符号が逆**。**探索の出目を採用しない規律が誤採用を1件止めた実例**。
  - ⚠️ オーナー提案（ピンバー/ダマシ）が否定されたのは「**既存ポジションの損切りを見送る**」用法のみ。「**ピンバーを新規エントリーのシグナルにする**」は別物＝及川手法①③でありエンジンへの検出実装が先（Q10と同じ壁）。
  - **✅ Q35の新規3件を前向きトラッカーに登録**（`signal_lab_tracker.py` の SEED＝12件に。**トラッカーJSONはSYNC禁忌でクラウド管理なのでSEED経由が正しい経路**）＝`bb_lower_1d_gate`(BB下限タッチ日足・回避)／`jpyfx_rsimid_gate`(JPYクロス×RSI中立帯・回避)／`long_up_above_edge`(ロング×上昇×MA両線の上)。**registered_at=2026-07-27＝遡及しない**。⚠️**`signal_lab_tracker.py` は SYNC入りなので、クラウドに反映するには push が必要**（本日は sync 未実施）。
  - 成果物＝`_q38_scale_in.py`／`_q39_rr_scale.py`／`_q40_granularity.py`／`_q41_stop_rule.py`／各summary.json／`_all15m_prices.json`（19銘柄15分足7.7MB）。**再生エンジン `_q36_replay.py`（自己点検99.8%）が全部の土台**＝今後の出口・建玉研究で再利用可。

- **⓪-🚪Q36 出口ルールは固定ATRを超えない＝❌棄却（7/27・オーナー問い「入り口でなく出口では」への回答）**
  - **経緯**＝オーナー問い「テクニカルは存在する以上意味があるはず。勝っている人はどう使っているのか。**入り口でなく出口戦略として有効活用するしかないのでは**」→ 実測で盲点と判明＝エンジンは全シグナル一律の固定値（SL=ATR×1.5/TP1=ATR×2.0）で、**出口を変えた検証がゼロ**だった。
  - **前提の実測**＝MFE/MAEをR換算すると **負けの46.2%が一度は+0.5R以上の含み益**（28.3%が+0.75R・14.3%が+1.0R・TP1到達は1.9%）＝**出口に余地は実在する**。ただし MFE/MAE は最大値のみで**順序が無い**（勝ちの40.9%も0.5R以上逆行）＝記録だけでは可否判定不能 → **バー再生**へ。
  - **🔑 再生エンジンの自己点検を関門にした**＝19銘柄の1時間足(730日・`_all1h_prices.json`)で再生し、**記録済み勝敗との一致率 99.8%（1944/1948）**。不一致4件は全て「同一バーがSLとTPの両方を含む」既知の曖昧ケース。**再現できない再生で出口を語らない**という順序を守った。
  - **結果＝18ルール中 合格0本**（ベースライン平均R −0.0130）。最良は 時間切れ12h **+0.0086・q=0.86＝非有意**。**逆に「利益を守る」系が有意に悪化**＝建値ストップ@0.5R **−0.1016 CI[−0.144,−0.059] q<0.0001**／トレーリング1.0ATR −0.0944／同1.5ATR −0.0902／建値@0.75R −0.0833／建値@1.0R −0.0650／トレーリング2.0ATR −0.0574／部分利確50%@1.0R −0.0332。**7本すべて train/holdout 両方で下限マイナス＝頑健**。コスト控除すると約定回数の多いこれらはさらに不利＝**netでも結論不変**。
  - **機序**＝勝ちの40.9%も途中で0.5R以上逆行するので、建値ストップは負けを救う以上に**勝ちを刈る**。算術も 勝ち1本を刈ると−1.333R／負け1本を救って+1.0R＝割に合わない。
  - **⚠️ 最大の限界（次段の設計に直結）**＝**ベースライン自体が平均R−0.0130＝ほぼゼロ期待値**。**エッジの無いエントリーは出口では作れない**（分散が減るだけ）という構造が効いている可能性が高く、本Qはそれを排除していない。→ **自然な次段＝エントリー側にエッジがある部分集合（§1昇格エッジ＝押し目反転買い×上昇トレンド等）に限定して同じ18ルールを回す。宣言グリッド外なので別Q番号で登録すること。**
  - 成果物＝`_q36_fetch.py`／**`_q36_replay.py`（再生エンジン・自己点検99.8%＝他の出口研究で再利用可）**／`_q36_exit_sweep.py`／`_all1h_prices.json`（19銘柄1h・23.5MB）／`_q36_summary.json`。全文は `hypothesis_queue_archive.md` の 2026-07-27 退避節。

- **⓪-⚪B2信頼度スコアは「反転」ではなく「無反応」だった＋⚠️ローカル signals-log を読んだ私の誤診を訂正（7/27）**
  - **経緯**＝Q35で「記録済みなのに未検証の次元」を洗い出し、`confidence` が CLAUDE.md に「**HIGHのみ参照**」と運用ルール化されているのに検証形跡ゼロと判明→監査した。
  - **⚠️ 最初の測定は誤り（記録として残す）**＝ローカル `signals-log.json` を読んだが、これは **Actions が GitHub側で生成する SYNC禁忌ファイル**で、ローカルの写しは **2026-06-18 で止まっていた**（1,028件/5-20〜6-18＝**真の2,720件の38%**）。その1か月窓では「HIGH −0.263 CI[−0.422,−0.105]・順張り−0.121・精鋭−0.222」と**劇的な反転**が出たが、**フルデータで全部消えた**。引き継ぎにも auto-memory にも「真の状態はGitHub／ライブを見る」と書いてあるのに違反した。→ 監査スクリプトの既定を**GitHub raw取得**に変更済（`--log` を渡した時だけローカル）。
  - **正しい結果（GitHub raw・決済済み1,948件・週末閉場除外後・2026-05-20〜07-27）＝スコアは discriminate していない**: **HIGH N=250 42.8% avgR−0.001 CI[−0.153,+0.150]／MID N=757 41.1% −0.041／LOW N=903 43.2% +0.008**＝損益分岐43%に対し3群とも41〜43%で**横一線・CIは全部0を跨ぐ**。score別も単調性なし。**つまり有害ではなく無益**。「HIGHのみ参照」は損をさせてはいないが、仕事もしていない。
  - **要因別で CIが0を跨がないのは2つだけ**＝**`FX 強弱順張り` N=332 34.6% −0.192 CI[−0.349,−0.035]**（明確にマイナス）／`反転検知あり` N=413 38.5% −0.102 CI[−0.199,−0.004]（ぎりぎり）。**トレンド順張り(+0.004)・逆張り(−0.040)・ティア精鋭(+0.017)・回避(−0.024)は全部ほぼゼロ**＝当初の「符号が逆」説は成立しない。
  - **🔑 ただしコードが依頼した検証は未実行のままだった点は有効**（`generate_technical_alerts.py` L2340）＝ティア補正の根拠「過去304件で avoid−0.248<neutral−0.093<good+0.364<**elite+0.758** と完全単調」＋「⚠️前向き検証段階…単調を保つかライブで検証できる」。フルデータ実測＝**精鋭+0.017 / 回避−0.024＝単調性は無く、elite+0.758 は影も形もない**（実装根拠は再現しない）。**加点の正当性は失われているが、実害も無い**（スコア自体が無反応なので）。
  - **📌 オーナー判断（急がない）**＝①ティア補正と `_build_confidence_block` のHIGH/LOW文言は、根拠が再現しない以上いずれ整理したい ②`CLAUDE.md` の「HIGHのみ参照」は実態に合わないので記述修正。**配信可否は `filter_send_email` が独立制御なので歪んでいない**＝緊急性は無い。エンジンは触っていない。
  - **再現＝`python research/_confidence_audit.py`**（既定でGitHub rawを取得・label/score/tier/要因の4表。統計は `signal_lab_sweep.cluster_stats`、週末除外は `is_weekend_closed_fire` を import＝どちらも二重実装なし）。
  - **⚠️ 持ち帰り（他の分析にも効く）**＝**ローカルの `signals-log.json` で分析してはいけない**。同様に `technical-alerts-history*.json`／`signal-lab-tracker.json` 等のSYNC禁忌ファイルも**ローカル写しは古い**。Q21/Q25等の過去分析がローカル読みだった場合は母集団が違う可能性があり、要確認。

- **⓪-🎯Q34 レジーム条件付きスイープ＝❌棄却（7/27・オーナー問い「どの場面でどう使うか」への20年での回答・DOCTRINE §0-11 に結晶化済み）**
  - **オーナー方針（7/27）**＝「素の指標がそのまま効かないのは当たり前。**どの環境で・どういう時に・どう使うか**を追求せよ」。→ 実測でボトルネックを特定＝環境系2次元（`environment` A〜D／`risk_regime`）は**20年BT 43,928件に1件も記録が無く**、ライブ決済済み838件でしか測れていなかった（sweepのコードL147の注記が事実と確認）。一方 Q27 の凍結レジームオラクルは 2004-08〜2026-07 の5,412日ぶんあり、日付結合で**43倍のN**が得られると分かった。
  - **実装**＝固定オラクルに新次元 `regime4` 追加（**既存 `regime`＝ライブRISK_ON系とは別キー**＝語彙が違うものを同キーに入れると既存仮説の母集団が汚染される。Q23と同じ理由）。オラクルはレコードのフィールドを読むだけ＝**ローカル専用ファイルに依存させない**（クラウドroutineは research/ を読めず挙動が割れるため）。後付けは `_regime4_annotate.py`。**先読み防止＝発火日より厳密に前の最新確定値**（オラクルは当日終値で確定・翌営業日から有効）。テスト `_test_regime4_dim.py` **16件PASS**＋既存 news 10件/Q23 9件も再実行してPASS。
  - **結果＝train 141セル評価 → FDR通過8本 → holdout生存0本**。⚠️8本は独立でなく**7本が `UP_HIGH` の入れ子＝実質1発見**で、**全て符号反転**（UP_HIGH単独 +0.115→**−0.035**・holdoutのNはtrainの5倍＝「消えた」でなく「反転が精度よく測れた」）。さらに**4状態すべてが期間で符号反転**（UP_LOW −0.004→+0.026／DOWN_LOW −0.073→+0.084／DOWN_HIGH −0.041→+0.000）＝**守りの読み筋も無い**。
  - **⚠️ これは設計の失敗ではない**＝対照は適格（どのレジームかは発火前に市場データだけで決まり勝敗と無関係＝Q33で追加した検査を通過）・検出力も潤沢（holdout>train）。**仮説そのものへの答え。**
  - **🔑 結晶化（DOCTRINE §0-11）＝条件付けは「構造」には効き「相場つき」には効かない**。①構造条件（銘柄群/方向/シグナル型/MA位置/RSI帯）は 20年 train→holdout で**11本が同方向再現**（`_state_sweep_bt.json`＝7/20の既存sweep。例: `rsi=買われすぎ×trend=上昇` +0.176→+0.151、`macd_golden×ma=両線の下` +0.140→+0.167、`other_fx×逆張り` −0.176→−0.184＝回避）。②時変の環境条件は再現しない（Q34）。→ **「今どういう相場か」でルールを切り替える設計はしない。切るなら「何を・どちら向きに・どの型で」の構造側で切る。**
  - 📌 **Q27（棄却棚のレジーム条件付き再検証）の期待値が大きく下がった**＝レジーム条件付けで蘇る見込みが薄いと実証。着手条件の再評価が要る。
  - 📌 **`environment`（環境警戒A〜D）の20年復元は保留**＝VIX履歴・指標カレンダー・危機キーワードを揃え直す別プロジェクト規模で、レジームと違い凍結オラクルが無い。Q34が全滅した以上**優先度は下がった**（オーナー「お任せします」で保留を選択）。
  - ⚠️ **DOCTRINE.md が 26.3KB＝error(28KB)まで残り1.7KB＝知見 約13行ぶん**。次に結晶化する前に §3 のスタブ化/退避が要る。
  - **✅事後の頑健性確認（7/27・Q35で発見した交絡を潰した）**＝BT3本を束ねると **train(〜2021)は100%が日足・holdout(2021〜)は1h49%/4h35%/1d16%** で、**splitが時間足と交絡している**（fx/4hのBTは2023年からしか無いため）。「符号反転はレジームでなく時間足構成の変化では?」を検証→**tf=1dだけに限定しても符号反転は4/4のまま**（UP_HIGH +0.115→−0.062／UP_LOW −0.004→+0.050／DOWN_LOW −0.073→+0.181／DOWN_HIGH −0.041→+0.004）＝**§0-11 の結論は交絡では説明できない**。
  - ⚠️ **Q34の既知の不備2点（結論は変わらないが記録）**＝①`_q34_regime_sweep.py` は**週末閉場発火を除外していない**（Q35で標準化した処理より前に書いたため。該当は全体の13%＝5,333件）②`signals-log-backtest-fx.json` は他2本より検出器が3種少なく（`support_bounce`/`double_bottom`/`double_top` 欠落）**6/16で止まっている**（他2本は7/19再生成）。→ **BT3本の再生成タイミングを揃えるのが宿題**。
  - 成果物: `research/_regime4_annotate.py`／`_q34_regime_sweep.py`／`_q34_summary.json`／オラクルの `regime4` 次元（**他の仮説でも再利用可**）。

- **⓪-📐Q33 週足ピボット＝🚫検定不能でクローズ（7/27・全文は `hypothesis_queue_archive.md` の 2026-07-27 退避節）**
  - **手順は正しく回った**＝①手元15分足は Yahoo 上限60日＝**11.4週**しか無く週足ピボットには使えないと実測 →②1時間足730日を新規取得（9ペア×**145週**＝`_fx1h_prices.json`）→③**着手前に検出力を実測**（145ブロック・厳密マッチ2,634組）→④事前登録 Q33 →⑤検定。
  - **④の途中で自分の設計ミスを1件、結果を見る前に発見・是正**＝プラセボを `+0.10×レンジ` の片側にしか置いておらず「ピボットか否か」でなく「その水準より上か」を測る方向交絡になっていた（±両側へ修正）。
  - **⑤の結果＝登録した合格基準を字面上クリアした（train下限+0.0032／holdout下限+0.0139・水準別もR1/R2/S1/RMがFDR有意）。だが事後診断で棄却した。**
  - **理由＝対照が結果の下流にあった**。プラセボが比較に入る条件は「タッチされたか」だが、それは測りたい結果（価格がどこまで行ったか）そのもの。観測が自動的に2群に選別される＝**非対称群(N=696・当たったのは中心寄りプラセボばかり=上方向536/552が`−`)＝価格が実水準の直後で止まった週**（差+0.0744）／**対称群(N=1859)＝突き抜けた週＝ピボットが効かなかった週**（差-0.0027・CI[-0.0103,+0.0053]・p=0.51・train/holdoutで符号が割れる）。加重平均 `(696×0.0744+1859×(-0.0027))/2555 = +0.0183` が主効果と**完全一致**＝効果の出所はサブセット選択のみ。**距離交絡は別途潰した**（プラセボ内 rej~dist の傾き-0.00092＝距離差+0.0066が説明できるのは主効果の**0.0%**）。
  - **⚠️ ここが今日いちばん大事**: この罠は**検出力・train/holdout分割・BH-FDRのどれでも捕まらない**。合格基準を満たしたまま通過する。→ キュー冒頭のテンプレ節に 🔒**「対照が結果の下流にないか」を登録前に1行で説明する**を追加した。**判定法＝「対照が観測に入るかどうかは、結果を知らなくても決まるか？」**。同日クロスセクションがこの罠に強いのは対照の存在が結果と無関係に決まるため。
  - **⚠️ 及川手法そのものは検定していない**（登録時に限界(a)として開示済み）。一次資料 `research/oikawa_fxism_methods.md`＝**5分足執行・決済20分以内**。**20分の反応は1時間足では原理的に見えない**＝切り番検証の「1d粒度でザラ場反発は不可視」と同型の盲点。1日足20年（16,221決済）を流用する案はこの盲点を相続するので不採用にした。**本当に及川手法を検定するには分足の長期データ調達（HistData等）が要る＝オーナー判断待ち**。
  - **Q34の設計方針（未登録・着手前チェックからやり直す）**＝タッチを条件にしない分布検定。例＝週の極値（高値/安値）が実ピボット近傍に集まるかをプラセボ水準群と比べる（対照の存在が結果に依存しない）。
  - 成果物: `research/_pivot_wk_fetch.py`／`_pivot_wk_common.py`（水準定義・タッチ判定・有効週を1本化＝検出力と検定が同じ関数を呼ぶ／`with_outcome=False` 既定で着手前チェックは結果を覗けない構造）／`_pivot_wk_power.py`／`_pivot_wk_test.py`／`_q33_summary.json`。全て `_` プレフィックス＝ローカル専用。

- **⓪-🔒コンプラ「公開しない」が実は公開だった＝SOP3本を修正（7/26 18:30・全文は auto-memory `project_marketwatch_compliance`）**
  - リポジトリは **public**（API実測 `private=False`）で `raw.githubusercontent.com` から **`drafts/` 配下が認証なしで読める**（REVIEW.md 88.6KB／PROVERB_LEDGER 8.9KB とも HTTP 200）。**サイト側の `noindex` と robots.txt は GitHub には効かない**。SOPには「エスカレした本文を保存してpush」と明記されていた＝黒判定が出た日に本文が公開される設計だった。
  - **実害はゼロ**＝🚩9件（REVIEW 4／PROVERB 2／NEWS 3）を分類したら**全部が手続き・技術理由**（EXIT=1／数値不整合／既存記事との重複）で**法務系の黒は1件も無い**。下書きHTMLは検査40件すべて noindex 付き。
  - **対応**＝SYNC入りSOP3本（`PROVERB_GUIDE.md`／`NEWS_DAILY_GUIDE.md`／`AUTOPUBLISH_GUIDE.md`）に「🔴黒の本文はコミットしない・台帳には理由の要約だけ」を追記。⚠️**autodraftレーンは前日05:30の生成時点で既にpublic**＝削除しても**git履歴に残る＝完全な取り消し不可**。恒久策（非公開の受け皿）は未実装。⚠️**GitHub Issue は使えない**（public repoのIssueは誰でも読める）。

- **⓪-⏱デイトレ開始の準備（7/26 21:00・オーナーが近く取引再開・中心はデイトレ）**
  - **記録経路は正常・設定済みだった**：`CSV から 0 行` を見て「沈黙の故障」と誤診したが誤り。7/05のE2Eテスト後にTEST行を削除し以後6/04〜未取引＝**空が正常**。Secret設定済・取得成功・`import_my_trades.py` は1日6回稼働・フォームは13項目＋新3欄で作成済。**始めれば流れる**。
  - `import_my_trades.py` に2点追加＝①0行のときの**中立な状況説明**（警告にすると毎runの偽陽性になるので煽らない）②行が来たとき**3欄のどれが未入力かを名指し**（ここが実効）。
  - **`MY_TRADING_RULES.md` に「⏱デイトレ開始にあたっての事前登録」を追加**＝実コスト控除後 net R／**N=30ごと**に判定（`PROMOTE_MIN_N_HOLDOUT=30` と同刻み）／**95%CI**で 下限>0=機能・0跨ぎ=**サイズ上げない**・上限<0=**手を止めて設計に戻る**／基準を緩めたら日付つきで別ルールとして再登録。根拠＝15分足実測(12,143トレード)で**コスト中央値0.118R/回・全セッションnetマイナス**＝デイトレはコストが線形に効く。
  - ⚠️ **`MY_TRADING_RULES.md` は 2026-06-19 にオーナー判断でSYNC除外＝ローカル専用**。public repo と分かった今この判断は正しいが、**`weekly-trade-review` routine は6月時点の旧版を読む**＝今日の追加はレビューから見えない。
  - **残るオーナー操作＝1件目の取引でフォームの「口座残高/リスク額/予定価格」を入力**。無いと2%ルール・相関合算・実スリッページが測れず**チェックポイントのnet Rが計算できない**。

- **⓪-📐FXism_Pivot-WK の数式を確定（7/26 22:00・移植自体は不要になった）**
  - オーナーがMT4のマルチタイムフレーム表示をMT5化したいと相談→調査＝画面の見た目は **FXism/GeniusChart の市販インジ（.ex4のみ・Indicators配下は .ex4 99件 vs .mq4 21件でFXism系はソース0）**＝逆コンパイルはせず**ベンダー確認を推奨→MT5版が存在し解決**。`MW_Guardian.mq4`（自作EA・25,880B・作業フォルダとMQL4\Expertsで同一）は移植可能だが**MT5のネッティング/ヘッジングで設計の意味が変わる**点が最大の論点（未着手）。
  - **副産物＝画面の7本の値だけから数式を逆算し 7/7 完全一致で確定**（逆コンパイルなし）。`PP=(H+L+C)/3・R1=2PP−L・R2=PP+(H−L)・R3=H+2(PP−L)・R4=R3+(H−L)・S1=2PP−H・RM=(R1+R2)/2`＝**クラシック・ピボット**（フィボ流/カマリラ流は外れる）。自由度3に7本が乗る＝偶然でない。→ **MT4/MT5なしで手元データから同じ線を引ける＝検証可能な題材**。切り番は❌棄却済みだが週足ピボットは「キリ番でないが実際の反発値でもない合成値・多くの人が見ている」＝事前確率はゼロでない。検算スクリプトは残していないが式は上記で足りる。`mt5/MW_PivotWeekly.mq5`（ローカル・SYNC外）は不要なら削除可。

- **⓪-🧭保留項目29件を棚卸し→「私が消せる6件」を消した（7/26 17:40・`mw check` 警告7→0）**
  - **分類が要点＝29件は4種類の混合**: ①私が今すぐ消せる ②オーナーにしかできない（フォーム入力/取引再開/ワイコフ・柴田の客観化定義/及川の一次出典） ③時間しか解決しない（前向き5本・Q29とQ32の2026-09再チェック） ④**恒久的に解決しない＝正しく諦める**（粗利益率＝J-Quantsに売上原価が存在しない／`samedate_diff_p` の戻り値変更＝14呼び出し箇所で費用>便益）。**「全部解決」を目標にすると②③④が永久に未解決として残りアラート疲れを生む**＝7/26 watch棚卸しの「ブロッカーは3種類あり同じラベルで並べない」の再確認。
  - **P1 道具が嘘をついていた2件**: (a) ナビ警告6件は**全部偽陽性**＝`_`プレフィックス5件（ローカル専用）＋`apply_books_nav_scripts.py`（ナビ"断片"を注入するツールで自分が10ボタンを持つ理由が無い）。`check_site_consistency.py` の §4 に除外を追加（実測で失う検査は `_gmn_remote.py`/`_guides_remote.html` のリモート写しのみ）。(b) `_doctrine_check.py` の状態カウントが**書式テンプレの凡例行を仮説1件と数えていた**（⏳4件と出るのに次候補が3件しか並ばない矛盾）→ `### Q` ブロック内に限定して修正＝**⏳3件**に是正。
  - **P2**: `guide-signal-anatomy.html` に back-to-top 注入（`--dry-run` で対象1件を確認後に適用）→ **`mw check` エラー0・警告0**。
  - **P3 DOCTRINE.md 25.0KB は予算を上げも下げもしない**: 実測＝194行に出典47・アンカー44＝キューと同じ密な知識で削減余地なし。ただし**28KBにハードなerrorがある**点がキュー（天井なし＝鳴りっぱなし）と違う。**黄信号は残し文言をゲージ化**＝「error(28KB)まで残り3.0KB＝知見 約23行ぶん」。
  - **P4**: `min_ctl` 緩めの実行時検出は作らず、**テンプレの「検出力」欄に `min_ctl` の明記を義務化**した。凍結された欄に書いてあれば「結果を見た後に緩めた」ことが一目で分かる＝**ハッシュで守れるぶん実行時チェックより強い**。
  - **⚠️P5 閾値が2箇所にあった（発見・修正）**: `mw declutter` は `declutter_audit.py` に**別の閾値表**を持っていて、`_doctrine_check.py` だけ 14→36 に直した結果**同じ警告が別経路から鳴り続けていた**。→ `DOC_LIMITS_KB` を `_doctrine_check` からの import に変更（`_`プレフィックスでクラウドに無いため fallback だけ数値を持つ）。**検査を直すときは「同じ数字が他にも書かれていないか」を必ず確認する**。declutter 6→5件。

- **⓪-⏭自社株買い＝事前チェックで着手見送り（7/26・Q番号は付けていない）**
  - データは作れた（`research/_buyback_events_raw.csv` 85,505開示・実行5,465件）が、**正しい対照＝同日開示の「買い実行なし」銘柄**を使うと成立が `min_ctl=20/fwd10` の1点・**余裕ゼロの8/8ブロック**＝Q29と同じ「走らせても検定不能」。**だから登録せず走らせなかった**＝「先に検出力を確かめる」規律が初めて**着手前に止める**形で効いた。
  - **✅見積もりズレの根治**＝`research/_q32_common.py` に信号定義と `valid_days()` を1本だけ置き、**検出力と検定が同じ関数を呼ぶ**構造にした（Q29とQ31で2回続けたズレは別実装が原因）。
  - **着手条件**＝Q29の再実行時期（有効日282日・目安2026-09）と同じタイミングで再チェック。対照を市場全体に緩めると決算開示の効果と混ざる＝**別の弱い仮説**なので流用しない。

- **⓪-🧹declutter＋キュー予算の決着（7/26 16:05→17:10・オーナー承認済み）**
  - Q30/Q31 を `hypothesis_queue_archive.md` へ退避（登録簿に `archived` 追記）。**キュー 38.3→27.0KB**。ハッシュ違反ゼロ・KPI も20本/生存8/棄却12のまま維持。
  - **🔍 推奨②（事前登録と設計分析の分離）は前提が崩れたので不採用**。9ブロックを実測（20.8KB／ヘッダ8.6KB）し、コミットメント欄以外を機械分類すると「設計分析60%・Q29だけで6.08KB」と出たが、**Q29を実読すると中身は事前登録の証拠だった**＝結果未参照で実測した制約5点・変種の全列挙（＝分母）・不採用にした代案・既知の限界の開示。**外出しするとハッシュが守るべき当のものが外れる**。Qブロックが大きいのは冗長だからでなく正直だから。
  - **→ 採った策＝①予算を実態に合わせる（`QUEUE_WARN_KB` 14→36・固定オラクルの緩和なので理由をここに記録）**。14 は「1件1.3KB」時代の設定で恒久超過＝鳴りっぱなしの警告は他の警告も殺す。36KB＝新Q約5本ぶん＝1作業サイクルで到達＝**行動可能な警告**（鳴ったら閉じたQの退避で応じる）。
  - **②の生きている部分だけ実装**＝`- 設計メモ:` を `_MUT`（ハッシュ対象外）に新設し、実装手順/テスト件数など**約束でない記述**の置き場にした（長ければ `research/_qNN_design.md` へ）。⚠️既存が使う `- 実装メモ:` は**追加してはいけない**（Q29のハッシュが変わる）。テンプレに🔒「証拠4種はブロック内に残す・長いからと削らない」を明記。
  - **オーナー判断が要る**: このままだと番人が**恒久的に警告を出し続ける**＝アラート疲れで他の警告も見なくなる。選択肢＝①閾値を上げる ②**事前登録（ルール/基準/凍結パラメータ＝ハッシュ固定の約束）と、設計分析の記述（検出力の表・罠のメモ）を分離**し、後者は `research/_qNN_design.md` へ出す ③現状維持。**②を推奨**（チェックの意味を保ったまま詳細も残せる・今後の登録から適用）。**閾値の変更は固定オラクル側なので私からは触っていない。**

- **⓪-❌Q31 EAP 決算公表前ドリフト＝棄却（7/26・詳細は `hypothesis_queue_archive.md` と DOCTRINE §3）**: **リターン差は再現**（K=5/10/20とも両期間同符号・FDR後 q 0.0003〜0.0087・+0.206%→+0.924%の用量反応）**だが blowup で不合格**＝train はシグナル側が安全なのに holdout で逆転（K10: 0.4%→3.8% vs 対照1.1%→1.8%）。**上乗せリターンはテールリスク増を伴う**＝§2（防御）側の知見として残す。**基準は緩めなかった。**
  - 先読み防止＝前年同期実績+364日で予想発表日を作る（実績日は信号に使わない・±5日以内89.1%）。成果物＝`research/_q31_test.py` / `_jp_trading_calendar.json`。
- **⓪-❌Q30 COT逆張り＝棄却（7/26・詳細は `hypothesis_queue_archive.md` と DOCTRINE §3）**: 合格0/4。**符号が期間で反転**（train〜2015は全て負／holdout2015〜は全て正）＝**分割が誤採用を止めた実例**。検定は成立した上での棄却。`_cot_weekly.csv`（8資産9,741週・CFTC公開APIはキー不要）は再利用可。
  - **🔒 先読み防止の型（イベント系で再利用）**: 公表遅れのある指標は「集計日+N日より後に始まる最初のバー」をエントリーにし、**実測ラグが閾値未満なら書き出しを中止**する検査をコードに内蔵する。
  - **⚠️ 事前登録簿の罠3つ**: ①**`- 補遺:`/`- 履歴:` は1行で書く**（折り返すと継続行がハッシュ対象に入り改竄判定） ②**Qブロックの挿入/削除で隣の区切り（`---`）が変わりハッシュがずれる** ③**`[src: ...]` はプロジェクト直下から解決**＝research配下なら `research/xxx.json`。
- ⚠️ **raw.githubusercontent はCDNキャッシュで数分間 古い内容を返す**（クエリ付与でも回避不可）。**書き込みの反映確認は Contents API で行う**（読み取りは raw で可）。7/26 の #049 記録補正で確認。
- **⓪-🚫Q29 PEAD第2版＝検定不能でクローズ（7/26・全文は `hypothesis_queue_archive.md` / SESSION_ARCHIVE.md）**
  - 有効日278日・train140/holdout138 → **ブロック 7/7（要8）**。設計時予測8/8を外した原因＝見積もり器が**末尾のホライズン未解決分を数えていた**（修正済・回帰テストで 278/140/138/7/7 をロック）。効果量は train+0.643%／holdout+0.681%（Q28と合わせ独立4推定すべて正）だが p が床値0.0003＝**「有意」とは言えない**。
  - **⚠️ここが今日いちばん大事な判断**：`min_ctl` を下げれば成立した（15→9/9・10→10/10）。**だが効果量を見た後の操作＝Q28と同じ罠**で母集団も閑散期側へすり替わる（30→5で増える332日は決算集中月13%のみ／既存207日は77%）。**やらなかった。**
  - **事前コミット**＝設計も基準も変えず、**有効日282日到達時に1回だけ再実行**（目安2026-09）。**毎月試すのは optional stopping なので禁止**。その1回でも8未満なら恒久クローズ。
- **⓪-✅watch 10件の棚卸し＝完了（7/26 14:10・全証拠は `research/_watch_audit_2026-07-26.md`）**
  - **結論＝「データ調達待ち」で止まっている項目は実質ゼロ**。止めていたのは調達ではなく着手判断。事前の見立て「真に調達が要るのは粗利益率・自社株買い・COTの3件」は**2件が外れ**だった。
  - **着手可4件**＝EAP／管理先物型TF／オールウェザー（月次4資産が20〜26年：^GSPC 1984-12〜・^TNX 1985-01〜・GC=F/CL=F 2000-09〜・TLT 2002-08〜・DBC 2006-03〜）／COT（CFTC公開API・**キー不要**・1986-01〜週次133列）。**近似のみ可1件**＝自社株買い（TDnet公表日は無いが `TrShFY`82.7%＋`DiscDate`100%で"実行の開示後"を観測＝別仮説）。**恒久的に不可1件**＝粗利益率（107列に売上原価/粗利が本当に無い）。
  - **⚠️ 持ち帰り**: 「調達待ち」は**主張であって観測ではない**。状態として書いた時点で誰も再確認せず固定される。ブロッカーは①恒久的に不可 ②1回の作業で外れる ③判断待ち の3種類があり、**同じラベルで並べない**（7/26夕の保留29件棚卸しでこの原則を再適用）。
  - **副次修正**: `🚫検定不能` を凡例に新設（`❌棄却` にすると生存率KPIが歪む＝`_doctrine_check.py` は ✅🛡/❌/📊 しか数えないので🚫がどこにも入らないのが正しい）。クローズ済みQが「次候補」に出続ける問題を解消。

- **⓪-✅autopublish の topicキュー補充＋枯渇の番人＝完了（7/26・全文は SESSION_ARCHIVE.md【2026-07-26 完了】節へ退避）**
  - #3は誤検知（ライブは既に150年表記／手元の `charts.html` が5/6生成の古い成果物＝**生成物のローカルコピーで判断しない**）。#4は仕様どおりの枯渇＝24/24公開済み。**#25〜#39の15件を補充**（既存199記事に機械突合しRED 0件）＋番人 `check_automation_health.py` §⑤（未公開<5でIssue・テスト12件）。
  - **⚠️ 記事系routineの指示を書くとき必読**: クラウドの routine は **`research/` 配下や `_` プレフィックスのローカル専用ファイルを読めない**＝独自実測値を本文に書けと指示すると**出典を確認できないまま数値を書く**。キュー冒頭に「独自数値は公開済みページの引用に限る」を明記済み。

<!-- 2026-07-26 declutter: 7/25完了節4件（ナビ文言150年／PEAD Q28クローズ／レジームオラクル稼働／期限確認2件）は
     SESSION_ARCHIVE.md の各【完了】節に全文あり（L1378/L1427/L1401/L1392）。以下は再利用する教訓だけを残した圧縮版。 -->

- **⓪-✅完了4件の要点だけ（7/25〜7/26・全文は SESSION_ARCHIVE.md）**
  - **ナビ文言150年化**（242箇所/223ファイル・sync 242/0）: ①一括置換のキーは **`📈 50年チャート` の完全一致**に限定（`50年` の素朴なgrepは `250年`/`1950年代` を `2150年`/`11950年代` に壊す） ②`generate_market_news.py` の更新履歴中の「50年チャートを…へ拡張」は**履歴の記述＝変更禁止** ③sync の🚫staleは `--force` を直に叩かず**リモート最新に自分の編集を乗せ直す**。ツール=`_relabel_remote_push.py`/`_relabel_api_targets.json`/`_relabel_live_state.py`
  - **⚠️環境**: `api.github.com` だけ TCP443 到達不能になる時間帯がある（`github.com`/`raw`/ライブは正常）＝**進捗実測は raw・書き込みは窓が開いた瞬間**
  - **PEAD Q28＝🚫検定不能でクローズ**（材料は全部手元にあった＝「調達待ち」は誤り。`research/_pead_events.csv` は再利用資産）。**⚠️実装の罠3つ**＝①合成データを「都合よく単純」にすると検査したいものが値として区別できない ②`samedate_diff_p` の戻り値は3-tuple `(est, p, 有効日数)`・判定は `est`（同ファイルの `block_boot_p` は float なので取り違える） ③blowup を期間跨ぎで比べない
  - **レジーム転換検知オラクル＝稼働**（4状態・`mw evolve` 表示・現在 UP_HIGH・テスト21件）。**凍結パラメータの変更は修正でなく新Q番号での再登録**（`_doctrine_check.py` が `frozen_params` を検査）。Q27第1段は着手条件待ちで未実行
  - **期限到来の確認2件＝両方クリア**（#050にチャート風図解あり＝7/24のプロンプト改定は初適用回で機能／REVIEW.mdの🚩は新規なし）
## 📎 運用メモ

- 作業フォルダ: `C:\Users\info0\OneDrive\デスクトップ\新しいフォルダー` ／ GitHub: `invest-ai-info/marketwatch-ai`(main)
- 運用は **`python mw.py <cmd>`** が単一入口（check / publish / sync / **deploy [--trigger]** / trigger <wf> / status [wf] / **issues** / **audit** / routines）。`mw routines` で全 routine ID 一覧。
- 🆕 **ループ・エンジニアリング土台（2026-06-23・決定論コマンド＝トークン0）**：②`mw issues`＝open health-check/automation-health Issue 一覧（トリアージの土台）。③`mw audit`＝guide記事の改善候補スコア化（desc短/本文短/内部リンク少/JSON-LD無）。**判断部分だけ上限付き `/loop` でモデルに渡す**設計。`/loop` レシピ＝②「mw issues→最大3件診断＋提案(自動適用しない)→STOP」／③「mw audit→最弱を1本改善→白確認→publish、最大3本/回、score≥2が尽きるかで停止」。**🔑調査結果（2026-06-23）＝audit最弱11件は全部すでに `noindex,follow` 済み**（週次振り返り/週次戦略/月次の自動生成は薄ページAdSense対策で既にインデックス除外＝`auto_weekly_review.py`:287 / `auto_weekly_strategy.py`:378 / `generate_monthly_report.py`:291）＝**AdSense薄コンテンツ対策は完了済み**。よって `mw audit` を **noindex対応**に改修（noindex薄ページは別枠カウント＝改善対象外）→**インデックス対象81件中 改善候補0件＝公開コンテンツは健全**と確認。底上げ不要。
- 🆕 **`mw deploy`（2026-06-23）＝自己修復デプロイ（決定論・モデル不使用＝トークン0）**：sync を ❌throttle 時に backoff して**最大5回**再試行・🚫staleは即エスカレ・成功/上限/合計15分で必ず停止→任意で workflow 起動(`--trigger`)→ライブ200検証。上限は `mw.py` の `DEPLOY_*` 定数で固定＝構造的に永久ループ不能（今日の api.github.com throttle 手動リトライを自動化）。
- 同期は `python sync_to_github.py`（＝`mw sync`）。staleガードに 🚫 されたら「先に最新を取り込む（reconcile）」か、意図的なら `--force`。workflow 手動起動は `mw trigger <wf.yml>`。**ローカルは UTF-8 強制**：`$env:PYTHONUTF8="1"`（PowerShell）。
- routine 操作: schedule スキル → `ToolSearch select:RemoteTrigger` → RemoteTrigger（list/get/update/run）。クラウド routine（`signal-lab-daily`／`news-daily-auto` 等）はこれで管理。
- ⚠️ ローカルは GitHub と未同期なことがある（OneDrive）。**真の状態は GitHub／ライブを見る**。token は `market-news-config.json`(.json)。
- ユーザー北極星：投資家全体の底上げ／サイト・SNS 年収1000万／個人投資成績 年収1億。
