# 🔖 セッション引き継ぎ（最終更新: 2026-09-25）

## 🆕 2026-09-26: 「出口の壁ラボ」＝近い壁で早めに利確／壁が無ければ伸ばす、は期待値を上げるか（オーナー指示）
- **`exit_wall_lab.py`＋`.github/workflows/exit-wall-lab.yml`**（作業ブランチ push か手動だけ）。損切りは全方式 1.5ATR 固定・利確だけ比べる。壁＝入値より上のいちばん近い水準（直近20/60本高値・山・25/75/200本線・+2σ）。方式 P0 いまの方式／P1 壁で早め／P2 伸ばす（最高値−3ATR を追う）／P3 壁で半分＋残り伸ばす／P4 場合分け／P5 期待値で選ぶ（〜2015学習→2016〜）。**偽薬**＝ランダムな入口に同じ方式を当てて相場全体の上げ下げ分を差し引く。結果 `exit-wall-lab.json`／`.md`
- **較正で見つけて本物のデータの前に直した2つ**: ①作り物の足の中の歩数が少ないと、損切りの線を飛び越えた分を無視するぶん「伸ばす」が +0.12R 有利に出る（256歩で解消）＝本物でも損切りの滑りは入っていない。②`exit_rule_backtest._mean_se` の二方向クラスタは、まとまりが少ないと幅が狭すぎる（±0.004R vs 素直な計算 ±0.028R）→ 壁ラボは V1+V2−V12・V1・V2・V12 の最大を使う。**⚠️ 出口の相性ラボ・環境の相性ラボの p値は少し楽観的**（環境の相性ラボの較正で、ボンフェローニの線を下回る割合が名目の約2.3倍だったのと同じ原因の疑い）。公開済みの出口の相性ラボの関数は変えていない
- **結果（日足19,206取引・主の10比較で基準を満たすもの0）**: P1 壁で早めは**一貫して少し悪い**（順張り −0.045R p=0.0003・前後半・4時間足とも同じ向き・偽薬も同じ）。壁を広く取ると順張りの76%は0.67R以内に何かある＝値段は壁をよく抜ける。P2 伸ばすは日足で少しプラス（順張り +0.05R／逆張り +0.12R）だが**偽薬も同じだけプラス**＝シグナルの力ではない。4時間足の順張りでは効かない。**「壁が2.67R以内に無い」場面だけ、伸ばすが日足で +0.13R（前後半とも）**＝ただし偽薬でも同じ・4時間足では逆
- 付随: 道の途中（0.67〜1.33R）に壁がある順張りは、どの出口でも最も悪いマス（−0.10R前後・偽薬/4時間足も同じ向き）＝エンジンの sr_runway「blocked」と同じ話で、出口ではなく入るかどうかの問題

## 🆕 2026-09-26: 「環境の相性ラボ」（テクニカルが効く環境・効かない環境）＋候補3つをトラッカーに登録
- **`regime_lab.py`＋`.github/workflows/regime-lab.yml`**（作業ブランチへの push か手動でだけ動く＝main の定期実行なし）。事前登録＝エンジンと同じ8入口を順張り/逆張りに分け、7つの環境（値動きの大きさ・ADX・効率比・200本線・VIX・値動きの広がり・最近効いていたか）で比較。較正＝作り物42回で主の誤検出1回あたり0.095。結果 `regime-lab.json`／`.md`
- **結果**: 日足20年で主の14比較・探索112マスとも基準を満たすもの**0**。最も近いのは順張り×ADX（弱+0.040R／強−0.037R・教科書と逆向き・差0.077Rで基準未満）。「最近効いていた」は続かない（年→翌年の相関0.02）。差がはっきりしたのは「どこで」＝**FX日足の逆張り買い −0.148R**（前後半とも負・9ペア全て負。読むための表由来＝事前登録外）。文献調査（54本）も「環境で切り替えて成績が上がる確かな証拠なし」
- **トラッカー登録（オーナー指示）**: `REGISTER_2026_09_26` に5本（順張り×ADX弱 edge／強 gate・逆張り買い×VIX高 edge／低 gate・FX×逆張り買い日足 gate）。固定オラクル `signal_lab_verify.py` に `family`/`adx_band`/`vix_band`/`asset_class` を追加（**人間の指示による正式拡張**。§④ が Claude 変更として一度赤くなる見込み）。⚠️ **検出力が足りない**（N80のMDE≈0.36R・期待効果±0.04〜0.15R・VIX≥25は5/20以降0件）＝N80で非有意でも「効果なし」と読まない。新キーはエンジンのメール照合に未対応＝昇格しても配信には反映されない
- 番人 §⑥ に「反映待ち」猶予1日（宣言は翌朝の研究日誌 routine の update で入るため、宣言した日の誤警告をなくす）

## 🚨 2026-09-25 夜: 日本株ランキングが毎日 21〜23時台にしか更新されていなかった（オーナー報告）
- **原因**: jp-rankings.yml の cron 4本（16:40〜19:10 JST）が**そろって約5時間遅れ**。9/8〜9/25 の営業日はすべて 21:21〜23:25 JST 更新（9/24 だけ手動起動で 18:21）。大引けから約6時間、トップ・hot-assets は前営業日のまま。番人 §⑫ は朝（実際は昼）に走るのでこの時間帯を見ない＝ずっと緑
- 9/25 は 21:57 に cron が当たり 21:58 に 9/25 分へ。直後の 21:59 の再描画は古い土台で走り「9/24 終値」のまま、**22:01 の workflow_run 再描画で 9/25 に直った**
- **対策（ブランチ `claude/youthful-knuth-2cy5sc`・未マージ＝オーナー確認待ち）**: ①routine の push に相乗り（`drafts/news/NEWS_LEDGER.md`＝実測17:48〜17:57／`drafts/sns/**`＝19:09〜19:13）②`build_jp_rankings.is_unsettled`＝16:00 JST 前の当日バーは書かない（起動時刻が管理外になるため）＋`tests/test_build_jp_rankings.py` ③`check_automation_health` §⑭ に「jp-rankings の push 起動が直近3日にあるか」（9/28 から見張る。マージが遅れたら日付をずらす）

## 🆕 2026-09-24〜25: 「出口の相性ラボ」と「エントリー方法の研究」シリーズを新設（PR #50・#51 マージ済み）

**オーナー方針**: 入口×出口の相性は**検証結果だけを公開**し、「この入口にはこの出口が合う／おすすめ」とは**絶対に書かない**。株・FX・コモディティは**分けて**検証する。出口・エントリーの記事と entry シリーズは**広告なし**（A8・AdSense とも）。

### できたもの
- **`exit_lab.py`＋`exit-lab.yml`**（日曜 04:43／06:17 JST）→ `exit-lab.json`／`exit-lab.md`（Actions 生成＝ローカルから push しない）。事前登録＝合成ランダムウォークで較正・Bonferroni・効果量・前半後半一致・ticker×時刻の2方向クラスタ頑健CI。「全体」と資産クラス（index/fx/commodity/crypto）は別の族で判定し、クラス側は基準を厳しくした（効果0.25R・n≥200。crypto は1銘柄なので旗を立てない）
  - 実データ初回（5c31065）: 目立つ組＝全体4／fx7／commodity1／index0。**全体4は「偶然でも平均2.2個出る」範囲内**＝記事ではそう正直に書く
- **`exit_lab_verify.py`**（固定オラクル・`GATE_FILES` 登録済み）＋ `tests/test_exit_lab_verify.py`（13件）
- **研究日誌「🚪 出口の相性の回」**＝週1（日曜）。`SIGNAL_LAB_SOP.md` に手順。signal-lab-daily の指示文には**追加④をオーナーが UI で貼付済み**（この routine は http_api 製＝`update_trigger` が効かない）
- **routine `entry-daily-auto`**（`trig_01Q9rvYpxzrYon35SYx6sFkr`・毎日 12:23 JST・sonnet-5）＝手順書 `drafts/ENTRY_GUIDE.md`（題材26件）→ `guide-entry-*.html`。`QUEUE_LANES`・`apply_series_nav.py`（key=entry）・`inject_ads.py` の `DENY_PREFIX` に登録済み。**#01 trend-following を 9/25 に公開**（main へ push できた）。全25回＋総まとめで完結したら `QUEUE_LANES` から外して routine を止める
- **研究日誌のトラッカー表**を改行して読みやすくした（宣言基準2行・前向き現在値3行。`signal_lab_tracker.py`＋`apply_tracker_plain_names.py` の relayout で既存72本も変換済み・冪等）
- **CLAUDE.md**: セッションで作った PR はテストが通れば Claude がマージしてよい（見た目・自動実行が大きく変わるものは事前に一言）

### 未公開の下書き（公開はオーナー判断）
- `drafts/draft-entry-methods.html`（エントリー方法11種の総覧・出典31件）＝**TODO(要原文確認) 6件**。うち1件は「58市場すべてプラス・うち52で有意」の **52 の出どころ**（要旨は「58すべて significant」。公開済み #01 は要旨どおり）
- `drafts/draft-exit-methods.html`＝TODO 10件

### ネットワーク（9/25）
- それまでこの環境から開ける学術サイトは **arXiv だけ**だった（sciencedirect／ssrn／doi.org／nber／aqr／federalreserve／bis は egress 遮断）。entry #01 は WebSearch に出る要旨の逐語引用で数字を確かめた（台帳に明記・数字は要旨と一致を確認済み）
- **オーナーが Default 環境の許可リストに学術系ドメインを追加**（9/25 夜）。効いたかは 9/26 の entry #02 の台帳メモ（「出版社ページが遮断」の記述が消えるか）で確かめる

### 予定している確認（新しいセッションへ移した）
- **9/26 13:30 JST**: entry #02（breakout）の公開と中身（出典リンク・株FXコモディティの節・広告なし・noindex 無し・`check_guide_draft.py`／`check_plain_japanese.py` 緑）＋ automation-health の 9/26 実行（§④ の赤は 9/24 23:52 UTC の ed1aef7 が 26h 窓を抜ければ消える見込み）。**緑なら Issue #6 を閉じてよいかオーナーに確認**（勝手に閉じない）
- **9/27 13:30 JST**: exit-lab の日曜実行／最初の「出口の相性の回」（1d `bb_lower_touch` long）が signal-lab-ledger・REVIEW.md・labnotes に出たか。手元で `exit_lab_verify.py`（`--signal-claims` 付き）と `signal_lab_verify.py` を回して緑を確かめる。良い組と悪い組の両方・「偶然」「前向き」の断り・資産クラス別・「合う／おすすめ」と読める文が無いか。＋ entry #03 ＋ トラッカー表の新しい改行

### オーナー側の宿題
- `check_site_consistency.py`（ゲートファイル）の SYNC_FORBIDDEN に `exit-lab.json`／`exit-lab.md` を足す。ローカルで sync する前に pull する

## 🎉 2026-09-22: キュー駆動レーンを全て完結させ、サイト全体の品質を一括で底上げ

**この日の方針（オーナー判断）**: 「枯渇したシリーズはリサーチして続きがもうなければ閉じる。
その分を**話題の銘柄を深掘りするシリーズ**に回す。そのほうが情報として価値がある」

### ✅ 3レーンを完結させた（QUEUE_LANES は空になった）

| レーン | 結果 | 総目次 |
|---|---|---|
| scam（投資詐欺） | **全20回**（#20 family-rules を執筆） | `guide-scam-series-index.html` |
| autodraft（心理＆リスク管理） | **全63回**（題材を出し切り） | `guide-mindset-series-index.html` |
| tse（東証のしくみ） | **全18回** | `guide-tse-series-index.html` |

- ⚠️ **tse は題材がまだ10本以上残っている**（空売り規制・大量保有報告書・インサイダー規制・
  清算決済・指数の銘柄入替など）。「書くことが無いから」ではなく**「配分を変えるから」畳んだ**。
  復活させたくなったらこの事実から出発すること。
- routine 4本（tse-daily-auto / autodraft-article / autodraft-publish / scam-daily-auto）は
  **エージェントからは停止できない**（http_api 作成のため delete も update も拒否される）。
  **オーナーが claude.ai 側で停止した**（9/22 実施済み）。
- `check_automation_health.py` の `QUEUE_LANES` から3レーンとも解除。
  🔑 **レーンを畳んだら見張りからも外す**（足すときの登録と対の手順）。残すと毎朝必ず赤くなる。

### 🆕 COMPANY_GUIDE を改訂（第5回・2026-09-26 から適用）

1. **日本株3 : 海外株1**（旧「日米を交互」は廃止）。判定は機械的に＝直近3回のうち海外株が
   1回以上あれば今週は日本株。理由＝読者は日本の個人投資家で、海外株は口座・税制・時差の壁がある。
2. **§1② 「どのつながりの中にいるか」を新設＝このシリーズの新しい芯。**
   > オーナー「次にこれが来るとか上がるとか下がるとかじゃなくて、今人気のある銘柄の関連する銘柄は
   > こういうセクターだとか、こういうつながりがあるとか、そういうのが知りたい。上がる上がらないはなしで大丈夫」

   川上→川中→川下の工程をSVG1枚にし、その会社が立つ箱を色で示す。根拠はセグメント売上（一次情報）。
   前後の工程は**一般名まで**（「変電設備を作る重電メーカー」）。
   🚨 **新しく足した禁止**＝「次はこのセクターが来る」と書かない／**関連銘柄のリストを作らない**
   （社名を並べた瞬間に実質的な推奨）／「〜が崩壊したら〜が恩恵」と書かない／
   **つながり＝値動きの連動と読ませない**（節末に打ち消しの一文を必ず置く）。
3. **自己判断の強調**を §1② と §1⑥ の締めにも置く。つながりを扱う記事は「次はこれ」と読まれやすく、
   免責を形式的に置くだけでは足りないため。
4. ⚠️ §1 に項目②を挿入したので**旧②〜⑦が③〜⑧に繰り下がった**。手順書内の §1 参照11箇所は更新済み。
   旧番号のメモが残っていたらこの繰り下げを疑うこと。

### 🆕 広告を22本→376本へ（`inject_ads.py --all` を新設）

- 従来は POOLS に手書きした22本だけ＝**全496本の4.5%**しかカバーしていなかった。
- `classify()` が本文の語数で FX / 株価指数 / 個別株を判定してプールを自動割当。
- **貼らない場所**（`DENY_PREFIX` / `DENY_EXACT`）＝`guide-scam-*`（詐欺注意喚起の直下に口座開設広告は
  本末転倒）／`guide-signal-lab-*`（検証記事）／`guide-new-books.html`（routine が毎週再生成＝消える）。
- 🔑 **`update-market-news.yml` に `inject_ads.py --all` を組み込んだ。**手動運用だと新記事に
  永久に入らない（4.5%だった原因がこれ）。あわせて commit ブロックの `git add` を
  `guide-*.html` 全体へ拡張（従来はシリーズ物しか stage しておらず、生成しても push されなかった）。

### 🆕 免責の三層を全記事で揃えた（252本→496本・`apply_disclaimer.py` 新設）

- 発端＝**ゲートの死角**。雛形流用で作った記事が2層のまま `check_guide_draft.py` を GREEN で通過した。
- 🔑 **判定はクラス名ではなく位置で行う**（上部バナー／本文末／フッター）。このサイトは時期とレーンで
  マークアップが違い（`disclaimer-banner` / `kinsho` / class無しの `<p data-disclaimer>`）、
  クラス名で数えると「あるのに無い」と誤判定して二重に足す。
- 実装中に2回、自分で壊して直した: ①footer に既存文があるのに新しい div を足して**文が重複（40本）**
  → 既存要素に**目印だけ付ける**方式へ ②バナーを h1 行頭に入れたため `<main>\n\n<h1>` の形で
  **冪等性が崩れた** → **本文コンテナの開きタグ直後**に入れる方式へ。
- 再発防止で `check_site_consistency.py` に三層チェックを追加（warning）。
  ⚠️ これは `GATE_FILES` の1本なので §④「固定ゲートのroutine改変」が翌朝鳴る。**想定済み**。

### 🔧 SVGのはみ出しを 25件→6件（`fix_svg_overflow.py` 新設）

- まず切り分け: 33件中**11件は誤検知**。固定オラクルは **`transform` を一切見ていない**（grepで0件）ため、
  `rotate(-90,...)` や `writing-mode:vertical-rl` の縦書き軸ラベルを横長ボックスとして誤計算する。
- 直し方を2回変えた: ①`x` をずらす方式は**ファイルを破損させた**（長さの変わる置換を前から当てて
  位置がずれた）うえ、ずらすと図と衝突して別のエラーを生んだ → revert
  ②**viewBox の幅を広げる**方式。相対位置が変わらないので衝突しない。置換は後ろから当てる。
- 🚨 **viewBox の原点は動かさない。**オラクルは `viewBox="0 0 W H"` にしか一致しないため、
  原点を変えると**検査対象から外れる＝ゲート破り**になる。

### 🚨 過去の「訂正」自体が誤りだった（数字の扱いの教訓）

- `SCAM_GUIDE` に「2026-08-14訂正：旧記載『1,362万円』は割り算が合わない誤り」とあったが、
  **1,362.5万円は警察庁の公表値で正しい数字**だった。警察庁は**「既遂1件当たり」＝分母が既遂件数**。
  被害額797.9億円÷**認知件数**5,893件＝約1,354万円と一致しないのは当然（認知には未遂が含まれる）。
- 🔑 教訓を書き換えた: **「合わない＝誤りと即断しない。まず分母を疑う」**。
  書くときは分母をラベルで明示する（「既遂1件当たり（警察庁公表）」／「÷認知件数の単純計算」）。
- 同種: `guide-nisa.html` の SVG RED も**ゲートの誤検知**だった（CSSクラスで `text-anchor:middle` を
  指定しており、オラクルはインライン属性しか読まない）。記事側にインライン属性を明示して解消。

### ⏭ 残課題（次にやるならここから）

1. **アフィリエイト（アクセストレード）の審査結果待ち**。運営＝株式会社インタースペース（東証スタンダード
   2122）。通ったら SBI証券等の提携を確認して `guide-nisa.html` / `guide-nisa-ranking.html` へ差し込む。
   ⚠️ 登録URLは検索で出たものを踏まず、必ず公式トップの「新規パートナー登録」から。
2. **SVG 残り6件**（occlusion 4／text重なり1／回転の誤検知1）と**左はみ出し3本**
   （`algo-volatility` / `margin-balance` / `proverb-ri-wa-moto-ni-ari`）。
   左はみ出しは長い中央寄せキャプションで、viewBox拡張では直らない（原点を動かせない）。
   **文言を短くするか2行に割る編集**が要る＝機械では判断できない。
3. **9/26（土）の company 第5回**が新方式の初回。結果を見て手順書を微調整する。
4. **公開済み記事に公開前ゲートを当てると208件 RED**（SVG114/noindex 77/ナビ不足16/スラッグ重複6）。
   ⚠️ ただし `check_guide_draft.py` は**下書きの公開可否を判定するゲート**で、公開済み記事の
   健康診断用ではない（`guide-auto-*` の noindex は指標プレビューの仕様の可能性が高い）。
   **一律に「不具合208件」と受け取らないこと。**切り分けが先。
5. **この文書のサイズ。**本日 153KB → **63KB** まで縮めた（2026-08 以前の12セクションを
   `SESSION_ARCHIVE.md` へ全文のまま退避）。ただし CLAUDE.md の予算 **30KB にはまだ2倍。**
   残っているのは 2026-09 の15セクションで、うち ✅完了済みのものは次の機会に退避してよい。
   🔑 **引き継ぎ文書が太ると、次セッションが読むだけでコンテキストを食う＝引き継ぎの目的と逆行する。**
   「最近のものだから残す」ではなく「**次の判断に要るか**」で選ぶこと。

---


## 🚨 2026-09-11: 米CPI の発表日を1日ずらして載せていた（オーナー報告・修正済み）

- **症状**: オーナー報告「米CPIの発表は今日（9/11）なのにカレンダーは昨日（9/10）」。
- **事実確認**: BLS 公式は **2026-09-11(金) 8:30 ET**（＝9/11 21:30 JST）。bls.gov はクラウドから egress 遮断
  （CONNECT 403）なので WebSearch で独立ソースを突き合わせ、さらに **BLS 自身の最新リリースが "2026 M07
  Results"（7月分）のまま＝8月分は未発表**であることを確認して裏を取った。
- **原因＝曜日からの推測**。「CPI は火〜木」という思い込みで 9/10(木) と書いていた。**BLS は金曜にも出す。**
- **同じ誤りがもう1件**: 10月分＝誤 11/12(木) → 正 **11/10(火)**。9月分 10/14(水)・11月分 12/10(木) は正しい。
- **修正**: `generate_market_news.py` の `ECONOMIC_EVENTS_2026`（マスター）と `economic-events.json`（生成物）の両方。
  マスターの先頭に「**曜日や前例から推測しない。BLS/FRB の公式スケジュールで確認する**」と明記した。
- 🔑 **これは再発**＝`check_economic_events()` は 2026-07-02 の日付誤り事故（CPI 7/10等）を受けて作られたのに
  今回を拾えなかった。**曜日ヒューリスティクスだったから**（雇用統計=金曜／ECB=木曜）。
  🚨 **CPI に曜日ルールを足してはいけない**＝正しい金曜(9/11)を誤りとして弾き、誤った木曜(9/10)を通してしまう。
- **入れた歯止め**: `check_us_monthly_indicators()`（リンター §7）＝**曜日は見ない**。今日以降の各月について
  米CPI・米雇用統計が**ちょうど1件**あるか（欠落・重複）と、CPI が**対象月の翌月8〜16日**という粗い窓に入るかだけ検査。
  欠落・重複・窓外の3種すべて、わざと壊して発火を実測済み。
- ✅ **恒久対策＝一次情報との機械突合を新設**（`verify_economic_calendar.py` ＋ `verify-calendar.yml`、
  月曜 07:10 と毎月25日）。🔑 **Claude セッションからは bls.gov が egress 遮断されるが、Actions のランナーからは
  届く**＝検証できる場所で回すのが肝。
- **実地検証で2つの落とし穴を潰した**（1回目の実行で発覚）:
  ①月名の略記（`Jan.` `Sept.`）を受けておらず解析が1行しか取れなかった
  ②その1行が過去分だったため**比較0件なのに「✅一致」と出た＝沈黙の成功**。
  → 解析6件未満・今日以降の比較0件は**失敗扱い**にし、解析できないときは素のテキストをログに出すようにした。
- **2回目で BLS 公式13件×2指標を正しく取得**。今回直した2件（CPI 8月分=9/11・10月分=11/10）が
  **公式と一致することを機械で確認**＝修正が正しかったことの独立した裏付け。
- 📌 **過去分にも同種のずれが残っている**（公式 2025年12月分=1/13 に対し当方 1/15、2026年1月分=2/13 に対し 2/12）。
  過ぎた回なので直していない。**系統的に曜日から推測していた**証拠として残す。
- ⚠️ 毎月の `monthly-calendar-reminder.yml`（25日）で翌月分を足すときも、**公式スケジュールで確認してから書くこと**。
  FOMC・ECB は今回の検証の対象外（BLS 管轄外）なので、引き続き人/エージェントの確認が要る。

## 🆕 2026-09-10: FX証券会社のアフィリエイト広告を7本に掲載（オーナー指示）

- **指示**: A8.net の FX 広告タグ（234×60・`a8mat=4BC736+9U8XPU+25B2+61C2P`）を掲載してほしい。
- **実装**: 既存の `inject_ads.py` に `"fx"` 枠を追加（DMM株・DMM CFD と同じ仕組み）。冪等・`--dry-run` 既定。
- **掲載先＝FXが主題の常設解説記事7本**（オーナー選択）: swap-points／leverage／currency-risk／
  currency-hedge-cost／yen-carry-trade／bid-ask-spread／investment-tax。
  🔑 **速報記事は外した**＝時間が経つと読まれないうえ本数が増え続けるため。DMM枠とも重複させない。
- **法務の芯**: ①「広告」ラベルを必ず出す（景表法ステマ規制・2023-10-01 施行）②**本文に推奨文・煽り文言を
  足さない**＝広告主のバナーをそのまま出すだけ（金商法の誇大広告／投資勧誘に寄せない。FX はレバレッジと
  損失リスクの表示が特に厳しい）③`rel="nofollow noopener"` 必須。
- **検証**: サイト全体の広告ブロック30個すべてで「広告」ラベルと nofollow を機械確認。冪等（2回目0件）。
  `check_site_consistency.py` は変更前後で警告リストが完全一致＝新たな指摘ゼロ。
- **PC/SP 出し分け**（オーナーが PC 用 468×60 を追送）: PC＝`a8mat=...+61JSH`／SP＝`a8mat=...+61C2P`。
- **alt＝「JFX株式会社」**（オーナー回答）。⚠️ `guide-yen-carry-trade.html` には別の A8 広告
  （`aid=260523623427`・300×250・手置き）が先にあり、その alt も一度巻き込んだので元に戻した。

## 🚨 2026-09-10 夜: 広告の「見えていない側も読み込まれる」欠陥を発見・修正（ランダム表示へ）

- **発見の経緯**: オーナーの「ランダムで表示できませんか」に答える前に現状を Chromium で実測したところ、
  **PC 表示でも SP 用バナーが、SP 表示でも PC 用バナーが読み込まれていた**（バナー画像2件・1x1計測gif 2件）。
  旧方式は PC/SP 両方を HTML に書き CSS `display:none` で隠す形で、**ブラウザは display:none の `<img>` も読む**。
  → **表示回数が実際の約2倍で記録され、クリック率が実態の半分に見える**。報酬は減らないが広告主にも好まれない。
  DMM株・DMM CFD を含む**全22か所**で起きていた。
- **修正＝ランダム表示と同時に解決**: `inject_ads.py` を書き換え、記事には枠だけ置き、`mw-ads.js` が
  **候補から1つ選び、画面幅に合う素材だけを描画**する。素材の定義は `CREATIVES` 1か所で、JS はそこから自動生成。
- **候補の組み方**（オーナー選択＝「記事に合う複数社からランダム」）: FX記事＝JFX＋DMM CFD／指数記事＝CFD＋株／
  個別株・NISA＝株＋CFD／`guide-investment-tax`＝JFX＋DMM株（株もFXも扱う回）。
- **検証**（すべて Chromium 実測）: 20回読み込みで両社が出る／PC は 468×60・SP は 234×60 を正しく選ぶ／
  **1表示あたりの計測gifがちょうど1件**（修正前は2件）／JS 無効でも `<noscript>` で1つだけ。
  `check_site_consistency.py` EXIT=0・冪等（`--replace --dry-run` で0件）。
- ⚠️ **ローカル `sync_to_github.py` の SYNC_FILES に `mw-ads.js` を足すこと**（新規ファイル）。
- 🚩 **未決**: `guide-yen-carry-trade.html` は手置きの 300×250 広告（`aid=260523623427`）とランダム枠が並ぶ。
  手置き側を外すか残すかはオーナー判断待ち。
- ⚠️ **広告主名は未確認なので `alt="広告"`**。リンクを開くと成果地点の誤クリックになるため、こちらでは開いていない。
  会社名がわかれば alt に入れるとスクリーンリーダー利用者に親切（任意）。

## 🚩 2026-09-08: signal-lab レーンで **#089 の上書き公開事故**（記事1本が消えている・未修復）

オーナーから「前後移動ボタンの番号がおかしい」と報告を受けて発見。**ナビの不具合ではなくレーンの事故**。

- **何が起きたか**: `guide-signal-lab-089.html` は 9/4 に **FWD N=287** の記事として公開されたが、
  **9/6 の実行が同じファイル名に FWD N=296 の別記事を上書き**した（コミット `publish: guide-signal-lab-089.html`
  2026-09-05 21:40 UTC・842行入れ替え）。**9/4 公開版の本文はサイトから消えている**（git 履歴には残る）。
- **結果**: 公開順が #88(9/3) → #90(9/5) → #89(9/6) → #91(9/7) と番号が前後。さらに **#089 と #090 が
  どちらも「FWD N=296」の同題材**になっている。
- **台帳との不一致**: `signal-lab-ledger.md` の #089 は依然として **N=287・9/4公開**と書いてあり、実ファイルの
  中身（N=296・9/6公開）と食い違う。#090 は N=296・9/5公開。
- **ナビ側の対処（済）**: `apply_series_nav.py` の signal-lab だけ `"order": "number"` ＝ **公開日ではなく
  ファイル名の回番号で並べる**。読者は #88→#89→#90 と番号で辿るため。他シリーズは従来どおり日付順。
- ✅ **2026-09-08 に4件すべて対処**（オーナー指示「順番にやってください」）:
  ①**復元**＝9/4 版（`9839c8b`）を `guide-signal-lab-089.html` へ戻した。`guides.html` のカードは
  9/6 の実行に触られておらず N=287・9/4 のままだった＝復元で記事とカードが一致した。
  ②**番号の重複解消**＝復元により 9/6 の重複記事はサイトから外れた（git 履歴には残る）。中身は #090 と
  完全に同じ数値（FWD 53.4%／4H 65.1%／N=296）＝別番号を与える価値が無いと判断。回番号の重複0件を機械確認。
  ③**台帳**＝トラッカー表 089 行の参照先を `guide-signal-lab-090.html` へ訂正し、冒頭に「🚨 事故記録」節を追加。
  ④**歯止め**＝`publish_article.py` に上書きゲート（HEAD 版と `datePublished` が違えば中止・`--allow-overwrite`
  で解除）／`check_site_consistency.py` に `check_series_numbering()`（回番号の重複・ファイル名とタイトルの
  #番号の不一致）。**どちらも事故を再現して発火を実測済み**。
- 🔑 **なぜ気づかれなかったか**＝ファイル名が既存だったため publish_article の②〜⑤が全て no-op になり、
  カード・サイトマップ・SYNC が「既に登録済み」で素通りした。**no-op は成功ではない**という教訓。

## 🆕 2026-09-05: 読者要望に対応＝連続シリーズに「前の記事／次の記事」ボタン（232本）

- **要望**（読者メール）: シリーズを遡るのに毎回 guides.html へ戻るのが不便。記事下部に前後移動ボタンが欲しい。
- **実装**: `apply_series_nav.py`（決定論ツール・既定 dry-run・`--apply` で書き込み・冪等）。
  対象＝signal-lab 89／proverb 48／scam 13／tse 4／news 78 の **232本**。
  並び順は記事の JSON-LD `datePublished`（同日はファイル名）＝**人が順番を管理しない**。
  差し込みは `</main>` → `</div><!-- /container -->` → `<footer` の順に見つかった位置の直前
  （記事の骨格が3世代あるため。`</main>` を持たない signal-lab 060/070/071 で実測）。
- **自動維持**: `update-market-news.yml` に `--apply` ステップ＋シリーズglobの `git add` を追加（1日2回）。
  🔑 **新記事が出ると「1つ前の記事の“次の記事”」も貼り替えが要る**ので、公開時に1回書く方式では必ず腐る。
  だから publish 側ではなく**毎回全部を敷き直す**設計にした。冪等なので変化が無ければ差分ゼロ。
- **検証**: 鎖の整合性（全232本で prev/next が隣と一致・先頭/末尾のダミー表示）0件不整合／リンク686本すべて実在／
  `check_site_consistency.py` EXIT=0（警告37件は従来どおりの既存偽陽性）／Chromium 実測でPC・375px・ダークの3態OK。
- **2026-09-05 オーナー指摘で順序を修正**: 記事途中（関連記事の直前）にあった「📚 解説記事一覧に戻る →」を
  **シリーズナビの下**へ移設（近い範囲＝シリーズ内移動を先に、広い範囲＝サイト全体の一覧を後に）。
  対象64本（proverb 48／scam 12／tse 4。signal-lab と news はこのボタンを元々持たない）で、全64本が同じ
  `<div>` 1形だった（実測）ので決定論で移設。**判定はブロック除去より前**に行う＝2回目以降はボタンが
  ブロックの中にいるため（冪等性の要）。一覧リンクの行き先が guides.html そのものの記事では
  「シリーズの記事一覧を見る →」を出さない＝同じ行き先のリンクを2本並べない。
- **2026-09-05 オーナー指摘② 差し込み位置**: 末尾ではなく **関連記事と免責事項のあいだ**へ（免責は一番下が自然）。
  実装＝`tail_disclaimer_pos()`＝「そこから記事終端までが免責要素・コメント・閉じタグ・空白だけ」を満たす
  **最も早い**免責要素の行頭に入れる（中間バナーと末尾バナーが並ぶ signal-lab 型では中間の手前が返る）。
  直前のコメント専用行（`<!-- ③ 末尾バナー -->` 等）も巻き取る。免責は `<p>` 296個・`<div>` 357個の2形で
  **入れ子div は0件**（実測）＝非貪欲の要素正規表現で安全。
  ⚠️ 14本（signal-lab の古い型）は **免責が関連記事より前**にあるので該当なし＝従来どおり記事の最後に置かれる。
  これは正しい挙動（「関連記事の後」は保たれる）。
- ⚠️ **ローカルミラーが232本遅行する**。オーナー側で `_pull_mirror.py --apply` を回すこと。
  また `apply_series_nav.py` をローカル `sync_to_github.py` の SYNC_FILES に登録すること。

## 🚩 同時に見つかった別件 — guides.html の「🧮 計算ツール（常設）」に記事カードが21本まぎれている

- `cat-tools` セクションのカード23本のうち、実際の計算ツールは `guide-breakeven-calc` / `guide-goal-calc` の**2本だけ**。
  残り21本は 東証のしくみ4・投資詐欺・話題の企業・格言など、**新カテゴリの記事が誤ってここへ挿入されている**。
- 原因の見立て＝`publish_article.py` が `--category` に対応するセクションを見つけられないとき先頭セクションへ入れている。
  新シリーズ（東証のしくみ／投資詐欺から身を守る／数字で見る、話題の企業）に **guides.html のカテゴリ節が無い**。
- **未着手**（読者要望の範囲外なのでオーナー判断待ち）。直すなら ①guides.html に3カテゴリ節を新設 ②`publish_article.py`
  にカテゴリ未知なら**止める**ゲートを足す（黙って先頭へ入れない）の2点。

## 🆕 2026-09-05: 投資格言シリーズ完結（#50 総集編）＝ routine 停止は**オーナーの手作業が必要**

- `guide-proverb-series-index.html`（#50 総集編・48本のテーマ別総目次）が 9/5 に公開され、`PROVERB_LEDGER.md` に
  「🏁 シリーズ完結（48本✅ + #23⛔スキップ + #50🏁）」が記録された。キューは全消化。
- ✅ **`proverb-daily-auto`（`trig_01P8Jjut79fHWoiALk4mfuJQ`）は削除済み**（オーナーが対応。2026-09-10 に
  `list_triggers` 26件を確認＝一覧に無い・ID検索でも不在）。
  ⚠️ **エージェントからは停止できなかった**＝オーナーが画面（http_api）で作った routine は `update_trigger` が
  「エージェントは自分が作った routine しか変更できない」と拒否する（実測 2026-09-05）。
  📌 **教訓＝オーナー作成の routine は、エージェントが停止まで含めて段取りできない。**停止を伴う計画は
  最初からオーナーの手作業を1ステップとして見込むこと。**そして「やってもらう前提」で放置せず、
  次の機会に必ず実態を確認すること**（2026-09-10 まで未対応として案内し続けていた＝確認を怠った）。
- 放置しても実害は小さい（PROVERB_GUIDE §【キュー】#50 に「以後の実行は何もしない」＝キュー空で手順2が自動停止）が、
  毎日 10:12 JST に空回りしてトークンを消費するので止めるのが望ましい。
- 📌 **教訓＝オーナー作成の routine は、エージェントが「停止まで含めて」段取りできない。**停止を伴う計画は
  最初からオーナーの手作業を1ステップとして見込むこと。

## 🆕 2026-09-02 夜: 更新履歴に「2026-09-06 公開」の未来日付＝週次戦略の cron 遅延事故（修正済み）

- **症状**: index.html の更新履歴に `今週の投資戦略（9/7〜9/11）` が **2026-09-06 公開**として載った（オーナー報告）。
- **原因**: 8/30(日) 20:13 JST の `weekly-strategy.yml` が GitHub 側で約4時間遅れ、**8/31(月) 00:19/00:26 JST** に実行。
  `auto_weekly_strategy.get_next_monday` が「今日が月曜なら次週の月曜」だったため、8/31 のデータで 9/7 週の記事
  `guide-weekly-2026-09-07.html` を生成（同じ夜に正規の 8/31 記事も既にあった）。`build_weekly_history_item` は
  最新ファイルの週開始−1日を公開日にするので 9/6 になった。
- **修正（PR #7 に同梱）**: ①`guide-weekly-2026-09-07.html` を削除（sitemap/guides には未掲載・index のみ参照）
  ②`get_next_monday`＝月曜に走ったらその月曜（今週）を返す ③`generate_market_news._find_latest_weekly_article`＝
  週開始が「今日+1日」より先のファイルは候補にしない（防御）。マージ後は `generate_market_news.py` の push で
  update-market-news が自動起動し、index の履歴とバナーが 8/31 記事に戻る。
- ⚠️ 9/6(日) の次回実行で 9/7 記事が**正しく**生成されるので、削除したファイル名が復活するのは正常。

## 🆕 2026-09-02: scam のエスカレ3件をすべて解消（3本公開）＋ proverb 停止を予約

| slug | 結果 | 台帳の「最短ルート」との差 |
|---|---|---|
| investment-seminar | ✅ 公開 | **「条文確認のみで即公開可」は誤り**。無登録営業の罰則は令和8年法律第64号（2026-07-23 公布・**2026-08-12 施行**）で **第197条第1項第4号の3（10年以下の拘禁刑/1,000万円）・第207条第1項第1号（法人7億円）**に上がっていた。下書きは改正前条文。e-Gov 法令API の CurrentEnforced 版で確認して差し替え |
| crypto-scam | ✅ 公開 | 下書きの「2,148件・246.3億円」も、**台帳が示した修正値「6.7%・19.5%」も令和7年の数字**。警察庁 PDF 原本（2026-07-30 付）から 2026年上半期の実数へ全面差し替え。同じ「暗号資産送信型」でも全体/SNS型投資/ロマンスで数字が違う |
| recovery-scam | ✅ 公開（再執筆） | content-writer 再執筆 → Opus 監査①🔴黒2 → 修正 → 別 Opus 監査②🟢白 → 公開。監査①の「SNS型投資詐欺は特殊詐欺の外数」は旧定義の誤り（2026年から内数・原本に 26.7% 明記） |

- 3本とも `publish_article.py` で②〜⑤登録済み・台帳に ✅エントリ追記済み。番人の scam 残量は 3→6 になる
- 🔑 **学び＝法令の条番号と統計の区分は「要約」でなく原本で**。台帳の修正指示すら古かった。SOP に「e-Gov CurrentEnforced 版」「PDF 原本の該当段落」を確認日つきで書く運用を足すべき
- ⏰ **proverb-daily-auto の停止**＝9/5 に #50 公開を確認 → オーナーが routine を削除（2026-09-10 に不在を確認済み）
- ⚠️ 本セッションの成果物はすべて **ブランチ `claude/issue-check-do2bbk`**（main 未マージ）。マージ後に update-market-news が index/sitemap を再生成する。ローカル `sync_to_github.py` の SYNC_FILES に `build_edinet_yuho.py`／`.github/workflows/edinet-yuho.yml`／`guide-scam-{investment-seminar,crypto-scam,recovery-scam}.html` を追加すること

## 🆕 2026-09-02: 有報を Actions が取って置く（`edinet-yuho.yml`）＝日本株の週次エスカレを止める

**きっかけ**＝9/1 の初回で「EDINET API v2 が 401＝Subscription-Key 未取得」としてオーナー判断に上げていたが、
Secrets を実際に見ると **`EDINET_API_KEY` は登録済み**で、`edinet-holdings.yml` は 8/10 から20回連続で v2 を叩けていた。
**routine が読めないのは Secrets を持たないから**であって、キーの有無ではない。

**打ち手（fundamental-context.json と同じ型）**
- `build_edinet_yuho.py` ＋ `.github/workflows/edinet-yuho.yml`（平日 19:40 JST）→ **`edinet-yuho.json`**（SYNC禁忌・GitHub側生成）
  - 候補＝`jp-rankings.json` に直近14日で2回以上（§2-1 と同じ定義。登場履歴は JSON 自身が積む＝**routine の git log 遡りが不要**）
  - 有報（docTypeCode 120）の索引を**日付単位で毎日60日ぶんずつ遡って**積む（規約の「大量アクセス」回避。420日で完成＝約1週間）
  - 候補ごとに type=5（XBRL→CSV）から **事業等のリスク／沿革／事業の内容／経営方針／MD&A** の本文と
    **主要な経営指標等の推移**の数値を入れる。**本文は無加工**（HTMLタグ除去のみ）・60,000字で切って `truncated` を立てる
  - 登録＝`check_site_consistency` SYNC_FORBIDDEN／`check_automation_health` WORKFLOW_CHECKS（80h・warn。
    ⚠️ **`edinet-holdings.yml` も監視表に無かった**ので同時に登録）
- `drafts/COMPANY_GUIDE.md` §2-1／§2-2 を「JSON を読むだけ」に書き換え（旧・要オーナー判断の3択は撤回）
- ⚠️ **ローカルの `sync_to_github.py` の SYNC_FILES に `build_edinet_yuho.py` と `.github/workflows/edinet-yuho.yml` を足すこと**
  （クラウド側の sync_to_github.py はスタブなのでここでは足せない）

**実キー検証（同日・ブランチ上で2回実行・いずれも success）**＝2回目で索引 2,551社（120日ぶん）、候補5社のうち3社
（スクウェア・エニックスHD／北海道電力／九州電力＝いずれも第102期・第46期の有報 2026-06-23〜24 提出）の**本文5種と主要指標22〜23項目を取得できた**。
`missing` の2社（パーク24＝10月期・PPIH＝6月期）は索引が届けば取れる。JSON は約1MB（索引が主）。検証用の push トリガーは外した。

**次に見るもの**＝`edinet-yuho.json` の `index_complete` が true になる日（初回は workflow_dispatch で backfill 90日）、
9/5(土) 14:23 の company-weekly-auto が日本株を `companies` から書けるか。

## 🛡️ コードが強制しているルール一覧（"覚える"でなく"コードで強制"）

> 設計思想（CLAUDE.md／auto-memory `feedback_rules_as_code`）＝**人が手で守るルールを、コードが自動で守る形へ**。
> 新ルールは「文書に書いて記憶で守る」より「**チェックを1個足す**」。私が記憶で守るルール数をゼロに近づける。
> 文書が長くなったら、必須ルールはコードへ移して文書から消し、古い履歴はアーカイブする（このスリム化もその一環）。

| 強制しているルール | 受け皿コード（単一の真実） | 効果 |
|---|---|---|
| SYNC禁忌ファイルを誤って push しない | `check_site_consistency.py` の `SYNC_FORBIDDEN`（`mw check`） | 巻き戻し事故を push 前に **error 停止** |
| **ドキュメントの文字でライブが止まるのを防止** 🆕8/5 | `.nojekyll`（Jekyll を丸ごと迂回）＋ `check_site_consistency.py` の `check_liquid_in_markdown` | Jekyll は**同期対象の .md 24件を Liquid として解釈**する。手順書に波括弧2連を書いただけで**Pages ビルドが12時間全失敗**（`duration=0` の即失敗・本文は "Page build failed." のみ）。**ライブは古いビルドを配信し続け health-check も緑**＝人力では気づけない。検査は `.nojekyll` 有無で error/warning を切替（無ければ実際に落ちるので error）。誤検知ゼロを実測 |
| **新記事が旧テンプレ/旧色で公開されるのを防止** 🆕8/5 | `check_guide_draft.py` 検査9（判定は `apply_brand_color` の RULES/TOKENS を流用＝**色の知識を複製しない**） | ①Phase1の役割に旧色が残る ②ブランド色が1つも無い（＝既存と構造が違うテンプレ）の2条件で公開ブロック。`guide-signal-lab-060` はクラウドが**CSS変数式の別テンプレ**で出したため既存ルールが1つも当たらず、60本中1本だけ未適用だった。**誤検知率を全295本で実測＝0本**（真陽性1のみ） |
| **ロゴのラスタ画像が旧色で取り残されるのを防止** 🆕8/5 | `make_favicons.py`（`favicon.svg` を**単一の真実**として Pillow で描き直す。cairosvg 等の依存は足さない） | `apply_logo.py` はHTML内のインラインSVGしか直せず、**favicon-32/192・apple-touch-icon・ico は旧色のまま残る**（SVG対応ブラウザだけ新色という食い違い）。旧SVGで描き起こして現物と照合＝画素の96〜99.6%が一致することを実測してから配色を変えた。⚠️ロゴを変えたら `apply_logo.py --apply` と**両方**回す |
| **コミット連打で Pages のビルド上限を叩かない** 🆕8/5 | `sync_to_github.py --batch`（Git Data API で blob/tree→**1コミット**。テキストは tree に content 直埋め・バイナリのみ blob 化） | Contents API は**1ファイル＝1コミット**なので 257件の sync がビルド連打になる（実測 JST04時台48件/05時台11件/06時台25件・目安は10回/時）。一括なら**内容一致251件は tree 取得1回で通信ゼロ判定**＝往復も激減。staleness ガードと `--force` の意味は逐次版と同一 |
| 公開前に main を取り込む（reconcile） | `publish_article.py` 内蔵 reconcile | ローカル公開での巻き戻しを防止 |
| **ローカルが古い状態での sync 巻き戻し防止** 🆕 | `sync_to_github.py` の staleness ガード（remote_sha baseline 比較） | 前回 sync 後に GitHub 側が更新されたファイルの push を **🚫中止**（意図的なら `--force`） |
| **公開記事が guides.html カードから消えていないか** 🆕 | `check_automation_health.py` §③（`automation-health.yml` 毎朝09:30 JST） | 巻き戻し（local-drift）を **翌朝 Issue で即検知** |
| **同一 workflow の同時実行レース防止** 🆕 | `update-market-news.yml` の `concurrency`（cancel-in-progress: true） | push(on:push)＋手動trigger の二重起動を新しい方に一本化＝失敗run・誤アラートを根絶 |
| kinsho-v1 免責 / 10ボタンナビ / リンク切れ / SYNC_FILES登録 | `check_site_consistency.py`（`mw check`／土曜 `site-qa-lint`） | 不変条件を push 前に検査・exit 1 |
| 研究日誌の数値捏造防止 | `signal_lab_verify.py`（固定オラクル・編集禁止） | claims.json を signals-log から独立再計算して突合 |
| 更新履歴の整列・最新5件 | `generate_market_news.py` の `_history_items` | 手で削らない（日付降順・自動整列） |
| **発注前ルールの遵守を数値監視** 🆕 | `_trade_discipline_check.py`（`mw discipline`・週次 /loop） | 指標持ち越し/指数重ね張り/SL未設定/損切りずらし/JP✕を **EVダメージ順に可視化** |
| **相関ポジ合算リスク2%・実スリッページ** 🆕7/5 | 同上 R6/SLP（フォーム任意3欄=口座残高/リスク額/予定価格の入力分から自動判定） | 同テーマ×同方向の同時保有を横断検出し合算%で2%判定（金銀・FXも対象＝指数限定R3の一般化）。前向き検証の群割当は登録時5ルール固定＝R6は参考枠 |
| **公開日のUTC日付ミス防止** 🆕7/6 | `signal_lab_verify.py` の `date_check()`（再監査は `SIGNAL_LAB_SKIP_DATE_CHECK=1`） | datePublished/公開表記≠JST今日なら**赤=公開ブロック**（#031が7/5付けで公開された事故の再発防止） |
| **似スラッグの重複記事防止** 🆕7/6 | `check_guide_draft.py` 検査7 スラッグ重複検査（トークン集合の同一/包含） | ⑫⑬型の「語順違い/部分一致スラッグの同一主題」自動公開を**RED=人間エスカレ**（82記事総当たりで誤検知ゼロ） |
| **研究台帳の数値転記ミス・改竄・肥大化防止** 🆕7/7 | `_doctrine_check.py`（`mw evolve`・固定オラクル扱い） | DOCTRINEのアンカー**全件**（件数は書かない＝古びる）を出典JSON/mdと突合＋事前登録簿SHA256＋サイズ予算＋§1構造検査で**error停止**。予算は7/31に30/36KBへ改定＝上の(d)節が根拠。⚠️prose は**部分一致**なので、退避時は必ず前後で `_doctrine_check.py` を回す（偽陽性通過の実例あり） |
| **自動生成レーンの投資助言化を防止** 🆕8/1 | **`compliance_gate.py`**＝禁止語の単一の真実（SYNC対象）。**3レーン全部**が import＝`auto_weekly_review`／`generate_monthly_report`／`auto_weekly_strategy`（テスト=**`_test_weekly_review_gate.py` 18件＋`_test_compliance_gate.py` 31件**） | 禁止語に触れた生成文は不採用→次モデル→全滅なら決定論テンプレへ。**LLMの自己申告に頼らずコードで止める**（プロンプト修正だけでは再発する）。⚠️**8/1夕方の追加調査で、同じ穴が月次・週次戦略にも在ると判明**＝weekly-review を直した時点では塞がっていなかった。良性マスク（`BENIGN_PHRASES`＝「注目すべき」等）で週次戦略の誤検知を 1/10→0/10 に実測 |
| **免責(kinsho-v1)の全レーン検査** 🆕8/1 | `check_site_consistency.py` の免責検査を `AUTO_PREFIXES` の `continue` **より前**へ移動 | 除外が SYNC登録/リンク切れの誤検知回避のために置かれ、**巻き添えで免責検査まで飛んでいた**＝免責ゼロ2本を1本も検知できず。免責は生成レーンを問わない共通不変条件。誤検知は実測ゼロ（278本中、未設置は真に2本） |
| **銘柄チャートを記事に埋め込む** 🆕8/1 | `_gen_stock_panel.py`（テスト=**`_test_gen_stock_panel.py` 30件**） | 座標は全て計算（目分量のSVGは必ず破綻）。**10倍超の値動きは自動で対数軸**／軸に負の株価を出さない／x軸右端は必ず最終足／**最終足の出来高が未確定なら描かず注記**（決算日の出来高が中央値の2.9%＝Yahooの未確定値で「急騰は薄商い」と逆の示唆になる事故を防止）／トレンドライン・目標株価・売買示唆は描かない |
| **「登録したが走らない設計」の防止** 🆕7/31 | `_precheck_feasibility.py`（`control`＝同日対照が必要本数取れるか／`dim`＝使う次元が対象ログに在るか。テスト=**`_test_precheck_feasibility.py` 13件**）＋`mw evolve` の次候補に欠落欄を併記 | 必須欄チェックは**欄が埋まっているか**しか見ておらず、**埋めた設計が実行可能かは見ていない**。Q10（19銘柄に `min_ctl=30`＝理論上限18）と Q24（BTログの `news_count` 保有0.0%）を1本ずつ失って追加。⚠️**独立warningは足さず、提案するその場で欠落を出す**＝鳴りっぱなしを1本も増やさない設計 |
| **非公開研究（research/）の公開リポ流出防止** 🆕7/31 | `check_site_consistency.py` 検査1 `check_sync_forbidden`＝**ディレクトリ単位**の規則（テスト=**`_test_sync_research_guard.py` 22件**） | 列挙式は足し忘れが穴になる＝実測で research/ の非アンダースコア .md **13件中 登録済みは2件だけ**（`hypothesis_queue_archive.md`＝非公開仮説の全文147KB すら未登録）。境界は実態と一致（SYNC_FILES に research/ 配下は皆無）＝**誤検知率は全件でテストが毎回実測** |
| **非公開研究ファイルの公開リポ流出防止** 🆕7/7 | `check_site_consistency.py`＝SYNC_FORBIDDEN追加＋**`_`プレフィックス=ローカル専用規約** | DOCTRINE/queue/`_jp_*`等が SYNC_FILES に混入したら**error停止**（REDテスト7ケース済） |
| **公開記事への下書き残骸混入防止** 🆕7/7 | `signal_lab_verify.py` date_check の残骸検査 | 「下書き中」が本文に残っていたら**赤=公開ブロック**（#032実例の再発防止） |
| **休場中の発火を勝率に含めない** 🆕7/11 | エンジン=`generate_technical_alerts.py`週末閉場ガード（土07:00〜月06:00 JST・BTC除外・発火スキップ）＋集計=`is_weekend_closed_fire`（track-record/週次/月次の3本に同一定義複製） | 塩漬けデータ発火（実測214件・勝率33% vs 全体41.6%＝週明けギャップでSL直撃の測定アーティファクト）を源流と集計の両方で遮断。生ログは不変・ページに除外注記あり |
| **ローカル公開の日付事故防止** 🆕7/22 | `publish_article.py` の `check_date_gate`（免除は `--allow-backdate`・テスト=`_test_publish_date_gate.py` 5件） | 公開日≠JST今日なら **🚫 exit 1 で公開停止**（7/15事故の恒久対策・signal-lab date_check と同型） |
| **自動公開レーンの「静かな停止」検知** 🆕7/26 | `check_automation_health.py` §⑤（`automation-health.yml` 毎朝09:30 JST・テスト=`_test_topic_queue.py` 12件） | autodraft の未公開 topic が5件未満で **Issue**。①②は「走ったか」しか見ないのでキュー枯渇による仕様どおりの停止を捕まえられなかった（7/20〜24 に5日連続スキップを誰も検知できなかった実例）|
| **事前登録した仮説の「登録漏れ」検知** 🆕7/27 | `check_automation_health.py` §⑥（同 09:30 JST・`check_tracker_registration`） | コード側の宣言（`SEED`＋register定数）と実体（`signal-lab-tracker.json`）を突合し、**idもfilterも不在**なら **Issue**。7/27 に Q35の3件が「SEEDに足しただけ＝一度も登録されず」なのに台帳が「観測開始」と書いていた事故の恒久対策。**filter重複による正常スキップ（`metal_all_1d`等4件）は誤検知しない**ことを実データで確認済み |
| **エスカレした研究日誌が「直せなくなる」のを防止** 🆕8/11 | `signal_lab_verify.py` の **基準時刻凍結**（claims.json の `"asof"` ／ CLI `--asof`。打ち切りは `outcome_resolved_at`＝決済確定時刻。テスト=**`_test_asof_freeze.py` 16件**）。**使い方の正は `SIGLAB_ESCALATION_RECOVERY.md`** | 🚩が付いた回は**後日ライブログが進むと永久にREDで直せなかった**（#065 0/6・#067 は当日夕方に 11/11→2/11）。凍結で **#065 6/6・#067 11/11 に完全再現**を実測。⚠️捏造不可は不変＝数字は実ログとの完全一致が必要で asof は断面を選べるだけ。しかも **asof の日付は記事の公開日と一致必須**。⚠️`fired_at` で切ると誤り |
| **エスカレの滞留検知** 🆕8/11 | `check_automation_health.py` §⑧（`ESCALATION_STALE_DAYS=1`・テスト=**`_test_escalation_backlog.py` 13件**） | 「走ったが人間待ちで止まっている」形は①②③⑤の全部の死角だった＝オーナーが**目視**で気づくまで #065 が2日滞留。解決判定は公開実体（`guide-<target>.html` の有無）。🚩当日は鳴らさない（ゲートが働いた正常系） |
| **やりかけの仮説を増やしすぎない（WIP上限）** 🆕8/11 | `_doctrine_check.py` の `QUEUE_WARN_N/QUEUE_ERR_N`＝**12本で黄信号・16本で error**（`check_queue_wip`・テスト10件。**根拠と閾値の単一ソースは同ファイルの定数コメント**） | 旧・キューのバイト予算(36KB)を置換。要点＝36KBは「1.3KB/本」前提だったが、証拠要件を入れた **7/26以降**のブロックは平均7.8KB／以前は1.6KB＝**厚く書くほど罰せられる**逆向きの予算だった（較正が1日ずれていた）。error は**1本閉じるまで新規登録しない**WIP上限。⚠️`declutter_audit.py` の表からキューを外した（二次側が一次側と食い違う対処を出すため） |
| **事前登録の「空欄のまま登録済み」防止** 🆕7/26 | `_doctrine_check.py` の `REQUIRED_Q_FIELDS`＋`_q_field_gaps`（回帰テスト=**`_test_doctrine_registry.py` 23件**・実キュー31件でE2E確認） | 新Qは 登録日/ルール素案/検証設計/**対照**/主要評価指標/合格基準/**検出力** が埋まるまで **error＝登録簿に載せない**。SHA256は登録"後"の改竄しか見ておらず、テンプレのまま登録される穴があった。既存Qには遡及しない |
| **取り直せないスナップショットの欠測検知** 🆕7/28 | `_doctrine_check.py --agenda`（`mw evolve`）の心拍鮮度＋`_jp_earnings_cal_logger.py` の追記/冪等 | 決算カレンダーは**翌営業日1日分・履歴なし＝走らなかった日は永久欠測**。3日沈黙で ⚠️。**automation-health は GitHub 側でローカル専用ロガーを見られない**ため番人をここに置いた。BOM有無/沈黙/正常の3分岐を実測（BOMで例外→握り潰し→**番人が黙る**壊れ方を実際に踏んで修正済み） |
| **実在しない記事へのリンク公開を防止** 🆕7/30 | `publish_article.py` の `check_link_gate`（判定は `check_guide_draft.internal_link_check` に一本化＝基準の単一ソース。テスト=**`_test_guide_link_check.py` 20件＋`_test_publish_link_gate.py` 7件**） | 参照先が実ファイルとして存在しなければ **🚫 exit 1 で公開停止**（免除は `--allow-missing-links`）。Search Console の404の恒久対策。**要点は「全レーンが通る関門に置く」**＝`check_guide_draft` 側だけでは news/proverb レーンが素通りする。併せて `CLOUD_GENERATED` でSYNC禁忌ページを除外しないと**ナビ経由で全記事RED**（実測217/217→5件） |
| **ローカルミラーの遅行を解消** 🆕7/31 | `_pull_mirror.py`（ローカル専用・冪等・dry-run既定） | クラウドが公開/更新した記事を取り込む。**内容ハッシュ(git blob sha)で比較**するので「ローカルに在るが古い」も検出。取り込み内容＝リモートと同一なので **sync は「⏭️内容変更なし」でスキップ＝無駄なコミットが出ない**。`guide-new-books.html` は SYNC_FORBIDDEN のため除外。⚠️これを怠ると `mw check`・404監査・FPテストが**揃って誤検知**する（7/31 に3回） |
| sitemap 全記事網羅 | `generate_market_news.py` の `build_sitemap_xml`＋`is_noindex_slug`（除外の単一ソース） | 全 guide を自動収集・手動編集不要。未掲載＝noindex 対象の意図的除外（7/31 実測で55本中54本が該当＝**不具合ではない**）。⚠️新記事公開時は sync 後に **workflow を手動 trigger**（下記の push 順序） |
| **「Run failed」の誤判定を防ぐ** 🆕8/8 | `judge_runs`（`check_automation_health.py`・テスト16件） | 8/6の失敗11件は**10件がcancelled・失敗step0**＋1件が GitHub の `Service Unavailable`＝コード起因ゼロ。旧番人は直近1件の`!=success`判定＝**誤Issueの時限爆弾**。新＝cancelled除外・閾値内にsuccess 0なら異常。⚠️`signal-workflows`群は`signals-log.json`共有＝**分割禁止**（詳細=auto-memory `reference_actions_failure_triage`） |
| **tickerフィードの停止検知** 🆕8/6 | `build_news_ticker.py` の `feed_health`＋§⑦（閾値=`FEEDS`の`stale_days`・テスト27件） | workflow緑のまま特定フィードだけ死ぬ形（8/1 Bloomberg型）を捕捉。同日ソース10→18本（公的機関+トピック横断・バッジは実発行元）。トピック検索は監視外＝誤検知ゼロ方針 |

🆕＝2026-06-20 追加（B＝カバレッジ番人 ／ C＝sync staleness ガード）。新ルールはこの表に1行＋チェック1個で増やす。

---

## ⚠️ 絶対遵守（事故防止）

- **SYNC禁忌**（ローカルから絶対 push しない＝routine/cron/generate が GitHub 側で生成）。**正は CLAUDE.md の SYNC禁忌リスト**。代表例：
  6コアHTML（index/calendar/charts/vix/market-health/hot-assets）／`signals-log.json`／`technical-alerts-history*.json`／`track-record.html`／political系／youtube系／`fundamental-context.json`／`weekly-levels.json`／`weekly-zone-plan.md`／`sitemap.xml`／`weekly-strategy-context.json`／`indicator-result.json`／`signal-lab-tracker.json`／`signals-log-backtest.json`／`article-ideas.md`／`daily-preview.md`／`political-digest.md`／`compliance-scan.md`／`site-qa-report.md`／`panic-scan.md`／`drafts/draft-*`・`drafts/news/*`・`drafts/sns/*`
  → `mw check`（`check_site_consistency.py`）が SYNC_FILES への誤混入を、sync の staleガードが「古いローカルでの上書き」を、それぞれ自動で止める。
- **SYNC対象（OK）**：`*.py`（※`sync_to_github.py` はローカル専用＝GitHub側は**クラウド用スタブ**。⚠️2026-07-28訂正＝**サイズで判定しない**。クラウドが記事を公開するたび `publish_article.py` が追記するので**増え続ける**（616B→3,763B/84記事）。逆に**クラウド公開記事の台帳として使える**。**`mw.py` はSYNC対象**＝7/7訂正。`_`プレフィックスのファイルは全てローカル専用＝SYNC禁止をコードで強制済）／`.github/workflows/*.yml`／個別 `guide-*.html`／`guides.html`／`robots.txt`／`my-trades.json`／`memory/*.md`／各 docs。
- 記事追加は **`python mw.py publish ...` → sync → workflow → ライブ確認**。公開前に compliance-reviewer(Opus)監査・教育トーン・特定銘柄の買い推奨は書かない・kinsho-v1 免責・10ボタンナビ。手動時も `mw check` で push 前点検。
- ネット不調時は無限リトライせず、ブラウザで手動 trigger を依頼（最大3〜5回）。
- **価格キャッシュは消さない**＝Yahoo の遡及上限（1h=730日/15m=60日）を超える分は**再取得不能**（`_all1h` は329日ぶん）。

---

## 📌 アクティブな宿題

## 2026-09-01（夜）Apple記事の出典URL補完／jp-rankings が4日間「静かに止まっていた」理由

### ① Apple記事（AAPL 第1回）の歴史年表に出典URLを補完 — `40cd0db`

**訂正から。**私（Claude）は前回「時価総額の金額が本文にある」「各種メディア報道という出典がある」と報告したが、
**それは審査途中の下書きを見ていた誤りだった。**コミット履歴が `11:26 A1-A5修正` → `11:36 auto-publish` の順で、
公開前にコンプラ／品質ゲートが両方とも直していた。**公開物を批評するときは公開後のコミットを取ること。**

実際に残っていた欠けは別で、年表3行が `Form 10-K` / `Form 8-K` / `Form 10-Q` を**名前だけで引用**していた
（提出日・確認日はあるがリンクが無い）。新設の機械チェック⑧「歴史年表の全行にURLがある」なら赤くなる。

EDGAR から正確な文書URLを取り、3本とも 200 を実測してから貼った。結果 **年表11行すべてにURL**（URL無し0件）。

- 10-K（2025-10-31提出）`https://www.sec.gov/Archives/edgar/data/320193/000032019325000079/aapl-20250927.htm`
- 8-K（2026-07-30提出）`https://www.sec.gov/Archives/edgar/data/320193/000032019326000018/aapl-20260730.htm`
- 10-Q（2026-07-31提出）`https://www.sec.gov/Archives/edgar/data/320193/000032019326000020/aapl-20260627.htm`

📌 **ルーティンが生成した記事をローカルで直して同期する前例**＝`guide-signal-lab-00N.html` と同じ扱い。
`SYNC_FILES` に `guide-company-aapl-apple.html` を追加した。

### ② 同期の巻き戻し防止ガードが実際に効いた（設計の実証）

押す前に同期対象292件を本番と突き合わせたら、**ローカルが本番より古いファイルが7件**あった
（`guides.html`・`generate_market_news.py`・`check_site_consistency.py` 等＝**ルーティンがGitHub側に直接書いたもの**）。
うっかり全同期していれば Apple記事の一覧カードごと巻き戻すところだった。

🔑 **このリポジトリは git ではなくAPI同期なので、「ローカルが常に最新」は成り立たない。**
ルーティンが本番に直接書く以上、**ローカルは構造的に遅れる**。
`sync_to_github.py` の「GitHub側が前回sync後に更新されています → push 中止」が正しくその7件だけを止め、
私の1件だけを送った。**このガードを `--force` で黙らせないこと。**

### ③ jp-rankings.json が 8/29 から止まっていた理由＝**遅すぎる実行は1営業日古いデータしか取れない** — `14a17c3`

**コードのバグではない。**ワークフローは毎回 `success` で、コミットもしていた（8/31分の遅延実行も
`jp-margin.json` は更新している）。動いていなかったのは `jp-rankings.json` の中身だけ。

過去14回の実行ログを全部読んで `asof` を並べたら、**実行時刻と鮮度に強い相関**が出た:

| 実行時刻(JST) | 取得できた asof | |
|---|---|---|
| 08-25 17:53（定刻） | 2026-08-25 | 当日 ✅ |
| 08-26 17:21 / 17:55（定刻） | 2026-08-26 | 当日 ✅ |
| 08-29 16:23（手動） | 2026-08-28 | 当日 ✅ |
| 08-28 03:24 / 04:02（深夜） | 2026-08-26 | **1営業日古い** |
| 08-29 04:34 / 05:09（深夜） | 2026-08-27 | **1営業日古い** |
| 09-01 00:24 / 01:06（深夜） | 2026-08-28 | **1営業日古い** |

🚨 **直感と逆。**「クローズから時間が経つほど確実」ではなく、**深夜(00-05時 JST)に流れると必ず1営業日古い。**
そして古いデータは前回と同じ内容になるので `git diff --cached --quiet` が真になり、
**「No changes.」で静かに終わって success として記録される。**4日間止まっても誰も気づかない壊れ方。

原因は GitHub の schedule 遅延（16:40/17:10 JST 設定に対し実測5〜8時間遅れ）。
9/1分は 20:47 JST 時点でまだ発火すらしていなかった。

**打った手:** cron を夕方帯に4本へ（＋18:10 / 19:10 JST）＝深夜に落ちきる前に1本当たる確率を上げる。
⚠️ **確率を上げるだけで保証ではない。**確実性が要るなら外部スケジューラから `workflow_dispatch` を叩く構成へ
（ai-tsukaikata 側も同じ結論に到達している）。

**手動実行の結果:** 2026-09-01 21:18 JST に workflow_dispatch → `asof 2026-09-01`（395銘柄・取得失敗5＝カバレッジ99%）を取得してコミット `25dc30e`。本番の jp-rankings.json も `asof 2026-09-01` に復旧したことを raw で実測。

📌 **company レーンの日本株候補選定は jp-rankings を読む**（14日で2回以上）。ここが止まると候補選定が静かに劣化する。

### ④ 私のミス: 同じ壊し方を2回した（`open(p,'wb')` の先評価）

`open(p,'wb').write(s.encode('utf-8'))` と1行で書き、**open が先に評価されてファイルを0バイトにし**、
その後 `encode` が例外で落ちた（絵文字をサロゲートペアのエスケープで書いたため）。
`jp-rankings.yml` が 2217バイト → 0バイトになり、GitHub の contents API から復元した。

🔑 **必ず `data = s.encode("utf-8")` を別の行で確定させてから `open(p,"wb")` を呼ぶ。**
🔑 **絵文字は `\uXXXX` で書かない。**リテラル文字で書くか、ヒアドキュメントのスクリプトファイル経由にする。


### ⑤ ローカルと本番の突き合わせ（reconcile）— 298件すべて一致させた

②で見つけた「ローカルが古い7件」を揃えた。**うち1件は逆向きで、本番のほうが誤りだった。**

| ファイル | 向き | 中身 |
|---|---|---|
| `generate_market_news.py` | 本番→ローカル | 9/1公開分の更新履歴19行 |
| `guides.html` | 本番→ローカル | 新着カード198行（Apple記事含む） |
| `drafts/idea-tested-slugs.txt` | 本番→ローカル | 8/24のバックログ一括登録13行 |
| `robots.txt`・記事2本 | 本番→ローカル | 改行/コメント文言の差 |
| `check_site_consistency.py` | **ローカル→本番** | **SYNC禁止リストの5行**（下記） |

🚨 **クラウドのルーティンがローカルの変更を静かに巻き戻した実例。**

- `1e60e2c4`（8/31 08:28 JST）＝ローカルから `economic-events.json` / `earnings-calendar.json` を
  **SYNC禁止リストに追加して push**（どちらもGitHub側のワークフローが生成する＝ローカルからpushしてはいけない）
- `fe45c502`（**その23分後**）＝**記事公開ルーティン**「リバランスとは何か 公開」が、
  **その5行だけを削除**（追加0・削除5）。記事公開に無関係な変更＝**古いクローンを書き戻した lost update**

**この向きは機械では判定できない。**同期ツールの「GitHub側が新しい＝ローカルが古い疑い」は正しい既定だが、
**時刻の新しさは正しさではない。**取り込む前に「本番側の変更が何だったか」をコミットのパッチで見ること。
今回は `--force` で正しい版を戻した（`908e667`）。⚠️ **同じ巻き戻しは再発しうる**——
ルーティンが古いクローンから `check_site_consistency.py` を書き戻す限り繰り返す。次に消えていたら、
消したコミットのパッチを見てから戻すこと。

**ミラー遅行も同時に解消:** `_pull_mirror.py --apply` で **53本**取り込み（ローカルに無い36本＋古い17本。
news 25 / signal-lab 9 / proverb 5 / auto 2 / その他12）。うち6本が `SYNC_FILES` 未登録だったので登録した
（`guide-index-vs-active` / `guide-rebalancing` / `guide-regret-aversion` /
`guide-scam-phishing-account-takeover` / `guide-shareholder-benefits` / `guide-trade-journal`）。

📌 **`_pull_mirror.py` は `guide-*.html` のクラウドレーン専用**で、今回の7件はどれも対象外だった
（`guides.html` は `guide-` で始まらない・`.py`/`.txt` は対象外・記事2本は `CLOUD_LANES` 外）。
**汎用の reconcile はまだ道具になっていない。**次に同じことをやるなら、
`SYNC_FILES` 全体を対象に「本番が新しい候補を出す→パッチを見せる→選んで取り込む」道具を作るのが早い。

**最終状態: `SYNC_FILES` 298件すべてローカル＝本番、`_pull_mirror.py` も「ミラーは最新」。**


### ⑥ `_reconcile.py` を作った（⑤の手作業を道具にした）

⑤でやった突き合わせは全部手作業だった（tree APIを叩いて sha を比較し、コミットのパッチを見て向きを決める）。
**このリポジトリでは routine が本番へ直接書く＝ローカルが遅れるのは構造的**なので、恒久ツールにした。

- **ローカル専用**（`_`接頭辞＝SYNC禁止）・既定 dry-run・`--apply` で取り込み・上書き前に `_reconcile_backup/` へ退避・冪等
- 判定は3つの sha: **L**=ローカル / **R**=本番 / **B**=`.sync-cache.json` の `remote_sha`（前回sync時点の本番）
  - `L != R` かつ `B != R` → **取り込み候補**（前回sync後に本番が動いた）
  - `L != R` かつ `B == R` → **ローカルが新しい**（push候補・取り込まない）
  - `B` 無し → 判定不能（fail-open）／ローカルに無い → 欠落
- 🚨 **主目的は「取り込むこと」ではなく「向きを間違えないこと」。**取り込む前に本番側のコミットを表示し、
  **追加0・削除のみのコミットに「⚠️ 巻き戻しの疑い」を付ける**（`--patch` でパッチ本体も表示）

**実データで検証済み。**`check_site_consistency.py` を候補に見せかけて走らせると、履歴が⑤の物語そのままに出た:

```
908e667d 2026-09-01T12:40:30Z  +5/-0  chore: sync 1 files from local
fe45c502 2026-08-30T23:51:02Z  +0/-5  feat: 新記事「リバランスとは何か」公開   ⚠️ 追加0の削除のみ＝巻き戻しの疑い
1e60e2c4 2026-08-30T23:28:21Z  +5/-0  chore: sync check_site_consistency.py from local
```

ローカルだけ変えた場合に `push候補` と出ることも実測で確認（`robots.txt` で試験→復元済み）。

⚠️ **`_pull_mirror.py` は残す。**役割が違う——あちらは `guide-*.html` のクラウドレーン（記事ミラーの遅行）専用で、
`SYNC_FILES` 全体は見ない。⑤で漏れた7件はどれも `_pull_mirror.py` の対象外だった。

