# 🚩 シグナル研究日誌 エスカレ回の復旧手順（人間編集・SYNC入り）

`signal-lab-daily`（毎朝 06:10 JST）が 🚩 で `drafts/REVIEW.md` にエスカレした回を、後日ちゃんと公開に
持っていくための手順。**2026-08-11 に「後日は構造的に直せない」問題を実測で潰した**ので、その使い方が本体。

> 関連: `CLAUDE.md`「シグナル研究日誌」節（レーンの全体像・SYNC禁忌）／`QUALITY_RUBRIC.md`（品質基準の単一ソース）

---

## 0. 最初に知っておくこと（ここを外すと必ず詰む）

**🔴 ライブログは動き続ける。凍結しないと正しい下書きでも RED になる。**

`claims.json` の k/n は**生成時点（朝）**のログで計算されるが、`signal_lab_verify.py` は
**実行時のライブログ**で再計算する。日中に決済が増えるだけで k/n はずれる。実測（2026-08-11）:

| 下書き | 凍結なし | 生成時点に凍結 |
|---|---|---|
| #067（当日夕方に実行） | **2/11 緑** | **11/11 緑** |
| #065（2日後に実行） | **0/6 緑** | **6/6 緑** |

⇒ **後日の検証は必ず `--asof` を付ける。** 付け忘れると「記事が間違っている」と誤診する。

```
python signal_lab_verify.py drafts/draft-signal-lab-NNN.html drafts/labnotes/lab-NNN-claims.json \
       --asof "<生成時刻 ISO8601>"
```

- `claims.json` に `"asof"` があればそちらが優先（routine が書くようになれば `--asof` は不要）
- 打ち切りは **`outcome_resolved_at`（決済確定時刻）**。`fired_at` ではない
  （朝より前に発火して日中に決済した玉を「朝も計上済み」と誤認するため）
- **asof の JST日付は記事が名乗る公開日と一致必須**（不一致は RED＝都合のよい断面を選ばせない）
- 生成時刻は `drafts/draft-signal-lab-NNN.html` の**最初のコミット時刻**を使う（GitHub の履歴が出典）

⚠️ **既知の限界**: 同一日内での断面選択の余地は残る（原理的に消せない）。捏造は不可（数字は実ログとの
完全一致が必要）だが、「どの瞬間で切るか」は選べる、という前提で読むこと。

---

## 1. 復旧の手順

1. **リモートから最新を取り込む**（drafts/ は GitHub 側が真実。⚠️ローカルが古いことがある）
   - `drafts/draft-signal-lab-NNN.html` / `drafts/labnotes/lab-NNN-claims.json`
   - ⚠️ **同じ番号でも中身が入れ替わっていることがある**（未公開のまま番号が消費されないため、
     翌朝の routine が**同じ番号で作り直す**。2026-08-11→12 に #067 で実際に発生）
2. **`drafts/REVIEW.md` の該当節を読む**。修正内容は具体値まで書かれている（表で正誤が示される）
3. **本文を修正**。数値を直したら **SVGのバー高さと%ラベルも再計算**（忘れやすい）
4. **`signal_lab_verify.py` を `--asof` 付きで再走** → EXIT=0 / 全claim緑 / 要約未検証0件
5. **Opusコンプラ → 独立Opus** の順に審査（🟡軽微は自己修正→数値再検証→別の独立Opusが白確認）
6. `finalize_signal_lab.py NNN` → `publish_article.py` → push

⚠️ **エンジン・発火条件・固定オラクルには触らない。** 直すのは記事本文だけ。

---

## 2. 日付の衝突（数日寝かせた回を公開するとき）

`date_check` は「datePublished ＝ JST今日」を要求する。一方 asof は「記事の公開日と同じ日付」を要求する。
**数日前の下書きをそのまま今日公開しようとすると、この2つが両立しない。**

対処はどちらかを選ぶ:

- **A. 生成日のまま公開（推奨）**: `SIGNAL_LAB_SKIP_DATE_CHECK=1` で date_check を免除し、
  `publish_article.py --allow-backdate` で公開。記事は「その日の分析」として残るので内容と日付が一致する
- **B. 今日の日付へ振り直す**: datePublished と本文の公開表記を今日に直す。ただし**その場合 asof も
  今日の日付でなければ通らない＝claims を今日のログで作り直す必要がある**＝実質は再生成

⇒ **寝かせた回は A**。B を選ぶくらいなら routine に作り直させたほうが早い。

---

## 3. レーンが「ループ」に入っていないかを見る

未公開のまま終わると**番号が消費されない**ので、翌朝 routine が**同じ番号で作り直す**。
別の理由でまたエスカレすると、同じ仮説を毎朝作り直し続ける。

実例（2026-08-11〜12 の #067）:

| 日 | 出来事 |
|---|---|
| 08-11 06:29 | draft 067 生成 → 06:40/06:52 に2回修正 → **07:02 🚩「独立Opus否・FWDデータ修正要」** |
| 08-12 06:29 | **同じ 067 を再生成** → 06:33 修正 → **06:37 🚩「IS/FWD混同・コンプラ黒」** |

⚠️ **再生成しても収束していない**（1日目は コンプラ🟢白→独立Opus否、2日目は コンプラ黒＝悪化）。
この形になったら「もう1日待つ」ではなく**人間が1本仕上げてループを抜く**か、
**その仮説を諦めて台帳の次番号を進める**かを決める。

🔑 **ループの原因がオラクルの表現力不足なら、キーを人間として正式拡張して塞ぐ**（実例=2026-08-12
`fired_before`/`fired_from` 追加・#067）。claims に期間の次元が無い間は「（IS）」列に全期間の数字を
書いても緑で通っていた（オラクルが数値照合するのは30秒まとめの%だけ）＝routine に何度作り直させても
同じ黒を踏む。制約を**増やす**方向の拡張は「固定オラクルを緩めない」原則と両立する。

番人＝`check_automation_health.py` §⑧ が 🚩 の滞留を **1日**で Issue 化する（`ESCALATION_STALE_DAYS`）。

---

## 4. 現在の宿題

**なし**（2026-08-12 時点）。直近の復旧実績＝**#065・#067 とも 2026-08-12 に公開完了**：

| 対象 | 復旧内容（実績） |
|---|---|
| **#065** | A案（`--allow-backdate`・8/9付け）で公開。免責3箇所＋軟化6件＋監査3巡の追加修正（計15件・#059行の -0.650→-0.417 誤帰属修正含む）。verify=asof凍結で 6/6 緑。監査=コンプラOpus→独立Opus×3巡で白 |
| **#067** | ループ原因=オラクルに期間次元が無い構造穴（§3の🔑）。`fired_before`/`fired_from` を正式拡張→claims 22件をIS/FWD分離で再構築（asof内蔵）→本文のIS列を真のIS値へ全面書き直し→22/22 緑→コンプラOpus→独立Opus白→8/12付け公開（backdate不要） |
